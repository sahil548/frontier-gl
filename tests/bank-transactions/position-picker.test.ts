import { describe, it } from "vitest";

describe("Positions API", () => {
  // CAT-01: Position picker / entity-wide positions list
  it.todo("returns all active positions across active holdings for entity");
  it.todo("includes holding name, type, account number in serialization");
  it.todo("excludes inactive positions and inactive holdings");
  it.todo("orders by holding name then position name");
});
