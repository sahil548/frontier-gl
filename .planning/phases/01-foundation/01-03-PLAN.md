---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/dashboard/page.tsx
  - src/app/(auth)/entities/page.tsx
  - src/app/(auth)/entities/new/page.tsx
  - src/app/(auth)/entities/[entityId]/page.tsx
  - src/app/(auth)/settings/page.tsx
  - src/components/layout/sidebar.tsx
  - src/components/layout/header.tsx
  - src/components/layout/entity-selector.tsx
  - src/components/layout/theme-toggle.tsx
  - src/components/entities/entity-form.tsx
  - src/components/entities/entity-type-badge.tsx
  - src/components/onboarding/welcome-screen.tsx
  - src/hooks/use-entity.ts
  - src/providers/entity-provider.tsx
  - src/__tests__/hooks/use-entity.test.ts
autonomous: false
requirements:
  - ENTM-03
  - ENTM-04
  - ENTM-05

must_haves:
  truths:
    - "User can switch between entities via header dropdown without page reload"
    - "Entity dropdown shows 'All Entities' pinned at top as default selection"
    - "Entities are grouped by type in the dropdown"
    - "Entity selection persists in localStorage across browser sessions"
    - "Deactivated entities are hidden from dropdown (not grayed out)"
    - "Sidebar collapses to icon-only mode and expands back"
    - "First-time user sees welcome screen with entity creation form"
    - "Dark mode toggles via sun/moon button in header"
  artifacts:
    - path: "src/components/layout/sidebar.tsx"
      provides: "Collapsible navigation sidebar with branding"
      min_lines: 60
    - path: "src/components/layout/header.tsx"
      provides: "Top bar with entity selector, theme toggle, user button"
      min_lines: 30
    - path: "src/components/layout/entity-selector.tsx"
      provides: "Entity switching dropdown"
      min_lines: 50
    - path: "src/hooks/use-entity.ts"
      provides: "Entity selection state + localStorage"
      min_lines: 20
    - path: "src/providers/entity-provider.tsx"
      provides: "React context for current entity"
      min_lines: 20
    - path: "src/components/onboarding/welcome-screen.tsx"
      provides: "First-login entity creation experience"
      min_lines: 30
  key_links:
    - from: "src/components/layout/entity-selector.tsx"
      to: "src/hooks/use-entity.ts"
      via: "useEntity hook for selection state"
      pattern: "useEntity"
    - from: "src/hooks/use-entity.ts"
      to: "localStorage"
      via: "Persist selection across sessions"
      pattern: "localStorage"
    - from: "src/components/layout/entity-selector.tsx"
      to: "/api/entities"
      via: "Fetch entity list for dropdown"
      pattern: "fetch.*api/entities"
    - from: "src/app/(auth)/layout.tsx"
      to: "src/providers/entity-provider.tsx"
      via: "EntityProvider wrapping authenticated routes"
      pattern: "EntityProvider"
---

<objective>
Build the complete application shell: collapsible sidebar with FOF branding, header bar with entity switcher and dark mode toggle, entity management pages (list, create, edit), onboarding welcome screen, and entity selection context with localStorage persistence.

Purpose: Deliver the interactive UI that users see and interact with. After this plan, users can create entities, switch between them, and navigate the app with the full layout in place.

Output: Complete layout shell, entity management UI, entity switcher with persistence, and onboarding flow.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from prior plans that the executor needs. -->

From src/types/index.ts:
```typescript
export type SerializedEntity = {
  id: string;
  name: string;
  type: string;
  typeOther: string | null;
  fiscalYearEnd: string;
  coaTemplate: string;
  isActive: boolean;
  createdAt: string;
  updatedAt: string;
}
```

From src/lib/validators/entity.ts:
```typescript
export const createEntitySchema = z.object({...});
export type CreateEntityInput = z.infer<typeof createEntitySchema>;
export type UpdateEntityInput = z.infer<typeof updateEntitySchema>;
```

From src/lib/validators/api-response.ts:
```typescript
export function successResponse<T>(data: T, status?: number): Response;
export function errorResponse(message: string, status?: number, errors?: z.ZodError): Response;
```

API Endpoints:
- GET /api/entities -> {success: true, data: SerializedEntity[]}
- POST /api/entities -> {success: true, data: SerializedEntity} (201)
- GET /api/entities/:entityId -> {success: true, data: SerializedEntity}
- PUT /api/entities/:entityId -> {success: true, data: SerializedEntity}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build entity context provider with localStorage persistence and collapsible sidebar layout</name>
  <files>
    src/hooks/use-entity.ts, src/providers/entity-provider.tsx,
    src/components/layout/sidebar.tsx, src/components/layout/header.tsx,
    src/components/layout/theme-toggle.tsx,
    src/app/(auth)/layout.tsx,
    src/__tests__/hooks/use-entity.test.ts
  </files>
  <behavior>
    - useEntity returns currentEntityId (default "all"), setCurrentEntityId, entities list, isLoading
    - setCurrentEntityId("xyz") persists "xyz" to localStorage key "frontier-gl-entity"
    - On mount, reads localStorage to restore previous selection
    - If stored entity ID is no longer in active entities list, falls back to "all"
    - "all" is always a valid selection (consolidated view)
  </behavior>
  <action>
    1. Create `src/hooks/use-entity.ts`:
       - State: currentEntityId (string, default "all"), entities (SerializedEntity[]), isLoading
       - On mount: fetch GET /api/entities to populate entities list
       - On mount: read localStorage key "frontier-gl-entity" for saved selection
       - setCurrentEntityId: update state + write to localStorage
       - If saved entity ID is not in the fetched active entities, fall back to "all"
       - Export useEntity hook and EntityContextType

    2. Create `src/providers/entity-provider.tsx`:
       - React context wrapping useEntity hook
       - Provider component that wraps children
       - useEntityContext() hook for consumers (throws if used outside provider)

    3. Create `src/components/layout/sidebar.tsx` -- Collapsible sidebar per locked decisions:
       - Toggle between expanded (icons + labels) and collapsed (icons only) mode
       - State: isCollapsed (boolean), persisted in localStorage key "frontier-gl-sidebar"
       - Header area: FOA ledger mark (teal) + "Frontier GL" text (hidden when collapsed, just icon)
       - Navigation items: Dashboard (LayoutDashboard icon), Entities (Building2 icon), Settings (Settings icon)
       - Active page highlight using usePathname()
       - Footer: "Powered by Family Office Frontier" text (hidden when collapsed)
       - Collapse toggle button (ChevronLeft/ChevronRight icon)
       - Use shadcn/ui Button, Tooltip (for collapsed icon labels), Separator
       - Use lucide-react icons throughout
       - Responsive: on mobile (< 768px), sidebar is a Sheet (overlay) triggered by hamburger in header

    4. Create `src/components/layout/theme-toggle.tsx`:
       - Sun/Moon icon button using next-themes useTheme()
       - Toggles between "light", "dark", and "system"
       - Use shadcn/ui DropdownMenu with three options: Light, Dark, System
       - Icons: Sun, Moon, Monitor from lucide-react

    5. Create `src/components/layout/header.tsx`:
       - Top bar spanning the content area (right of sidebar)
       - Left side: mobile hamburger menu trigger (visible < 768px), page title
       - Right side: EntitySelector component (built in Task 2), ThemeToggle, Clerk UserButton
       - Use Clerk's `<UserButton />` component for profile/logout

    6. Create `src/app/(auth)/layout.tsx`:
       - Wraps all authenticated pages
       - Renders EntityProvider > Sidebar + main content area with Header
       - Layout structure: flex row with sidebar on left, main content flex-1 on right
       - Main content: Header at top, page content below with padding

    7. Write tests FIRST for useEntity hook:
       - Mock localStorage (jsdom provides it)
       - Mock fetch for /api/entities
       - Test: default entityId is "all"
       - Test: setting entity persists to localStorage
       - Test: on mount, reads from localStorage
       - Test: invalid stored ID falls back to "all"
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx vitest run src/__tests__/hooks/use-entity.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>
    Entity context provides current entity selection with localStorage persistence. Sidebar renders with collapsible behavior, FOF branding, and navigation. Header shows theme toggle and Clerk UserButton. Authenticated layout wraps pages with sidebar + header + entity provider. useEntity tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build entity selector dropdown, entity management pages, and onboarding welcome screen</name>
  <files>
    src/components/layout/entity-selector.tsx,
    src/components/entities/entity-form.tsx,
    src/components/entities/entity-type-badge.tsx,
    src/components/onboarding/welcome-screen.tsx,
    src/app/(auth)/dashboard/page.tsx,
    src/app/(auth)/entities/page.tsx,
    src/app/(auth)/entities/new/page.tsx,
    src/app/(auth)/entities/[entityId]/page.tsx,
    src/app/(auth)/settings/page.tsx
  </files>
  <action>
    1. Create `src/components/layout/entity-selector.tsx`:
       - Uses useEntityContext() to get entities and currentEntityId
       - shadcn/ui Popover + Command component (like Vercel's team switcher -- fast, searchable)
       - "All Entities" pinned at top of list, separated by a Separator
       - Entities grouped by type below (group headers: "LPs", "LLCs", "Corporations", etc.)
       - Only show groups that have active entities
       - Deactivated entities: completely hidden (not grayed out) -- per locked decision
       - Show check mark next to currently selected entity
       - On select: call setCurrentEntityId, close popover -- stays on current page (no redirect)
       - Display current selection in the trigger button: entity name or "All Entities"
       - Use ChevronsUpDown icon for the trigger, Check icon for selected item

    2. Create `src/components/entities/entity-type-badge.tsx`:
       - Small colored badge displaying entity type (LP, LLC, etc.)
       - Use shadcn/ui Badge component with variant styles per type
       - Map entity types to readable labels (S_CORP -> "S-Corp", etc.)

    3. Create `src/components/entities/entity-form.tsx`:
       - Reusable form for create and edit entity
       - Uses react-hook-form with zodResolver and createEntitySchema/updateEntitySchema
       - Fields: name (Input), type (Select with all 9 EntityType options), typeOther (Input, shown only when type=OTHER), fiscalYearEnd (Input with MM-DD format helper, default "12-31"), coaTemplate (Select: "Start with template" / "Start blank")
       - Entity types displayed with readable labels: LP, LLC, Corporation, S-Corp, Trust, Foundation, Partnership, Individual, Other
       - Submit button with loading state
       - Mode prop: "create" or "edit" (edit pre-fills values, shows deactivate option)
       - On submit: POST or PUT to /api/entities, show success/error toast via sonner
       - On deactivate: PUT with {isActive: false}, confirm dialog first

    4. Create `src/components/onboarding/welcome-screen.tsx`:
       - Shown when user has zero entities (detected via useEntityContext)
       - "Welcome to Frontier GL" heading
       - "Create your first entity to get started" subtext
       - Embedded EntityForm in create mode
       - FOF branding: teal accent, clean layout
       - After entity creation, redirect to /dashboard

    5. Create pages:

       **`src/app/(auth)/dashboard/page.tsx`:**
       - Check if user has entities via API or context
       - If no entities: render WelcomeScreen (onboarding)
       - If has entities: render placeholder dashboard card saying "Dashboard coming in Phase 4" with quick links to Entities page
       - Display current entity name or "All Entities" as page context

       **`src/app/(auth)/entities/page.tsx`:**
       - Fetch entities from GET /api/entities
       - Display as a card grid or table showing: name, type badge, fiscal year end, active status
       - "Create Entity" button linking to /entities/new
       - Click entity row/card to go to /entities/[entityId] for editing

       **`src/app/(auth)/entities/new/page.tsx`:**
       - Render EntityForm in create mode
       - On success: redirect to /entities with success toast

       **`src/app/(auth)/entities/[entityId]/page.tsx`:**
       - Fetch entity by ID from GET /api/entities/:entityId
       - Render EntityForm in edit mode, pre-filled
       - Show deactivate button (with confirmation dialog)

       **`src/app/(auth)/settings/page.tsx`:**
       - Simple page with heading "Settings"
       - Sections: Appearance (current theme display -- the actual toggle is in header), About (app version, "Frontier GL by Family Office Frontier")
       - Placeholder for future settings

    6. Wire entity-selector.tsx into header.tsx (if not already done in Task 1).
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    Entity selector dropdown in header shows "All Entities" at top, groups entities by type, hides deactivated entities. Switching entities updates context without page reload. Entity form validates input with Zod (create and edit modes). Welcome screen appears for first-time users with zero entities. All pages render: dashboard (placeholder), entities (list with create/edit), settings. ENTM-03 (switch entities), ENTM-04 ("All Entities" option), ENTM-05 (localStorage persistence) are fully implemented.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1 application visually and interactively</name>
  <files>n/a</files>
  <action>
    Human verifies the complete Phase 1 application: authentication, entity CRUD, entity switcher with localStorage persistence, collapsible sidebar with FOF branding, dark mode, and onboarding flow.

    Steps to verify:
    1. Run `npm run dev` and navigate to http://localhost:3000
    2. Verify redirect to /sign-in (unauthenticated)
    3. Sign up or sign in via Clerk
    4. Verify welcome screen appears ("Create your first entity to get started")
    5. Create an entity: name "Test LP", type "LP", fiscal year "12-31", template "Blank"
    6. Verify redirect to dashboard after creation
    7. Create a second entity: name "Test Trust", type "Trust"
    8. Open entity selector dropdown in header -- verify "All Entities" is at top, entities grouped by type
    9. Switch between entities -- verify no page reload, data refreshes
    10. Toggle dark mode via sun/moon button -- verify FOF brand colors (teal #0D7377 accent, dark bg #0F1419)
    11. Collapse sidebar -- verify icon-only mode with just ledger mark
    12. Close browser, reopen -- verify same entity is selected (localStorage persistence)
    13. Navigate to Entities page -- verify entity list with type badges
    14. Edit an entity -- verify form pre-fills
    15. Deactivate an entity -- verify it disappears from dropdown
    16. Verify "Powered by Family Office Frontier" in sidebar footer
    17. Verify Plus Jakarta Sans font renders throughout
  </action>
  <verify>Human visual and interactive verification</verify>
  <done>User confirms all 17 verification steps pass. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. `npx vitest run --reporter=verbose` -- all tests pass (validators, response format, useEntity hook)
3. Full auth flow: sign-up -> sign-in -> protected routes -> sign-out
4. Entity CRUD: create -> list -> edit -> deactivate
5. Entity switcher: select entity -> switch -> localStorage persists -> page reload keeps selection
6. "All Entities" is default and always available
7. Sidebar collapses/expands with branding
8. Dark mode toggles correctly with FOF brand colors
9. Onboarding welcome screen shows for users with no entities
</verification>

<success_criteria>
- Collapsible sidebar with FOF branding (teal ledger mark + "Frontier GL")
- Entity selector in header top-right, always visible, grouped by type
- "All Entities" pinned at top of dropdown as default
- Switching entities stays on current page (no redirect/reload)
- Entity selection persists in localStorage across sessions
- Deactivated entities hidden from dropdown
- Welcome screen on first login with entity creation form
- Dark mode toggle in header with system preference default
- All Phase 1 pages: dashboard (placeholder), entities, settings
- Human verification confirms all visual and interactive behaviors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
