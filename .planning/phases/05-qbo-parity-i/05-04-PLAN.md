---
phase: 05-qbo-parity-i
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - src/components/layout/sidebar.tsx
  - src/app/(auth)/dashboard/page.tsx
  - src/app/(auth)/accounts/page.tsx
  - src/app/(auth)/journal-entries/page.tsx
  - src/app/(auth)/journal-entries/new/page.tsx
  - src/app/(auth)/journal-entries/[journalEntryId]/page.tsx
  - src/app/(auth)/trial-balance/page.tsx
  - src/app/(auth)/gl-ledger/page.tsx
  - src/app/(auth)/reports/page.tsx
  - src/app/(auth)/holdings/page.tsx
  - src/app/(auth)/period-close/page.tsx
  - src/app/(auth)/recurring/page.tsx
  - src/app/(auth)/settings/page.tsx
  - src/components/journal-entries/je-form.tsx
  - src/components/journal-entries/je-line-items.tsx
  - src/components/data-table/data-table.tsx
autonomous: false
requirements: [UI-02]

must_haves:
  truths:
    - "Every page is usable on a 375px mobile screen with no horizontal overflow"
    - "Sidebar collapses to hamburger menu on mobile (already works)"
    - "Data tables have horizontal scroll with first column pinned"
    - "Forms stack vertically on mobile"
    - "JE line items display as vertical cards on mobile instead of spreadsheet rows"
    - "Dashboard charts stack vertically on mobile"
    - "No clipped content on any page at 375px"
  artifacts:
    - path: "src/components/data-table/data-table.tsx"
      provides: "Mobile-responsive data table wrapper with pinned first column"
    - path: "src/components/journal-entries/je-line-items.tsx"
      provides: "Mobile card layout for JE line items"
  key_links:
    - from: "src/components/data-table/data-table.tsx"
      to: "all data table pages"
      via: "shared component with sticky first column on mobile"
      pattern: "sticky left-0|overflow-x-auto"
    - from: "src/components/journal-entries/je-line-items.tsx"
      to: "JE form"
      via: "mobile card layout rendered below sm breakpoint"
      pattern: "sm:hidden|hidden sm:block"
---

<objective>
Systematic mobile responsiveness pass over every page in the application, ensuring usability at 375px (iPhone SE) minimum width with no horizontal overflow, pinned-column tables, and vertical card layouts for forms.

Purpose: Delivers UI-02 -- the app works correctly on tablet and mobile screens. This goes last in the phase because it must account for all UI added in plans 01-03 (charts, attachments, recurring page).
Output: Every page tested and working at 375px width.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-qbo-parity-i/05-CONTEXT.md
@.planning/phases/05-qbo-parity-i/05-RESEARCH.md
@src/components/data-table/data-table.tsx
@src/components/journal-entries/je-form.tsx
@src/components/journal-entries/je-line-items.tsx
@src/app/(auth)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data table mobile pattern and form responsive layouts</name>
  <files>src/components/data-table/data-table.tsx, src/components/journal-entries/je-line-items.tsx, src/components/journal-entries/je-form.tsx</files>
  <action>
1. Update `src/components/data-table/data-table.tsx` (the shared DataTable component):
   - Wrap the Table in `<div className="overflow-x-auto">` to enable horizontal scroll on mobile
   - Add `sticky left-0 z-10 bg-background` classes to the first TableHead and first TableCell in each row to pin the first column
   - Ensure the wrapper has `min-w-full` and the table uses `w-max min-w-full` so it scrolls when content exceeds viewport
   - This automatically applies to: COA page, JE list, GL Ledger, Trial Balance, Holdings -- all pages using the shared DataTable

2. Update `src/components/journal-entries/je-line-items.tsx`:
   - Add mobile card layout: below `sm:` breakpoint, render each line item as a vertical Card instead of a table row
   - Desktop (sm and up): keep existing table row layout using `hidden sm:table-row` on TR elements
   - Mobile (below sm): show `sm:hidden` card per line item with stacked fields:
     - Account (full width combobox)
     - Debit / Credit side-by-side in a 2-column grid
     - Memo (full width input)
     - Remove button
   - Use the pattern: `<div className="sm:hidden space-y-3">` for mobile cards, `<table className="hidden sm:table">` for desktop table

3. Update `src/components/journal-entries/je-form.tsx`:
   - Ensure the JE form header fields (date, description, entry number) use responsive grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`
   - Buttons at bottom should wrap on mobile: `flex flex-wrap gap-2`
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <done>DataTable has horizontal scroll with pinned first column on mobile. JE line items render as vertical cards on mobile. Form layouts stack vertically on small screens.</done>
</task>

<task type="auto">
  <name>Task 2: Page-by-page responsive fixes across all routes</name>
  <files>src/app/(auth)/dashboard/page.tsx, src/app/(auth)/accounts/page.tsx, src/app/(auth)/journal-entries/page.tsx, src/app/(auth)/journal-entries/[journalEntryId]/page.tsx, src/app/(auth)/trial-balance/page.tsx, src/app/(auth)/gl-ledger/page.tsx, src/app/(auth)/reports/page.tsx, src/app/(auth)/holdings/page.tsx, src/app/(auth)/period-close/page.tsx, src/app/(auth)/recurring/page.tsx, src/app/(auth)/settings/page.tsx, src/app/(auth)/entities/page.tsx</files>
  <action>
Systematically review and fix each page for 375px mobile compatibility. For each page:

**Common patterns to apply:**
- Page title + action buttons: `flex flex-col sm:flex-row sm:items-center sm:justify-between gap-2` (stack on mobile, row on desktop)
- Card grids: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4` (single column on mobile)
- Padding: `px-4 sm:px-6` (tighter on mobile)
- Text truncation: `truncate` on long text in tight spaces
- Button groups: `flex flex-wrap gap-2` instead of fixed row

**Page-specific fixes:**

1. **Dashboard** (`dashboard/page.tsx`): Summary cards already use responsive grid. Ensure chart grid (from plan 01) uses `grid-cols-1 lg:grid-cols-3`. Period selector and nav links should wrap.

2. **COA** (`accounts/page.tsx`): DataTable fix handles table. Ensure header actions (create button, search) wrap on mobile.

3. **JE List** (`journal-entries/page.tsx`): DataTable fix handles table. Ensure filter row and bulk action bar wrap on mobile.

4. **JE Detail** (`journal-entries/[journalEntryId]/page.tsx`): JE form fix handles form. Ensure attachments section (plan 02) and audit trail section have proper padding. Status badge and action buttons should wrap.

5. **Trial Balance** (`trial-balance/page.tsx`): If using a manual table (not DataTable), add overflow-x-auto wrapper with sticky first column. Ensure filter controls wrap.

6. **GL Ledger** (`gl-ledger/page.tsx`): DataTable handles table. Ensure date range pickers and filter controls wrap on mobile.

7. **Reports** (`reports/page.tsx`): Financial statement tables need overflow-x-auto. Tab navigation should be scrollable if too wide. Ensure export buttons wrap.

8. **Holdings** (`holdings/page.tsx`): DataTable handles table. Ensure holding detail cards stack on mobile.

9. **Period Close** (`period-close/page.tsx`): Month grid should use `grid-cols-2 sm:grid-cols-3 lg:grid-cols-4`. Action buttons wrap.

10. **Recurring** (`recurring/page.tsx`): Table from plan 03 already uses DataTable pattern. Ensure generate button and header wrap.

11. **Settings** (`settings/page.tsx`): Form fields stack. Team member list wraps.

12. **Entities** (`entities/page.tsx`): Entity cards stack to single column on mobile.

**DO NOT:**
- Add `overflow-x-hidden` to the body or main layout (hides important content)
- Set fixed widths that break mobile
- Use custom media queries -- stick to Tailwind responsive prefixes
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <done>Every page renders correctly at 375px. No horizontal overflow. Tables scroll with pinned columns. Forms stack vertically. Buttons wrap. Cards use single-column layout on mobile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of mobile responsiveness at 375px</name>
  <files>all pages listed in Task 2</files>
  <action>
Human verifies mobile responsiveness across all pages at 375px viewport width. All automated implementation is complete in Tasks 1 and 2.

What was built: Mobile responsiveness across all pages -- pinned-column tables, card-style JE line items, stacked layouts, and no horizontal overflow at 375px.

How to verify:
1. Run `npm run dev` and open the app in Chrome
2. Open DevTools (F12), toggle device toolbar (Ctrl+Shift+M), set viewport to 375px width (iPhone SE)
3. Navigate through each page and verify:
   - Dashboard: summary cards and charts stack vertically, no overflow
   - Chart of Accounts: table scrolls horizontally, first column stays pinned
   - Journal Entries list: table scrolls, filters wrap
   - JE Detail: form fields stack, line items show as cards, attachments section fits
   - New JE: line items render as vertical cards, form fields stack
   - Trial Balance: table scrolls with pinned column
   - GL Ledger: table scrolls, date filters wrap
   - Reports: financial tables scroll, tabs accessible
   - Holdings: cards stack, tables scroll
   - Period Close: month grid 2 columns
   - Recurring: table scrolls, buttons wrap
   - Settings: form stacks
4. On each page: no horizontal scrollbar on the body (table scroll containers are OK)

Resume signal: Type "approved" or describe pages with issues.
  </action>
  <verify>
    <automated>echo "Human visual verification checkpoint"</automated>
  </verify>
  <done>Human has verified all pages are usable at 375px with no horizontal overflow, pinned columns in tables, and stacked layouts on mobile.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- All pages visually verified at 375px viewport width
- No horizontal overflow on any page body
- Tables have horizontal scroll with pinned first column
- Forms stack vertically
- JE line items render as cards on mobile
</verification>

<success_criteria>
Every page in the application is usable on a 375px mobile screen. No horizontal overflow, no clipped content. Sidebar collapses to hamburger menu. Data tables scroll with pinned first columns. Forms stack vertically. JE line items display as cards on mobile.
</success_criteria>

<output>
After completion, create `.planning/phases/05-qbo-parity-i/05-04-SUMMARY.md`
</output>
