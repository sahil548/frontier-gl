---
phase: 05-qbo-parity-i
plan: 03
type: execute
wave: 1
depends_on: ["05-00"]
files_modified:
  - src/components/journal-entries/je-audit-trail.tsx
  - src/app/(auth)/recurring/page.tsx
  - src/components/recurring/recurring-templates-table.tsx
  - src/components/recurring/recurring-edit-dialog.tsx
  - src/app/api/entities/[entityId]/templates/recurring/route.ts
  - src/components/layout/sidebar.tsx
  - src/app/(auth)/journal-entries/[journalEntryId]/page.tsx
  - src/components/journal-entries/je-status-badge.tsx
autonomous: true
requirements: [AUDT-01, AUDT-02, RECR-01, RECR-02, RECR-03, RECR-04, RECR-05]

must_haves:
  truths:
    - "JE detail page shows collapsible audit trail with all status changes and timestamps"
    - "Audit trail shows field-level diffs for edits (field: old -> new)"
    - "Sidebar has a Recurring nav item"
    - "Recurring page lists all recurring templates with name, frequency, next run date, status"
    - "User can click Generate Due Entries to create draft JEs from overdue templates"
    - "User can edit frequency, next run date, name, and template lines on active templates"
    - "User can stop a recurring template"
    - "Generated draft JEs show a Recurring badge linking to source template"
  artifacts:
    - path: "src/components/journal-entries/je-audit-trail.tsx"
      provides: "Enhanced audit trail with field-level diff display"
      exports: ["JEAuditTrail"]
    - path: "src/app/(auth)/recurring/page.tsx"
      provides: "Dedicated recurring templates management page"
    - path: "src/components/recurring/recurring-templates-table.tsx"
      provides: "TanStack Table listing recurring templates"
      exports: ["RecurringTemplatesTable"]
    - path: "src/components/recurring/recurring-edit-dialog.tsx"
      provides: "Dialog for editing recurring template frequency, scheduling, and line items"
      exports: ["RecurringEditDialog"]
    - path: "src/app/api/entities/[entityId]/templates/recurring/route.ts"
      provides: "Enhanced recurring API with PATCH for editing"
      exports: ["GET", "POST", "PATCH"]
  key_links:
    - from: "src/app/(auth)/recurring/page.tsx"
      to: "/api/entities/{entityId}/templates/recurring"
      via: "fetch for list, POST for generate/setup/stop, PATCH for edit"
      pattern: "fetch.*templates/recurring"
    - from: "src/components/journal-entries/je-audit-trail.tsx"
      to: "JournalEntryAudit.changes JSON"
      via: "formatFieldDiffs parses changes object for old/new values"
      pattern: "formatFieldDiffs|formatChanges"
    - from: "src/components/layout/sidebar.tsx"
      to: "src/app/(auth)/recurring/page.tsx"
      via: "navItems entry with RefreshCw icon"
      pattern: "recurring.*RefreshCw"
---

<objective>
Enhance the audit trail with field-level diff display and build a dedicated recurring JE management page with list, generate, edit (including template lines), and stop capabilities.

Purpose: Delivers AUDT-01/02 (full audit trail with field diffs) and RECR-01-05 (recurring JE management UI) -- the two feature areas that extend existing components.
Output: Enhanced audit trail on JE detail, new /recurring page with full template management including line item editing.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-qbo-parity-i/05-CONTEXT.md
@.planning/phases/05-qbo-parity-i/05-RESEARCH.md
@src/components/journal-entries/je-audit-trail.tsx
@src/components/journal-entries/recurring-manager.tsx
@src/app/api/entities/[entityId]/templates/recurring/route.ts
@src/components/layout/sidebar.tsx
@src/app/(auth)/journal-entries/[journalEntryId]/page.tsx
@src/components/journal-entries/je-line-items.tsx

<interfaces>
From src/components/journal-entries/je-audit-trail.tsx:
```typescript
type AuditEntry = {
  id: string;
  action: string;
  userId: string;
  changes: unknown;
  createdAt: string;
};

type JEAuditTrailProps = {
  entries: AuditEntry[];
};

// Existing formatChanges returns string | null from changes object
// Expects Record<string, { old: string; new: string }>
```

From src/components/journal-entries/recurring-manager.tsx:
```typescript
interface Template {
  id: string;
  name: string;
  description: string | null;
  isRecurring: boolean;
  frequency: string | null;
  nextRunDate: string | null;
  lastRunDate: string | null;
  lines: Array<{ id: string }>;
}

interface RecurringManagerProps {
  entityId: string;
  onEntriesGenerated?: () => void;
}
```

From src/components/layout/sidebar.tsx (navItems, line 34-44):
```typescript
const navItems = [
  { href: "/dashboard", label: "Dashboard", icon: LayoutDashboard },
  // ... existing items
  { href: "/settings", label: "Settings", icon: Settings },
];
```

From src/components/journal-entries/je-line-items.tsx:
```typescript
export function JELineItems({ entityId, disabled }: JELineItemsProps)
// Renders line items with account combobox, debit, credit, memo, remove button
// Uses React Hook Form useFieldArray pattern
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance audit trail with field-level diffs and update JE edit API to capture diffs</name>
  <files>src/components/journal-entries/je-audit-trail.tsx, src/app/api/entities/[entityId]/journal-entries/[journalEntryId]/route.ts</files>
  <action>
1. Update `src/components/journal-entries/je-audit-trail.tsx`:
   - Keep existing formatChanges() for backward compatibility but add a new `formatFieldDiffs()` function that returns structured diffs: `{ field: string; old: string; new: string }[] | null`
   - In the timeline rendering, when action is "EDITED" and changes exist, render field-level diffs below the action label:
     ```
     field: old_value -> new_value
     ```
   - Style: field name in `font-medium text-muted-foreground`, old value with `line-through text-red-500/70`, arrow in `text-muted-foreground`, new value in `text-green-600`
   - Each diff on its own line, wrapped in a `text-xs` container
   - For non-EDITED actions, keep existing behavior (just show action label + timestamp)
   - AUDT-01 is already implemented (status change timeline) -- verify the existing component renders CREATED, APPROVED, POSTED actions with user and timestamp. If any gaps, fill them.

2. Update the JE PATCH/PUT route at `src/app/api/entities/[entityId]/journal-entries/[journalEntryId]/route.ts`:
   - Before updating the journal entry, snapshot the current values of editable fields (description, date, line items)
   - After update, diff old vs new values
   - Store diffs in the audit entry's `changes` field as `{ fieldName: { old: "value", new: "value" } }` format
   - For line item changes, format as "Line N account/debit/credit" with old and new values
   - This ensures AUDT-02 data is captured -- without this, the UI has nothing to display
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <done>Audit trail displays field-level diffs for EDITED actions in "field: old -> new" format with styled highlighting. JE edit API captures before/after values in audit changes JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Recurring JE management page with list, generate, edit (including template lines), stop, and badge</name>
  <files>src/app/(auth)/recurring/page.tsx, src/components/recurring/recurring-templates-table.tsx, src/components/recurring/recurring-edit-dialog.tsx, src/app/api/entities/[entityId]/templates/recurring/route.ts, src/components/layout/sidebar.tsx, src/components/journal-entries/je-status-badge.tsx, src/components/journal-entries/je-list.tsx</files>
  <action>
1. Add "Recurring" nav item to sidebar.tsx:
   - Import RefreshCw from lucide-react
   - Add `{ href: "/recurring", label: "Recurring", icon: RefreshCw }` after the "Journal Entries" item in navItems array

2. Update the recurring API route `src/app/api/entities/[entityId]/templates/recurring/route.ts`:
   - Ensure GET returns all recurring templates for the entity with fields: id, name, description, frequency, nextRunDate, lastRunDate, isRecurring, lines (with account details including accountId, account name, debit, credit, memo), plus a computed `status` field: "active" if isRecurring && nextRunDate exists, "stopped" if isRecurring && no nextRunDate, "overdue" if nextRunDate < today
   - Add PATCH handler: accepts `{ templateId, frequency?, nextRunDate?, name?, description?, lines?: Array<{ accountId, debit, credit, memo }> }`. Updates template fields. If lines provided, delete existing lines and recreate. Only allow editing active recurring templates. Return updated template.
   - Ensure POST "generate" action creates JEs with `templateId` foreign key set (the FK added in plan 02's schema migration) so generated entries link back to their source template. Also set description to include "(recurring)" marker.
   - Ensure POST "stop" action sets nextRunDate to null and isRecurring to false.

3. Create `src/components/recurring/recurring-templates-table.tsx`:
   - "use client" component
   - Props: `templates: RecurringTemplate[]`, `onGenerate: () => void`, `onEdit: (template) => void`, `onStop: (templateId: string) => void`
   - TanStack Table with columns: Name, Frequency (capitalized), Next Run Date (formatted with date-fns), Last Run Date, Status (Badge: green for active, yellow for overdue, gray for stopped), Actions (Edit, Stop buttons)
   - Sortable by name and next run date
   - Empty state: "No recurring templates. Set up recurring from a JE template."

4. Create `src/components/recurring/recurring-edit-dialog.tsx`:
   - "use client" component
   - Props: `template: RecurringTemplate | null`, `entityId: string`, `open: boolean`, `onOpenChange: (open: boolean) => void`, `onSaved: () => void`
   - shadcn Dialog with form fields:
     - **Name** (text input)
     - **Frequency** (Select: monthly/quarterly/annually)
     - **Next Run Date** (date input)
     - **Template Lines** (inline editable rows, same pattern as JE form's line items):
       - Each row: Account (GL account combobox using the same account selector pattern from je-line-items.tsx), Debit (number input), Credit (number input), Memo (text input)
       - "Add Line" button to append a new blank row
       - Remove button (X/trash icon) on each row to delete it
       - Display running debit/credit totals below the line items
       - Pre-populate with existing template lines when editing
   - Per user decision (RECR-04): full edit capability on active recurring templates includes frequency, next run date, template lines, and name
   - On save: PATCH to recurring API with all fields including lines array, toast success, call onSaved

5. Create `src/app/(auth)/recurring/page.tsx`:
   - "use client" page
   - Uses useEntityContext for entityId
   - Fetches recurring templates on mount from GET /api/entities/{entityId}/templates/recurring
   - Renders page title "Recurring Journal Entries" with description
   - "Generate Due Entries" button at top: POST to recurring API with action "generate", show toast "Generated N draft entries" (get count from response), refresh list
   - Renders RecurringTemplatesTable with templates data
   - Handles edit (opens RecurringEditDialog) and stop (confirm then POST stop action) callbacks
   - Loading skeleton while fetching

6. Add "Recurring" badge to generated JEs:
   - In `src/components/journal-entries/je-list.tsx` or `je-status-badge.tsx`: when a JE has a templateId (non-null), show a small "Recurring" Badge (variant outline, with RefreshCw icon) next to the status badge
   - The badge should link to `/recurring` page
   - Check how the JE list fetches data -- may need to include templateId in the JE list API response (add to the select/include in the journal-entries list API if not already there)
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <done>Sidebar has Recurring nav item. /recurring page lists all recurring templates with status badges. User can generate due entries (toast feedback), edit frequency/schedule/name/template lines (inline add/remove rows), and stop recurrence. Generated JEs show Recurring badge in JE list.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no type errors
- Sidebar includes "Recurring" nav item with RefreshCw icon
- Recurring page renders with template list table
- RecurringEditDialog allows editing name, frequency, next run date, AND template line items (add/remove rows with account/debit/credit/memo)
- Audit trail renders field-level diffs for EDITED actions
- JE list shows Recurring badge for template-generated entries
</verification>

<success_criteria>
1. JE detail audit trail shows status changes with timestamps (AUDT-01) and field-level diffs for edits (AUDT-02).
2. Recurring page lists all recurring templates with name, frequency, next run date, status.
3. Generate Due Entries creates draft JEs and shows toast feedback.
4. User can edit frequency, schedule, name, and template lines (inline row editing) and stop recurrence.
5. Generated JEs display Recurring badge linking to source template.
</success_criteria>

<output>
After completion, create `.planning/phases/05-qbo-parity-i/05-03-SUMMARY.md`
</output>
