---
phase: 05-qbo-parity-i
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/dashboard/chart-data.test.ts
  - tests/audit/field-diff.test.ts
  - tests/attachments/upload.test.ts
  - tests/attachments/list.test.ts
  - tests/attachments/blob-storage.test.ts
  - tests/recurring/setup.test.ts
  - tests/recurring/list.test.ts
  - tests/recurring/generate.test.ts
  - tests/recurring/edit-stop.test.ts
  - tests/recurring/generated-link.test.ts
autonomous: true
requirements: [DASH-03, AUDT-02, ATTCH-01, ATTCH-02, ATTCH-03, RECR-01, RECR-02, RECR-03, RECR-04, RECR-05]

must_haves:
  truths:
    - "All 10 test stub files exist with describe/it blocks that fail (RED state)"
    - "npx vitest run completes without config errors"
  artifacts:
    - path: "tests/dashboard/chart-data.test.ts"
      provides: "Stub tests for DASH-03 chart data API"
    - path: "tests/audit/field-diff.test.ts"
      provides: "Stub tests for AUDT-02 field-level diffs"
    - path: "tests/attachments/upload.test.ts"
      provides: "Stub tests for ATTCH-01 upload flow"
    - path: "tests/attachments/list.test.ts"
      provides: "Stub tests for ATTCH-02 listing"
    - path: "tests/attachments/blob-storage.test.ts"
      provides: "Stub tests for ATTCH-03 Vercel Blob integration"
    - path: "tests/recurring/setup.test.ts"
      provides: "Stub tests for RECR-01 setup recurring"
    - path: "tests/recurring/list.test.ts"
      provides: "Stub tests for RECR-02 list templates"
    - path: "tests/recurring/generate.test.ts"
      provides: "Stub tests for RECR-03 generate due entries"
    - path: "tests/recurring/edit-stop.test.ts"
      provides: "Stub tests for RECR-04 edit/stop recurring"
    - path: "tests/recurring/generated-link.test.ts"
      provides: "Stub tests for RECR-05 generated entries linked to template"
  key_links: []
---

<objective>
Create all 10 test stub files required by the Validation Strategy before Wave 1 plans execute. Each stub contains describe/it blocks with `expect(true).toBe(false)` (RED state) so they fail until the corresponding plan implements the feature.

Purpose: Nyquist compliance -- every plan task has a pre-existing test file to validate against.
Output: 10 test stub files in tests/ directory, all initially failing.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-qbo-parity-i/05-VALIDATION.md
@.planning/phases/05-qbo-parity-i/05-RESEARCH.md
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 10 test stub files with failing describe/it blocks</name>
  <files>tests/dashboard/chart-data.test.ts, tests/audit/field-diff.test.ts, tests/attachments/upload.test.ts, tests/attachments/list.test.ts, tests/attachments/blob-storage.test.ts, tests/recurring/setup.test.ts, tests/recurring/list.test.ts, tests/recurring/generate.test.ts, tests/recurring/edit-stop.test.ts, tests/recurring/generated-link.test.ts</files>
  <action>
Create 10 test stub files. Each file follows this pattern:

```typescript
import { describe, it, expect } from "vitest";

describe("[Feature Area]", () => {
  it("[behavior description]", () => {
    // TODO: implement in plan 05-XX
    expect(true).toBe(false);
  });
});
```

Specific files and their describe/it blocks:

1. `tests/dashboard/chart-data.test.ts` (DASH-03):
   - describe "Dashboard chart data API"
   - it "returns asset breakdown with name and value fields"
   - it "returns income vs expense totals for selected period"
   - it "returns equity trend with monthly data points"

2. `tests/audit/field-diff.test.ts` (AUDT-02):
   - describe "Audit trail field-level diffs"
   - it "formats field diffs from changes JSON into old/new pairs"
   - it "returns null for empty or missing changes"
   - it "handles line item changes with account/debit/credit diffs"

3. `tests/attachments/upload.test.ts` (ATTCH-01):
   - describe "Attachment upload"
   - it "accepts PDF files under 10MB"
   - it "accepts PNG/JPG/HEIC image files"
   - it "rejects files over 10MB"
   - it "rejects unsupported file types"

4. `tests/attachments/list.test.ts` (ATTCH-02):
   - describe "Attachment listing"
   - it "returns attachments for a journal entry ordered by createdAt desc"
   - it "returns empty array when no attachments exist"

5. `tests/attachments/blob-storage.test.ts` (ATTCH-03):
   - describe "Vercel Blob storage integration"
   - it "uploads file to correct blob path"
   - it "deletes blob when attachment is removed"

6. `tests/recurring/setup.test.ts` (RECR-01):
   - describe "Recurring template setup"
   - it "marks template as recurring with frequency and start date"

7. `tests/recurring/list.test.ts` (RECR-02):
   - describe "Recurring template listing"
   - it "returns all recurring templates with status fields"
   - it "computes active/stopped/overdue status correctly"

8. `tests/recurring/generate.test.ts` (RECR-03):
   - describe "Generate due entries"
   - it "creates draft JEs from overdue recurring templates"
   - it "updates lastRunDate and advances nextRunDate after generation"

9. `tests/recurring/edit-stop.test.ts` (RECR-04):
   - describe "Edit and stop recurring template"
   - it "updates frequency and next run date on active template"
   - it "updates template line items"
   - it "stops recurrence by clearing nextRunDate and isRecurring"

10. `tests/recurring/generated-link.test.ts` (RECR-05):
    - describe "Generated entries linked to template"
    - it "generated JE has templateId set to source template"
    - it "generated JE description contains recurring marker"

Create the directories `tests/dashboard/`, `tests/audit/`, `tests/attachments/`, `tests/recurring/` if they do not exist.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx vitest run tests/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All 10 test files exist with failing it() blocks. `npx vitest run tests/` runs without config errors and reports all tests as FAIL (RED state).</done>
</task>

</tasks>

<verification>
- All 10 test files exist in the correct directories
- `npx vitest run tests/` executes without import or config errors
- All tests report FAIL (RED state -- expect(true).toBe(false))
</verification>

<success_criteria>
10 test stub files created in tests/ directory matching the VALIDATION.md Wave 0 requirements. All tests run and fail, confirming Nyquist compliance readiness for Wave 1 plans.
</success_criteria>

<output>
After completion, create `.planning/phases/05-qbo-parity-i/05-00-SUMMARY.md`
</output>
