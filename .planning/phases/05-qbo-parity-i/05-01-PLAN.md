---
phase: 05-qbo-parity-i
plan: 01
type: execute
wave: 1
depends_on: ["05-00"]
files_modified:
  - prisma/schema.prisma
  - src/app/api/entities/[entityId]/dashboard/route.ts
  - src/components/dashboard/asset-pie-chart.tsx
  - src/components/dashboard/income-expense-bar.tsx
  - src/components/dashboard/equity-trend-area.tsx
  - src/app/(auth)/dashboard/page.tsx
autonomous: true
requirements: [DASH-03]

must_haves:
  truths:
    - "Dashboard displays asset breakdown pie chart below summary cards"
    - "Dashboard displays income vs expense bar chart"
    - "Dashboard displays equity trend area chart"
    - "Charts update when user changes period selector"
    - "Clicking a pie slice or bar navigates to the relevant report/ledger"
  artifacts:
    - path: "src/components/dashboard/asset-pie-chart.tsx"
      provides: "Pie chart showing asset breakdown by top-level asset accounts"
      exports: ["AssetPieChart"]
    - path: "src/components/dashboard/income-expense-bar.tsx"
      provides: "Bar chart showing income vs expense totals"
      exports: ["IncomeExpenseBar"]
    - path: "src/components/dashboard/equity-trend-area.tsx"
      provides: "Area chart showing equity trend over time"
      exports: ["EquityTrendArea"]
    - path: "src/app/api/entities/[entityId]/dashboard/route.ts"
      provides: "Chart data in dashboard API response"
  key_links:
    - from: "src/app/(auth)/dashboard/page.tsx"
      to: "src/components/dashboard/*-chart.tsx"
      via: "imports and renders chart components with fetched data"
      pattern: "import.*from.*dashboard/"
    - from: "src/app/(auth)/dashboard/page.tsx"
      to: "/api/entities/{entityId}/dashboard"
      via: "fetch with period params"
      pattern: "fetch.*dashboard"
---

<objective>
Install Recharts, extend the dashboard API to return chart data (asset breakdown, income vs expense, equity trend), and create three chart components rendered below the existing summary cards.

Purpose: Delivers DASH-03 -- the three dashboard mini charts that give accountants at-a-glance financial visualization.
Output: Three chart components on the dashboard page with drill-down navigation and teal palette theming.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-qbo-parity-i/05-CONTEXT.md
@.planning/phases/05-qbo-parity-i/05-RESEARCH.md
@src/app/(auth)/dashboard/page.tsx
@src/app/api/entities/[entityId]/dashboard/route.ts
@src/lib/utils/accounting.ts

<interfaces>
<!-- Dashboard page DashboardData type (extend with chart fields) -->
From src/app/(auth)/dashboard/page.tsx:
```typescript
interface DashboardData {
  summary: {
    totalAssets: number;
    totalLiabilities: number;
    totalEquity: number;
    netIncome: number;
  };
  recentEntries: Array<{
    id: string;
    entryNumber: string;
    date: string;
    description: string;
    status: string;
    totalAmount: number;
  }>;
  periodStatus: {
    year: number;
    month: number;
    isClosed: boolean;
  };
}
```

From src/app/api/entities/[entityId]/dashboard/route.ts:
```typescript
// Uses: auth(), prisma, Prisma.sql, successResponse, errorResponse, getAccessibleEntityIds
// Query params: consolidated, incomeStart, incomeEnd
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts and extend dashboard API with chart data</name>
  <files>package.json, src/app/api/entities/[entityId]/dashboard/route.ts</files>
  <action>
1. Run `npm install recharts` to add the charting library.

2. Extend the GET handler in the dashboard API route to return three additional data fields alongside the existing summary/recentEntries/periodStatus:

   a. `assetBreakdown: { name: string; value: number }[]` -- Query top-level ASSET accounts (parentId IS NULL, isActive=true) grouped by account name, with ABS(SUM(debit - credit)) as value. Filter to POSTED JEs within the income date range. Limit to top 8. Only include accounts with non-zero balances.

   b. `incomeVsExpense: { category: string; amount: number }[]` -- Two rows: one for total REVENUE (SUM(credit - debit)) and one for total EXPENSE (SUM(debit - credit)) within the income date range. Already have similar logic in the existing summary query -- reuse the pattern.

   c. `equityTrend: { month: string; equity: number }[]` -- Monthly cumulative equity balance over the last 12 months. Use a window function or cumulative SUM approach: for each month, sum all EQUITY account transactions (credit - debit) from the beginning of time up to that month-end. Format month as "YYYY-MM". Only include POSTED JEs.

Use the existing `Prisma.sql` tagged template and `Prisma.join` patterns already in the codebase. Scope all queries to the resolved entity IDs (same consolidated logic already in the route).
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx tsx -e "import('./src/app/api/entities/[entityId]/dashboard/route.ts').then(() => console.log('compiles'))" 2>&1 | head -5; npm ls recharts</automated>
  </verify>
  <done>Recharts installed. Dashboard API returns assetBreakdown, incomeVsExpense, and equityTrend arrays alongside existing data.</done>
</task>

<task type="auto">
  <name>Task 2: Create chart components and integrate into dashboard page</name>
  <files>src/components/dashboard/asset-pie-chart.tsx, src/components/dashboard/income-expense-bar.tsx, src/components/dashboard/equity-trend-area.tsx, src/app/(auth)/dashboard/page.tsx</files>
  <action>
1. Create `src/components/dashboard/asset-pie-chart.tsx`:
   - "use client" component
   - Props: `data: { name: string; value: number }[]`, `entityId: string`
   - Uses Recharts PieChart, Pie, Cell, Tooltip, ResponsiveContainer
   - Teal palette: ["#0D7377", "#0A5C5F", "#10908F", "#14ADAB", "#18C9C4", "#1CE5DE"]
   - ResponsiveContainer width="100%" height={250}
   - Tooltip with formatCurrency from @/lib/utils/accounting
   - Wrapped in shadcn Card with CardHeader "Asset Breakdown" and CardContent
   - onClick handler on Pie cells: navigate to `/gl-ledger?accountName={name}` for drill-down (use next/navigation useRouter)
   - Handle empty data: show "No asset data" message

2. Create `src/components/dashboard/income-expense-bar.tsx`:
   - "use client" component
   - Props: `data: { category: string; amount: number }[]`
   - Uses Recharts BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer
   - Single teal color (#0D7377) for bars
   - Card wrapper with "Income vs Expense" title
   - Tooltip with formatCurrency
   - onClick on bars: navigate to `/reports` for drill-down
   - Handle empty data gracefully

3. Create `src/components/dashboard/equity-trend-area.tsx`:
   - "use client" component
   - Props: `data: { month: string; equity: number }[]`
   - Uses Recharts AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer
   - Teal gradient fill (#0D7377 to transparent)
   - Card wrapper with "Equity Trend" title
   - Format X-axis months as short labels (e.g., "Mar '26")
   - Tooltip with formatCurrency
   - Handle empty data gracefully

4. Update `src/app/(auth)/dashboard/page.tsx`:
   - Extend the DashboardData interface to include the three new chart data fields
   - Import the three chart components
   - Render them in a responsive grid below the summary cards: `grid grid-cols-1 gap-4 lg:grid-cols-3` -- pie chart, bar chart, area chart side by side on desktop, stacked on mobile
   - Pass entityId to AssetPieChart for drill-down routing
   - Charts should only render when data is available (conditional rendering)

5. **Confirm period-selector wiring:** Verify that the existing period-selector fetch already re-calls the dashboard API with `incomeStart` and `incomeEnd` date params when the user changes the period. The chart data (assetBreakdown, incomeVsExpense, equityTrend) is included in the same API response extended in Task 1, so charts will automatically update when the period changes -- no additional wiring needed. If the period selector does NOT re-fetch the full dashboard API response, add the re-fetch call so chart data updates with the selector.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <done>Three chart components render on the dashboard below summary cards. Charts use teal palette, have tooltips, and support click drill-down navigation. Layout is responsive (3 columns on desktop, stacked on mobile). Charts update when user changes the period selector.</done>
</task>

</tasks>

<verification>
- `npm ls recharts` confirms Recharts installed
- `npx tsc --noEmit` passes with no type errors
- Dashboard page imports and renders all three chart components
- API route returns assetBreakdown, incomeVsExpense, equityTrend in response
- Period selector re-fetches dashboard data including chart fields
</verification>

<success_criteria>
Dashboard displays three mini charts (pie, bar, area) below the summary cards. Charts update with the period selector. Clicking chart elements navigates to relevant reports/ledger pages.
</success_criteria>

<output>
After completion, create `.planning/phases/05-qbo-parity-i/05-01-SUMMARY.md`
</output>
