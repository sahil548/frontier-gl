---
phase: 12-reporting-fixes-onboarding-wizard
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/utils/cash-flow-backfill.test.ts
  - src/__tests__/queries/cash-flow-field.test.ts
  - src/__tests__/utils/contra-netting.test.ts
  - src/__tests__/utils/rate-based-budget.test.ts
  - src/__tests__/api/rate-budget.test.ts
  - src/__tests__/utils/opening-balance.test.ts
  - src/__tests__/utils/llm-column-mapper.test.ts
  - src/__tests__/api/column-mappings.test.ts
  - src/lib/validators/account.test.ts
autonomous: true
requirements:
  - SCHEMA-01

must_haves:
  truths:
    - "All test stubs exist and are parseable by vitest"
    - "Test stubs reference the correct future source files"
    - "Existing account validator tests are extended for new fields"
  artifacts:
    - path: "src/__tests__/utils/cash-flow-backfill.test.ts"
      provides: "CF-01 test stubs"
    - path: "src/__tests__/queries/cash-flow-field.test.ts"
      provides: "CF-02 test stubs"
    - path: "src/__tests__/utils/contra-netting.test.ts"
      provides: "CONTRA-01 test stubs"
    - path: "src/__tests__/utils/rate-based-budget.test.ts"
      provides: "RATE-01 test stubs"
    - path: "src/__tests__/api/rate-budget.test.ts"
      provides: "RATE-02 test stubs"
    - path: "src/__tests__/utils/opening-balance.test.ts"
      provides: "WIZ-03 test stubs"
    - path: "src/__tests__/utils/llm-column-mapper.test.ts"
      provides: "CSV-01, CSV-02 test stubs"
    - path: "src/__tests__/api/column-mappings.test.ts"
      provides: "CSV-04 test stubs"
    - path: "src/lib/validators/account.test.ts"
      provides: "SCHEMA-01 extended tests"
  key_links: []
---

<objective>
Create Wave 0 test stubs for all Phase 12 requirements to satisfy Nyquist validation.

Purpose: Every automated verification command in later plans needs a test file to exist. This plan creates all test stubs with commented-out imports (to avoid parse errors before implementation files exist) and `test.todo()` placeholders describing expected behaviors.

Output: 9 test files (8 new, 1 extended) ready for later plans to fill in.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-RESEARCH.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit test stubs for utility functions</name>
  <files>
    src/__tests__/utils/cash-flow-backfill.test.ts,
    src/__tests__/utils/contra-netting.test.ts,
    src/__tests__/utils/rate-based-budget.test.ts,
    src/__tests__/utils/opening-balance.test.ts,
    src/__tests__/utils/llm-column-mapper.test.ts
  </files>
  <action>
Create 5 test stub files in src/__tests__/utils/:

1. **cash-flow-backfill.test.ts** -- CF-01 stubs:
   - `test.todo("assigns OPERATING to receivable/payable assets and liabilities")`
   - `test.todo("assigns INVESTING to investment/securities/real-estate asset accounts")`
   - `test.todo("assigns FINANCING to loan/mortgage liabilities and equity/capital accounts")`
   - `test.todo("assigns EXCLUDED to cash asset accounts")`
   - `test.todo("returns null for INCOME and EXPENSE account types")`
   - `test.todo("defaults unmatched balance sheet accounts to OPERATING")`
   - Comment out: `// import { inferCashFlowCategory } from "@/lib/accounts/cash-flow-backfill";`

2. **contra-netting.test.ts** -- CONTRA-01 stubs:
   - `test.todo("computes net amount by subtracting contra from parent")`
   - `test.todo("groups contra accounts under their parent account")`
   - `test.todo("handles accounts with no contra children unchanged")`
   - `test.todo("handles multiple contra accounts under one parent")`
   - Comment out: `// import { applyContraNetting } from "@/lib/accounts/contra-netting";`

3. **rate-based-budget.test.ts** -- RATE-01 stubs:
   - `test.todo("computes monthly budget as holdingValue * rate / 12")`
   - `test.todo("rounds to 4 decimal places matching Decimal(19,4)")`
   - `test.todo("handles zero rate returning zero budget")`
   - `test.todo("handles large holding values without overflow")`
   - Comment out: `// import { computeMonthlyBudget } from "@/lib/budgets/rate-based";`

4. **opening-balance.test.ts** -- WIZ-03 stubs:
   - `test.todo("detects balanced grid when total debits equal total credits")`
   - `test.todo("detects unbalanced grid with correct difference amount")`
   - `test.todo("uses 0.005 tolerance for floating point comparison")`
   - `test.todo("handles empty grid as balanced")`
   - Comment out: `// import { getBalanceCheck } from "@/lib/onboarding/opening-balance";`

5. **llm-column-mapper.test.ts** -- CSV-01, CSV-02 stubs:
   - `test.todo("returns valid column mapping for standard bank CSV headers")`
   - `test.todo("returns valid mapping for non-standard headers via LLM")`
   - `test.todo("falls back to null when LLM is unavailable")`
   - `test.todo("handles empty headers array gracefully")`
   - `test.todo("returns mapping for COA import type")`
   - `test.todo("returns mapping for budget import type")`
   - Comment out: `// import { inferColumnMapping } from "@/lib/bank-transactions/llm-column-mapper";`

Each file: `import { describe, test } from "vitest";` then `describe("...", () => { ... })` wrapping the todos.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/utils/cash-flow-backfill.test.ts src/__tests__/utils/contra-netting.test.ts src/__tests__/utils/rate-based-budget.test.ts src/__tests__/utils/opening-balance.test.ts src/__tests__/utils/llm-column-mapper.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>5 test stub files exist, all parseable by vitest with todo tests listed</done>
</task>

<task type="auto">
  <name>Task 2: Create API test stubs and extend account validator tests</name>
  <files>
    src/__tests__/queries/cash-flow-field.test.ts,
    src/__tests__/api/rate-budget.test.ts,
    src/__tests__/api/column-mappings.test.ts,
    src/lib/validators/account.test.ts
  </files>
  <action>
Create 3 new test stub files and extend 1 existing:

1. **src/__tests__/queries/cash-flow-field.test.ts** -- CF-02 stubs:
   - `test.todo("classifies account by cashFlowCategory field instead of name matching")`
   - `test.todo("treats null cashFlowCategory as OPERATING for balance sheet accounts")`
   - `test.todo("skips EXCLUDED category accounts from cash flow")`
   - `test.todo("ignores cashFlowCategory for income and expense accounts")`
   - Comment out: `// import { getCashFlowStatement } from "@/lib/queries/report-queries";`

2. **src/__tests__/api/rate-budget.test.ts** -- RATE-02 stubs:
   - `test.todo("POST creates budget lines from holding value and rate")`
   - `test.todo("budget amounts are fixed at creation, not recalculated")`
   - `test.todo("rejects invalid rate values (negative, > 1)")`
   - Comment out: `// import handler`

3. **src/__tests__/api/column-mappings.test.ts** -- CSV-04 stubs:
   - `test.todo("GET returns saved mappings for entity")`
   - `test.todo("POST saves new column mapping with source name")`
   - `test.todo("GET by source name returns matching mapping")`
   - `test.todo("saved mapping auto-applies on repeat import")`
   - Comment out: `// import handler`

4. **src/lib/validators/account.test.ts** -- Extend existing tests (SCHEMA-01):
   Read the existing file first. Add a new `describe("Phase 12 extensions", () => { ... })` block with:
   - `test.todo("createAccountSchema accepts cashFlowCategory for ASSET type")`
   - `test.todo("createAccountSchema accepts isContra boolean")`
   - `test.todo("createAccountSchema rejects cashFlowCategory for INCOME type")`
   - `test.todo("updateAccountSchema accepts cashFlowCategory and isContra")`
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/queries/cash-flow-field.test.ts src/__tests__/api/rate-budget.test.ts src/__tests__/api/column-mappings.test.ts src/lib/validators/account.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>3 new test stub files exist, account validator tests extended with Phase 12 todos, all parseable by vitest</done>
</task>

</tasks>

<verification>
All 9 test files parseable by vitest: `npx vitest run --reporter=verbose 2>&1 | grep -E "todo|PASS|FAIL"`
</verification>

<success_criteria>
- 8 new test stub files created with correct todo descriptions
- 1 existing test file extended with new Phase 12 todos
- Full vitest suite still passes (no regressions from new stubs)
</success_criteria>

<output>
After completion, create `.planning/phases/12-reporting-fixes-onboarding-wizard/12-00-SUMMARY.md`
</output>
