---
phase: 12-reporting-fixes-onboarding-wizard
plan: 01
type: execute
wave: 1
depends_on:
  - "12-00"
files_modified:
  - prisma/schema.prisma
  - src/lib/validators/account.ts
  - src/lib/accounts/cash-flow-backfill.ts
  - src/lib/accounts/template.ts
  - src/app/api/entities/[entityId]/accounts/route.ts
  - src/app/api/entities/[entityId]/accounts/[accountId]/route.ts
autonomous: true
requirements:
  - SCHEMA-01
  - CF-01
  - CF-03

must_haves:
  truths:
    - "Account model has cashFlowCategory enum field and isContra boolean"
    - "Existing accounts receive correct cashFlowCategory from backfill migration"
    - "Account create/update validators accept cashFlowCategory and isContra"
    - "COA templates pre-fill cashFlowCategory for balance sheet accounts"
    - "Account API serializes new fields in responses"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "CashFlowCategory enum, cashFlowCategory field, isContra field, ColumnMapping model, wizardProgress field"
      contains: "enum CashFlowCategory"
    - path: "src/lib/validators/account.ts"
      provides: "Extended validators with cashFlowCategory and isContra"
      exports: ["createAccountSchema", "updateAccountSchema"]
    - path: "src/lib/accounts/cash-flow-backfill.ts"
      provides: "inferCashFlowCategory function for migration backfill"
      exports: ["inferCashFlowCategory"]
    - path: "src/lib/accounts/template.ts"
      provides: "Templates with cashFlowCategory pre-filled"
      contains: "cashFlowCategory"
  key_links:
    - from: "src/lib/accounts/cash-flow-backfill.ts"
      to: "prisma/schema.prisma"
      via: "CashFlowCategory enum type"
      pattern: "CashFlowCategory"
    - from: "src/lib/validators/account.ts"
      to: "prisma/schema.prisma"
      via: "nativeEnum import"
      pattern: "CashFlowCategory"
---

<objective>
Add CashFlowCategory enum, isContra boolean, ColumnMapping model, and wizardProgress field to Prisma schema. Extend account validators. Create backfill utility. Update COA templates. Update account API routes to handle new fields.

Purpose: This is the foundational schema migration that all other Phase 12 plans depend on. Without these fields, cash flow refactoring, contra display, wizard progress, and saved CSV mappings cannot proceed.

Output: Updated schema, validators, backfill utility, templates, and API routes handling new fields.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-CONTEXT.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs -->

From src/lib/validators/account.ts:
```typescript
export const createAccountSchema = z.object({
  name: z.string().min(1).max(200),
  number: z.string().min(5).regex(/^\d+$/),
  type: z.nativeEnum(AccountType),
  description: z.string().max(500).optional(),
  parentId: z.string().cuid().optional(),
});
export const updateAccountSchema = createAccountSchema.partial().extend({
  isActive: z.boolean().optional(),
});
```

From prisma/schema.prisma (Account model):
```prisma
model Account {
  id          String             @id @default(cuid())
  entityId    String
  number      String
  name        String
  type        AccountType
  description String?
  parentId    String?
  isActive    Boolean            @default(true)
  // ... relations ...
  @@unique([entityId, number])
  @@index([entityId, isActive])
  @@index([entityId, type])
  @@map("accounts")
}
```

From src/lib/accounts/template.ts:
```typescript
interface TemplateAccount {
  number: string;
  name: string;
  type: AccountType;
  description?: string;
  parentNumber?: string;
}
export const FAMILY_OFFICE_TEMPLATE: TemplateAccount[] = [...]
export async function applyTemplate(entityId: string, template?: string): Promise<...>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema migration + validators + backfill utility</name>
  <files>
    prisma/schema.prisma,
    src/lib/validators/account.ts,
    src/lib/validators/account.test.ts,
    src/lib/accounts/cash-flow-backfill.ts,
    src/__tests__/utils/cash-flow-backfill.test.ts
  </files>
  <behavior>
    - inferCashFlowCategory("ASSET", "Cash in Bank") returns "EXCLUDED"
    - inferCashFlowCategory("ASSET", "Investments - Securities") returns "INVESTING"
    - inferCashFlowCategory("LIABILITY", "Mortgage Payable") returns "FINANCING"
    - inferCashFlowCategory("EQUITY", "Owner Capital") returns "FINANCING"
    - inferCashFlowCategory("ASSET", "Accounts Receivable") returns "OPERATING"
    - inferCashFlowCategory("INCOME", "Service Revenue") returns null
    - inferCashFlowCategory("EXPENSE", "Office Supplies") returns null
    - inferCashFlowCategory("ASSET", "Other Asset") returns "OPERATING" (default)
    - createAccountSchema accepts cashFlowCategory "OPERATING" for ASSET type
    - createAccountSchema accepts isContra: true
    - createAccountSchema rejects invalid cashFlowCategory values
    - updateAccountSchema accepts cashFlowCategory and isContra as optional
  </behavior>
  <action>
1. **Prisma schema** -- Add to schema.prisma:
   - New `CashFlowCategory` enum: OPERATING, INVESTING, FINANCING, EXCLUDED
   - Add `cashFlowCategory CashFlowCategory?` to Account model (nullable -- income/expense don't need it)
   - Add `isContra Boolean @default(false)` to Account model
   - New `ColumnMapping` model: `{ id, entityId, sourceName, importType, mapping Json, createdAt, updatedAt }` with entity relation and `@@unique([entityId, sourceName, importType])`
   - Add `wizardProgress Json?` to Entity model (stores step completion status)
   - Run `npx prisma db push` to apply

2. **Backfill utility** -- Create `src/lib/accounts/cash-flow-backfill.ts`:
   - Export `inferCashFlowCategory(accountType: AccountType, accountName: string): CashFlowCategory | null`
   - Logic matches the existing name-matching from report-queries.ts:686-768 exactly:
     - INCOME/EXPENSE -> null
     - ASSET + cash -> EXCLUDED
     - ASSET + investment/securities/real estate/private equity -> INVESTING
     - LIABILITY + loan/mortgage -> FINANCING
     - EQUITY + equity/capital (not retained) -> FINANCING
     - EQUITY + distribution -> FINANCING
     - ASSET + receivable/prepaid -> OPERATING
     - LIABILITY + payable/accrued -> OPERATING
     - Default for unmatched balance sheet -> OPERATING
   - Export `backfillAllAccounts(prisma: PrismaClient): Promise<{ updated: number }>` that queries all accounts with null cashFlowCategory, infers category, and batch-updates

3. **Validators** -- Extend `src/lib/validators/account.ts`:
   - Import CashFlowCategory from `@/generated/prisma/enums`
   - Add `cashFlowCategory: z.nativeEnum(CashFlowCategory).optional()` to createAccountSchema
   - Add `isContra: z.boolean().optional()` to createAccountSchema (defaults handled at DB level)
   - updateAccountSchema inherits via `.partial()` automatically

4. **Fill in test stubs** -- Replace todo stubs in:
   - `src/__tests__/utils/cash-flow-backfill.test.ts`: Real tests for inferCashFlowCategory with all the cases above
   - `src/lib/validators/account.test.ts`: Fill in Phase 12 extension todos with real validation tests
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/utils/cash-flow-backfill.test.ts src/lib/validators/account.test.ts --reporter=verbose</automated>
  </verify>
  <done>CashFlowCategory enum exists in Prisma, Account has cashFlowCategory + isContra fields, ColumnMapping model exists, Entity has wizardProgress field, inferCashFlowCategory passes all tests, validators accept new fields</done>
</task>

<task type="auto">
  <name>Task 2: Update COA templates and account API routes</name>
  <files>
    src/lib/accounts/template.ts,
    src/app/api/entities/[entityId]/accounts/route.ts,
    src/app/api/entities/[entityId]/accounts/[accountId]/route.ts
  </files>
  <action>
1. **COA templates** -- Update `src/lib/accounts/template.ts`:
   - Extend `TemplateAccount` interface: add `cashFlowCategory?: CashFlowCategory` and `isContra?: boolean`
   - Update `FAMILY_OFFICE_TEMPLATE` entries with cashFlowCategory pre-filled for all balance sheet accounts:
     - Cash/bank accounts -> EXCLUDED
     - Investment/securities/real estate/PE accounts -> INVESTING
     - Loan/mortgage accounts -> FINANCING
     - Equity/capital/distribution accounts -> FINANCING
     - Receivable/prepaid/payable/accrued accounts -> OPERATING
     - Income/expense accounts -> no cashFlowCategory (null)
   - Create `HEDGE_FUND_TEMPLATE: TemplateAccount[]` with hedge fund accounts:
     - Assets: Cash at Prime Broker (EXCLUDED), Securities - Long (INVESTING), Securities - Short (INVESTING), Margin Receivable (OPERATING), Subscriptions Receivable (OPERATING)
     - Liabilities: Prime Broker Payable (OPERATING), Redemptions Payable (OPERATING), Accrued Management Fee (OPERATING), Accrued Performance Fee (OPERATING)
     - Equity: Partner Capital (FINANCING), Accumulated P&L (FINANCING)
     - Income: Realized Gains, Unrealized Gains, Dividend Income, Interest Income, Management Fee Income
     - Expenses: Prime Brokerage Fees, Fund Admin Fees, Audit Fees, Legal Fees, Trading Commissions
   - Update `applyTemplate` to accept template name ("family_office" | "hedge_fund") and select the right array. Pass cashFlowCategory and isContra when creating accounts.

2. **Account API routes** -- Update both route files:
   - **POST /api/entities/[entityId]/accounts** (create): Accept `cashFlowCategory` and `isContra` from validated body, pass to `prisma.account.create()`
   - **PUT /api/entities/[entityId]/accounts/[accountId]** (update): Accept `cashFlowCategory` and `isContra` from validated body, pass to `prisma.account.update()`
   - **GET responses**: Ensure serialization includes `cashFlowCategory` and `isContra` fields (Prisma returns them automatically, but verify the select/include clauses don't exclude them)
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>FAMILY_OFFICE_TEMPLATE has cashFlowCategory on all balance sheet entries, HEDGE_FUND_TEMPLATE exists with fund accounting accounts, applyTemplate handles both templates, account API routes accept and return cashFlowCategory + isContra</done>
</task>

</tasks>

<verification>
- `npx prisma validate` passes (schema is valid)
- `npx vitest run --reporter=verbose` all tests pass
- Account model has cashFlowCategory and isContra fields
- ColumnMapping model exists
- Entity model has wizardProgress field
</verification>

<success_criteria>
- CashFlowCategory enum with OPERATING, INVESTING, FINANCING, EXCLUDED in Prisma schema
- Account.cashFlowCategory nullable field and Account.isContra boolean field exist
- ColumnMapping model with entityId, sourceName, importType, mapping fields
- Entity.wizardProgress Json? field
- inferCashFlowCategory matches existing name-matching logic exactly
- Both COA templates pre-fill cashFlowCategory
- Account API accepts and returns new fields
</success_criteria>

<output>
After completion, create `.planning/phases/12-reporting-fixes-onboarding-wizard/12-01-SUMMARY.md`
</output>
