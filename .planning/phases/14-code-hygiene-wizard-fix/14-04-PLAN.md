---
phase: 14-code-hygiene-wizard-fix
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/bank-transactions/categorize.ts
  - tests/bank-transactions/categorize.test.ts
autonomous: true
requirements: [BANK-04]
must_haves:
  truths:
    - "src/lib/bank-transactions/categorize.ts no longer exports applyRules"
    - "src/lib/bank-transactions/categorize.ts no longer declares the applyRules-only TransactionInput interface"
    - "src/lib/bank-transactions/categorize.ts STILL exports matchRule and RuleInput (live production code, used by bank-tx PATCH route)"
    - "tests/bank-transactions/categorize.test.ts no longer references applyRules; matchRule tests remain green"
  artifacts:
    - path: "src/lib/bank-transactions/categorize.ts"
      provides: "matchRule + RuleInput only (applyRules + TransactionInput removed)"
      exports: ["matchRule", "RuleInput"]
    - path: "tests/bank-transactions/categorize.test.ts"
      provides: "matchRule + Position-targeted matchRule coverage; applyRules describes removed"
  key_links:
    - from: "src/app/api/entities/[entityId]/bank-transactions/[transactionId]/route.ts (PATCH)"
      to: "matchRule"
      via: "named import — UNCHANGED"
      pattern: "import.*matchRule"
---

<objective>
Delete the orphaned `applyRules` export and its supporting `TransactionInput` interface from `src/lib/bank-transactions/categorize.ts`. Remove the corresponding `applyRules` test suites from `tests/bank-transactions/categorize.test.ts`. Live production code (`matchRule`, `RuleInput`, `toNum` helper) stays untouched.

Purpose: Close the BANK-04 orphan production export flagged in v1.0-MILESTONE-AUDIT.md. The `applyRules` function has zero production callers (verified via grep — only the test file imports it). Inventing a batch-categorization endpoint to justify keeping it would be scope creep.

Output: One source file slimmed (~35 lines removed), one test file with two `it()` blocks removed (the standalone `applyRules` describe AND the Position-targeted `applyRules` it-block), and an import line tidied.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-code-hygiene-wizard-fix/14-CONTEXT.md
@.planning/phases/14-code-hygiene-wizard-fix/14-RESEARCH.md
@.planning/phases/14-code-hygiene-wizard-fix/14-VALIDATION.md

@src/lib/bank-transactions/categorize.ts
@tests/bank-transactions/categorize.test.ts

<interfaces>
<!-- Key contracts the executor needs. Already on disk; do not re-derive. -->

From src/lib/bank-transactions/categorize.ts (TODAY — 111 lines):
- KEEP lines 1-16: RuleInput interface (matchRule constrains on it)
- DELETE lines 17-26: TransactionInput interface (applyRules-only)
- KEEP lines 28-39: toNum helper (used by matchRule)
- KEEP lines 41-75: matchRule function (live production code)
- DELETE lines 77-111: applyRules function (orphan production export)

From tests/bank-transactions/categorize.test.ts (TODAY — 162 lines):
- Line 2 — UPDATE import: `import { matchRule } from "@/lib/bank-transactions/categorize";` (drop applyRules)
- Lines 26-72: matchRule describe (4 tests) — KEEP
- Lines 74-91: Position-targeted "matchRule matches rule with positionId" — KEEP
- Lines 93-123: Position-targeted "applyRules matches positionId-bearing rule" — DELETE
- Lines 126-162: standalone "applyRules" describe (1 test) — DELETE

Verified production callers of `matchRule` (per RESEARCH.md grep): src/app/api/entities/[entityId]/bank-transactions/[transactionId]/route.ts (PATCH path). UNCHANGED by this plan.

Verified production callers of `applyRules`: ZERO. Only the test file imports it.
</interfaces>

# Independent of Plan 14-01/02/03 — no dependencies, no file conflicts. Wave 1 parallel.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surgically remove applyRules + TransactionInput from categorize.ts</name>
  <files>src/lib/bank-transactions/categorize.ts</files>
  <action>
    Open `src/lib/bank-transactions/categorize.ts` and delete two specific blocks.

    DELETE — `TransactionInput` interface (currently lines 17-26): the entire `export interface TransactionInput { ... }` block including its leading JSDoc `/** Minimal transaction shape used by the categorization engine. */`.

    DELETE — `applyRules` function (currently lines 77-111): the entire function declaration `export function applyRules<R extends RuleInput, T extends TransactionInput>(transactions: T[], rules: R[]): { matched: T[]; unmatched: T[] } { ... }` including its leading JSDoc.

    KEEP unchanged:
    - `RuleInput` interface (lines 1-16) — `matchRule<R extends RuleInput>` constrains on it
    - `toNum` helper (lines 28-39) — used by `matchRule`
    - `matchRule` function (lines 41-75) — live production code

    Result: file shrinks from 111 lines to roughly 75 lines. Single trailing newline after `matchRule`.

    DO NOT rename anything. DO NOT modify `RuleInput`, `toNum`, or `matchRule`. DO NOT add a deprecation notice — the function and its sole interface are simply gone.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx tsc --noEmit 2>&1 | grep -E "categorize\.ts|TransactionInput|applyRules" | head -10</automated>
  </verify>
  <done>
    Zero TS errors in `categorize.ts` from the deletion. `applyRules` and `TransactionInput` no longer appear in the file. `matchRule` and `RuleInput` still exported and unchanged. (Test file will fail-to-compile until Task 2 — that's expected.)
  </done>
</task>

<task type="auto">
  <name>Task 2: Surgically remove applyRules describe blocks from categorize.test.ts</name>
  <files>tests/bank-transactions/categorize.test.ts</files>
  <action>
    Open `tests/bank-transactions/categorize.test.ts` and make three edits.

    EDIT 1 — Update line 2 import (drop applyRules):
    - BEFORE: `import { matchRule, applyRules } from "@/lib/bank-transactions/categorize";`
    - AFTER:  `import { matchRule } from "@/lib/bank-transactions/categorize";`

    DELETE — lines 93-123 (the `it("applyRules matches positionId-bearing rule...")` block inside the `describe("Position-targeted rules", ...)` parent describe):

    The block to remove starts with `it("applyRules matches positionId-bearing rule and returns it in matched bucket", () => {` and ends with the matching closing `});`. Remove the leading NOTE comment block too (the multi-line `// NOTE: applyRules currently copies ONLY accountId...` comment). Keep the parent `describe("Position-targeted rules", ...)` block AND the preceding `it("matchRule matches rule with positionId and null accountId", ...)` test (lines 76-91).

    DELETE — lines 126-162 (the entire standalone `describe("applyRules", () => { ... })` block):

    The whole describe block is one `applyRules` test. Remove it cleanly including the closing `});`. The file ends after this block, so the new EOF should follow the closing brace of the prior `describe("Position-targeted rules", ...)`.

    Result: file shrinks from 162 lines to roughly 95 lines. Two describe blocks remain: `matchRule` (4 tests) and `Position-targeted rules` (1 test — the matchRule positionId one).

    DO NOT modify the `TestRule` type, `makeRule` helper, or any matchRule test — those are live production coverage.

    DO NOT rename describe blocks. DO NOT add coverage comments. The deletion is silent — the audit log of WHY (`applyRules` was orphan production export per v1.0-MILESTONE-AUDIT.md) lives in this PLAN.md and the eventual SUMMARY.md.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx vitest run tests/bank-transactions/categorize.test.ts --reporter=verbose 2>&1 | tail -15 && echo "--- TSC ---" && npx tsc --noEmit 2>&1 | grep -E "categorize" | head -10</automated>
  </verify>
  <done>
    All remaining tests in categorize.test.ts pass: matchRule describe (4 tests) and Position-targeted matchRule (1 test) — total 5 tests green. Zero TS errors mentioning `categorize`. Zero references to `applyRules` or `TransactionInput` anywhere in the file.
  </done>
</task>

</tasks>

<verification>
Phase-level verification at end of plan:

1. Scoped tests: `npx vitest run tests/bank-transactions/categorize.test.ts` — green (5 tests).
2. Touched-file tsc check: `npx tsc --noEmit 2>&1 | grep "categorize"` returns zero matches.
3. Orphan removal verified: `grep -n "applyRules\|TransactionInput" src/lib/bank-transactions/categorize.ts tests/bank-transactions/categorize.test.ts` returns ZERO matches.
4. Live API still imports matchRule: `grep -n "matchRule" src/app/api/entities/\[entityId\]/bank-transactions/\[transactionId\]/route.ts` still returns ≥1 match.
5. Full suite: `npm test` — no regressions.
</verification>

<success_criteria>
- `applyRules` export deleted from `src/lib/bank-transactions/categorize.ts`
- `TransactionInput` interface deleted from same file
- `matchRule` and `RuleInput` exports preserved
- `tests/bank-transactions/categorize.test.ts` no longer imports or references `applyRules`
- Both standalone `applyRules` describe block AND the Position-targeted `applyRules` it-block removed
- Remaining 5 tests in the file pass
- BANK-04 orphan production export item from v1.0-MILESTONE-AUDIT.md is closed
</success_criteria>

<output>
After completion, create `.planning/phases/14-code-hygiene-wizard-fix/14-04-SUMMARY.md` summarizing:
- Files modified (2: categorize.ts source, categorize.test.ts test)
- Lines removed: ~35 (applyRules + TransactionInput in source) + ~67 (two describe/it blocks in test)
- Final file sizes (~75 lines source, ~95 lines test)
- Test count delta: -2 tests (1 standalone applyRules + 1 Position-targeted applyRules)
- Confirmation that BANK-04 orphan-production-export item is closed
- Note: PLEASE update `.planning/phases/12-reporting-fixes-onboarding-wizard/deferred-items.md` to flag any newly-surfaced unrelated TS errors discovered during this plan (per CONTEXT.md "Verification Bar")
</output>
