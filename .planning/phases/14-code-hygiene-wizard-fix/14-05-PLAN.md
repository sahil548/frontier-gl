---
phase: 14-code-hygiene-wizard-fix
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/budgets/page.tsx
  - src/types/account.ts
  - src/app/(auth)/accounts/page.tsx
  - src/components/accounts/account-table.tsx
  - src/components/accounts/account-form.tsx
  - src/components/csv-import/column-mapping-ui.tsx
  - package.json
  - tests/attachments/blob-storage.test.ts
  - .planning/phases/12-reporting-fixes-onboarding-wizard/deferred-items.md
autonomous: true
requirements: [DEFERRED-ITEMS]
must_haves:
  truths:
    - "tsc --noEmit emits zero errors mentioning src/app/(auth)/budgets/page.tsx (Item #1 cleared)"
    - "src/types/account.ts is the single canonical source of truth for SerializedAccount; src/app/(auth)/accounts/page.tsx, src/components/accounts/account-table.tsx, and src/components/accounts/account-form.tsx all import it (Item #3 cleared)"
    - "All 7 tests in src/__tests__/hooks/use-entity.test.ts pass (Item #5 cleared via package.json NODE_OPTIONS flag)"
    - "tsc --noEmit emits zero errors mentioning src/components/csv-import/column-mapping-ui.tsx (Item #6 cleared)"
    - "tsc --noEmit emits zero TS2348 errors mentioning tests/attachments/blob-storage.test.ts (Item #7 cleared)"
    - ".planning/phases/12-reporting-fixes-onboarding-wizard/deferred-items.md marks items #1, #3, #5, #6, #7 as RESOLVED with phase reference"
  artifacts:
    - path: "src/types/account.ts"
      provides: "Canonical SerializedAccount type with cashFlowCategory + isContra fields"
      exports: ["SerializedAccount"]
    - path: "package.json"
      provides: "Updated test script with NODE_OPTIONS=\"--no-experimental-webstorage\""
      contains: "no-experimental-webstorage"
  key_links:
    - from: "src/app/(auth)/accounts/page.tsx"
      to: "src/types/account.ts"
      via: "import type { SerializedAccount }"
      pattern: "from.*@/types/account"
    - from: "src/components/accounts/account-table.tsx"
      to: "src/types/account.ts"
      via: "import type { SerializedAccount }"
      pattern: "from.*@/types/account"
    - from: "src/components/accounts/account-form.tsx"
      to: "src/types/account.ts"
      via: "import type { SerializedAccount }"
      pattern: "from.*@/types/account"
---

<objective>
Sweep the 5 remaining pre-existing TypeScript/test issues catalogued in `.planning/phases/12-reporting-fixes-onboarding-wizard/deferred-items.md` (items #1, #3, #5, #6, #7). Each item is a small, localized fix at a known location — no abstractions, no opportunistic refactoring beyond the audit-flagged surface.

Items #2 and #4 in deferred-items.md are already RESOLVED in 12-07 — DO NOT re-fix; just leave their RESOLVED notes intact when updating the file.

Purpose: Close success criterion #4 of Phase 14 — `tsc --noEmit` clean across touched files; full test suite green (530 baseline + 7 recovered use-entity = 537 baseline going forward).

Output: 5 small-diff fixes spread across 7 source/test files + 1 new types module + 1 package.json edit + 1 deferred-items.md status update.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-code-hygiene-wizard-fix/14-CONTEXT.md
@.planning/phases/14-code-hygiene-wizard-fix/14-RESEARCH.md
@.planning/phases/14-code-hygiene-wizard-fix/14-VALIDATION.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/deferred-items.md

@src/app/(auth)/budgets/page.tsx
@src/app/(auth)/accounts/page.tsx
@src/components/accounts/account-table.tsx
@src/components/accounts/account-form.tsx
@src/components/csv-import/column-mapping-ui.tsx
@src/__tests__/hooks/use-entity.test.ts
@tests/attachments/blob-storage.test.ts
@package.json

<interfaces>
<!-- Existing exports the executor needs to align against. -->

From src/components/accounts/account-form.tsx:59-71 (CANONICAL shape — has cashFlowCategory + isContra):
```typescript
type SerializedAccount = {
  id: string;
  entityId: string;
  number: string;
  name: string;
  type: string;
  description: string | null;
  parentId: string | null;
  isActive: boolean;
  balance: string;
  debitTotal?: string;
  creditTotal?: string;
  cashFlowCategory: string | null;  // Phase 12 addition
  isContra: boolean;                // Phase 12 addition
};
```

From src/app/(auth)/accounts/page.tsx:10-22 + src/components/accounts/account-table.tsx:47-59 (DRIFTED — missing Phase 12 fields):
The two non-canonical declarations omit `cashFlowCategory` and `isContra`. Dedup uses the form's superset shape.

Verified by RESEARCH.md grep: API at `/api/entities/[entityId]/accounts/` already serializes `cashFlowCategory` + `isContra` per Phase 12, so adopting the superset shape at consumers is safe.

From src/components/csv-import/column-mapping-ui.tsx:218 (source of null) and :230 (where TS2345 fires):
- Line 218: `const selectedHeader = mapping[role] ?? "";` — already coalesces.
- Line 230: `onValueChange={(val) => handleRoleChange(role, val)}` — `val` is typed `string | null` from base-ui Select, but `handleRoleChange` expects `string`.
- Line 236: `<SelectItem value="__none__">` — `__none__` sentinel clears the mapping.

From tests/attachments/blob-storage.test.ts:35,43 (vitest 4 Mock TS2348):
- Both lines cast as `(put as ReturnType<typeof vi.fn>)(...)` — vitest 4 narrowed Mock generic, must use `vi.mocked(put)(...)` instead.

From src/__tests__/hooks/use-entity.test.ts (TS-clean today; runtime-fails 7/7 due to Node 25 localStorage shadow):
- Fix is at runtime layer (NODE_OPTIONS in package.json `test` script), not source.
</interfaces>

# Independent of Plans 14-01/02/03/04 — different files (zero overlap with bank-tx, JE, or wizard work).
# Wave 1 parallel-eligible.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Item #1 — Coalesce Select onValueChange in budgets page (2 sites)</name>
  <files>src/app/(auth)/budgets/page.tsx</files>
  <action>
    Open `src/app/(auth)/budgets/page.tsx` and update two Select onValueChange handlers.

    Site 1 — line 746 (rate-target holding selector):
    - BEFORE: `<Select value={selectedHoldingId} onValueChange={setSelectedHoldingId}>`
    - AFTER:  `<Select value={selectedHoldingId} onValueChange={(v) => setSelectedHoldingId(v ?? "")}>`

    Site 2 — line 775 (rate-target income account selector):
    - BEFORE: `<Select value={selectedAccountId} onValueChange={setSelectedAccountId}>`
    - AFTER:  `<Select value={selectedAccountId} onValueChange={(v) => setSelectedAccountId(v ?? "")}>`

    Then run `grep -n "onValueChange={set" src/app/\(auth\)/budgets/page.tsx` to confirm there are no other unguarded sites in this file. If any other `onValueChange={setFoo}` patterns exist where `setFoo` is a `Dispatch<SetStateAction<string>>`, apply the same coalesce. (RESEARCH.md confirmed only 2 sites at the time of research; verify before completing.)

    DO NOT change state types from `string` to `string | null`. DO NOT introduce a `<SafeSelect>` wrapper component (per CONTEXT.md "Deferred Ideas"). DO NOT touch unrelated Selects in this file (e.g., year/month selectors that don't trigger TS2322).
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx tsc --noEmit 2>&1 | grep -E "budgets/page\.tsx" | head -10</automated>
  </verify>
  <done>
    Zero TS errors mentioning `budgets/page.tsx` from `tsc --noEmit`. The two flagged TS2322 errors at lines 746 and 775 are gone. No other onValueChange sites in the file emit new errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Item #3 — Canonicalize SerializedAccount in src/types/account.ts</name>
  <files>src/types/account.ts, src/app/(auth)/accounts/page.tsx, src/components/accounts/account-table.tsx, src/components/accounts/account-form.tsx</files>
  <action>
    CREATE NEW FILE `src/types/account.ts` with the canonical shape (superset that includes Phase 12's `cashFlowCategory` + `isContra`):

    ```typescript
    /**
     * Serialized shape of an Account row as returned by /api/entities/:entityId/accounts.
     * Used by all UI consumers (accounts page, account-table, account-form).
     *
     * Decimal money fields are serialized as strings; nullable cashFlowCategory
     * defaults to null when the account has no explicit CF mapping (Phase 12).
     */
    export type SerializedAccount = {
      id: string;
      entityId: string;
      number: string;
      name: string;
      type: string;
      description: string | null;
      parentId: string | null;
      isActive: boolean;
      balance: string;
      debitTotal?: string;
      creditTotal?: string;
      cashFlowCategory: string | null;  // Phase 12
      isContra: boolean;                // Phase 12
    };
    ```

    Then update three consumer files:

    **src/app/(auth)/accounts/page.tsx** (line 10-22 area):
    - DELETE the local `type SerializedAccount = { ... }` declaration
    - ADD at the top imports area: `import type { SerializedAccount } from "@/types/account";`

    **src/components/accounts/account-table.tsx** (line 47-59 area):
    - DELETE the local `type SerializedAccount = { ... }` declaration
    - ADD at the top imports area: `import type { SerializedAccount } from "@/types/account";`

    **src/components/accounts/account-form.tsx** (line 59-71 area):
    - DELETE the local `type SerializedAccount = { ... }` declaration (it's the canonical shape — the new file inherits FROM it)
    - ADD at the top imports area: `import type { SerializedAccount } from "@/types/account";`

    After all three updates, the canonical declaration lives ONLY in `src/types/account.ts`. No file should declare a local `type SerializedAccount` anymore — verify via `grep -rn "type SerializedAccount" src/` returning a single match (the new types file).

    Pitfall (per RESEARCH.md Pitfall 6): if dedup surfaces NEW TS2719 or "missing property" errors in unrelated files (e.g., a test or page that constructs a SerializedAccount literal without `cashFlowCategory`/`isContra`), DO NOT absorb the fix. LOG it in `deferred-items.md` (Task 6 handles the file; just queue up the new entry text). Continue with the dedup; the goal is single-source-of-truth.

    DO NOT add a `src/types/index.ts` barrel — direct deep import is fine and matches existing project style (e.g., `@/types/account` mirrors `@/types/SerializedEntity` per RESEARCH.md).
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx tsc --noEmit 2>&1 | grep -E "(accounts/page\.tsx|account-table\.tsx|account-form\.tsx)" | grep TS2719 | head -10</automated>
  </verify>
  <done>
    Zero TS2719 errors mentioning the three flagged files. `src/types/account.ts` exists and exports `SerializedAccount` with all 13 fields (including Phase 12's `cashFlowCategory` + `isContra`). All three consumer files import from `@/types/account`. Single-source-of-truth verified by grep.
  </done>
</task>

<task type="auto">
  <name>Task 3: Item #5 — Add NODE_OPTIONS to package.json test script (Node 25 localStorage fix)</name>
  <files>package.json</files>
  <action>
    Open `package.json` and update the `scripts.test` entry.

    BEFORE (current line 11):
    ```json
    "test": "vitest run --reporter=verbose"
    ```

    AFTER:
    ```json
    "test": "NODE_OPTIONS=\"--no-experimental-webstorage\" vitest run --reporter=verbose"
    ```

    Why: Node 25 ships an experimental built-in `localStorage` global that is enabled by default and shadows jsdom's localStorage. The Node global is an empty object (no `clear`, `setItem`, `getItem` methods) when no `--localstorage-file` is provided. Disabling Node's at the runtime level lets jsdom's full localStorage take over. Verified working: 7/7 use-entity.test.ts assertions pass with the flag (per RESEARCH.md Pitfall 5).

    Caveat — leave a brief inline tracking note in the PR/commit message (NOT in package.json — it doesn't accept comments). The package.json itself just gets the script change.

    DO NOT modify `vitest.config.ts` (the issue is a Node global, not a jsdom URL config — verified by RESEARCH.md). DO NOT add a vitest setup file stub for localStorage. DO NOT use the alias `--no-webstorage` (the spelled-out `--no-experimental-webstorage` is more discoverable for future maintainers).
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx vitest run src/__tests__/hooks/use-entity.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>
    All 7 tests in `src/__tests__/hooks/use-entity.test.ts` pass when invoked via the updated test script. The `localStorage.clear is not a function` error is gone. `package.json scripts.test` includes the NODE_OPTIONS flag.

    Note: the verify command above runs vitest directly without the script wrapper — to confirm the script-level fix, also run `cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npm test 2>&1 | grep -E "(use-entity|localStorage)" | head -10` and verify 7/7 passing AND no localStorage errors.
  </done>
</task>

<task type="auto">
  <name>Task 4: Item #6 — Coalesce string|null in column-mapping-ui Select handler</name>
  <files>src/components/csv-import/column-mapping-ui.tsx</files>
  <action>
    Open `src/components/csv-import/column-mapping-ui.tsx`.

    The TS2345 error fires at line 230 where `val` (typed `string | null` from base-ui Select) is passed into `handleRoleChange(role, val)` — `handleRoleChange` expects `string`.

    Read `handleRoleChange` (around line 121 per RESEARCH.md) to confirm: when the second arg equals `"__none__"`, the mapping for that role is deleted; otherwise the mapping is set to that string. So the safe coalesce is the `"__none__"` sentinel — passing `""` would set an empty mapping rather than clearing it.

    BEFORE (line 230):
    ```typescript
    onValueChange={(val) => handleRoleChange(role, val)}
    ```

    AFTER:
    ```typescript
    onValueChange={(val) => handleRoleChange(role, val ?? "__none__")}
    ```

    DO NOT touch line 218 (`mapping[role] ?? ""`) — that's the source of the value being read FROM mapping, not the sink the TS error complains about. The fix is at the sink (line 230) per RESEARCH.md.

    DO NOT change the prop type of `handleRoleChange` to accept `string | null` — per CONTEXT.md, fix at the call site.

    Run grep to verify no other `onValueChange={(val) => handleRoleChange` instances exist in this file at risk of the same error. If a second site exists, apply the same coalesce.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx tsc --noEmit 2>&1 | grep -E "column-mapping-ui\.tsx" | head -10</automated>
  </verify>
  <done>
    Zero TS errors mentioning `column-mapping-ui.tsx`. The handler at line 230 (or wherever the line shifts to after edits) coalesces `null` to `"__none__"` so `handleRoleChange` receives a non-null string.
  </done>
</task>

<task type="auto">
  <name>Task 5: Item #7 — vitest 4 Mock generic fix in blob-storage.test.ts</name>
  <files>tests/attachments/blob-storage.test.ts</files>
  <action>
    Open `tests/attachments/blob-storage.test.ts` and update the two TS2348 sites.

    Line 35 (inside `simulateUpload`):
    - BEFORE:
      ```typescript
      await (put as ReturnType<typeof vi.fn>)(path, file, {
        access: "public",
        contentType: file.type,
      });
      ```
    - AFTER:
      ```typescript
      await vi.mocked(put)(path, file, {
        access: "public",
        contentType: file.type,
      });
      ```

    Line 43 (inside `simulateDelete`):
    - BEFORE: `await (del as ReturnType<typeof vi.fn>)(attachmentUrl);`
    - AFTER:  `await vi.mocked(del)(attachmentUrl);`

    Why: vitest 4 narrowed `Mock` to `Mock<Procedure | Constructable>` — the union flips on whether the mock could be `new`-called. `as ReturnType<typeof vi.fn>` produces a non-callable union. `vi.mocked(fn)` returns `MockInstance<typeof fn>` which is callable without TS2348 (per RESEARCH.md Pitfall 7).

    Then audit the rest of the file for any OTHER `as ReturnType<typeof vi.fn>` casts (e.g., the `mockPut` and `mockDel` references later in the file) and confirm they don't trigger TS2348. If they do, apply the same `vi.mocked(...)` pattern. If they're used only for type assertions (e.g., `mockPut.mockResolvedValue(...)`), those still work — the issue is specifically when CALLING the mock as a function.

    DO NOT modify production code in `src/lib/attachments/` or the actual blob upload routes — this is a test-only fix.

    DO NOT alter the test assertions or expected behavior — purely a typing fix.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx vitest run tests/attachments/blob-storage.test.ts --reporter=verbose 2>&1 | tail -15 && echo "--- TSC ---" && npx tsc --noEmit 2>&1 | grep -E "blob-storage\.test\.ts" | grep TS2348 | head -10</automated>
  </verify>
  <done>
    All blob-storage.test.ts tests still pass (no behavior change — type-only fix). Zero TS2348 errors mentioning `blob-storage.test.ts`.
  </done>
</task>

<task type="auto">
  <name>Task 6: Update deferred-items.md to mark items #1, #3, #5, #6, #7 as RESOLVED</name>
  <files>.planning/phases/12-reporting-fixes-onboarding-wizard/deferred-items.md</files>
  <action>
    Open `.planning/phases/12-reporting-fixes-onboarding-wizard/deferred-items.md`.

    For each of items #1, #3, #5, #6, #7 (DO NOT touch items #2 and #4 — they are already RESOLVED in 12-07 and have intact resolution notes):

    Append a `**RESOLVED in Phase 14:**` line right after the `**Why deferred:**` line, with a brief description of how it was fixed.

    Suggested entries:

    Item #1 — `**RESOLVED in Phase 14:** Per-site `(v) => setFoo(v ?? "")` inline coalesce at both Select sites (746, 775) in budgets/page.tsx. No SafeSelect wrapper introduced.`

    Item #3 — `**RESOLVED in Phase 14:** Canonical type extracted to `src/types/account.ts` with `cashFlowCategory` + `isContra` (Phase 12 fields). All three consumers (accounts/page.tsx, account-table.tsx, account-form.tsx) now import from `@/types/account`.`

    Item #5 — `**RESOLVED in Phase 14:** Added `NODE_OPTIONS="--no-experimental-webstorage"` to `package.json scripts.test`. Disables Node 25's experimental built-in localStorage global (which shadows jsdom's). All 7 use-entity tests pass.`

    Item #6 — `**RESOLVED in Phase 14:** Inline coalesce at line 230: `(val) => handleRoleChange(role, val ?? "__none__")` — the sentinel value clears the mapping, matching handleRoleChange's existing semantics.`

    Item #7 — `**RESOLVED in Phase 14:** Replaced `(put as ReturnType<typeof vi.fn>)(...)` with `vi.mocked(put)(...)` at lines 35 and 43. vitest 4 Mock generic narrowing handled.`

    If during Tasks 1-5 you discovered any NEW unrelated pre-existing TS errors in the transitive graph of touched files (per CONTEXT.md "Verification Bar"), append them as new numbered items (#8, #9, ...) in the same file with the standard format:
    - **Files:**
    - **Error:**
    - **Why deferred:** Surfaced during Phase 14 sweep; out of scope per CONTEXT.md verification bar.

    DO NOT modify items #2 and #4 — they have RESOLVED notes from 12-07 already.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && grep -E "RESOLVED in Phase 14" .planning/phases/12-reporting-fixes-onboarding-wizard/deferred-items.md | wc -l</automated>
  </verify>
  <done>
    `grep "RESOLVED in Phase 14" deferred-items.md | wc -l` returns >= 5 (one per item closed in this plan; more if items #8+ are added). Items #2 and #4 RESOLVED notes from 12-07 are intact (verifiable by `grep "RESOLVED in 12-07" deferred-items.md` returning >= 2).
  </done>
</task>

</tasks>

<verification>
Phase-level verification at end of plan:

1. Touched-file tsc check (zero matches expected): `npx tsc --noEmit 2>&1 | grep -E "(budgets/page\.tsx|accounts/page\.tsx|account-table\.tsx|account-form\.tsx|column-mapping-ui\.tsx|blob-storage\.test\.ts)"` returns ZERO matches.
2. localStorage fix: `npm test 2>&1 | grep -E "(use-entity|localStorage)"` shows 7/7 use-entity tests passing AND no `localStorage.clear is not a function` errors.
3. SerializedAccount canonicalisation: `grep -rn "^type SerializedAccount" src/` returns ZERO matches; `grep -rn "^export type SerializedAccount" src/` returns exactly 1 match (in `src/types/account.ts`).
4. Test suite green: `npm test` passes — baseline 530 + 7 recovered (use-entity) = 537 minimum + any new tests from sibling Phase 14 plans.
5. deferred-items.md updated: items #1/#3/#5/#6/#7 each have a RESOLVED in Phase 14 entry; items #2 and #4 unchanged.
</verification>

<success_criteria>
- Item #1 (Select onValueChange null-coalesce in budgets) resolved
- Item #3 (SerializedAccount canonical in src/types/account.ts) resolved
- Item #5 (Node 25 localStorage shadowing → NODE_OPTIONS flag) resolved
- Item #6 (column-mapping-ui line 230 string|null) resolved
- Item #7 (vitest 4 Mock generic in blob-storage test) resolved
- deferred-items.md reflects RESOLVED status with Phase 14 reference for each
- Zero new TS errors in touched files; full test suite green (≥537 passing)
- Items #2 and #4 unchanged (already RESOLVED in 12-07)
</success_criteria>

<output>
After completion, create `.planning/phases/14-code-hygiene-wizard-fix/14-05-SUMMARY.md` summarizing:
- Files modified (8 total: 6 source + 1 package.json + 1 deferred-items.md; plus 1 NEW types file)
- Each item resolved with the specific fix approach
- Test count delta (7 recovered use-entity tests now passing)
- Confirmation that `tsc --noEmit` is clean across all 6 touched source files
- Any newly-discovered unrelated TS errors documented as new entries in deferred-items.md
</output>
