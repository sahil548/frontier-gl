---
phase: 02-accounting-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_add_accounting_models/migration.sql
  - prisma/migrations/*_add_posted_immutability_trigger/migration.sql
  - prisma/migrations/*_add_balance_validation_trigger/migration.sql
  - prisma/migrations/*_add_closed_period_trigger/migration.sql
  - src/lib/validators/account.ts
  - src/lib/validators/journal-entry.ts
  - src/lib/accounts/next-number.ts
  - src/lib/accounts/template.ts
  - src/lib/journal-entries/post.ts
  - src/lib/journal-entries/reverse.ts
  - src/lib/journal-entries/auto-number.ts
  - src/lib/journal-entries/bulk-post.ts
  - src/lib/validators/account.test.ts
  - src/lib/validators/journal-entry.test.ts
  - src/lib/accounts/next-number.test.ts
  - src/lib/journal-entries/status.test.ts
autonomous: true
requirements:
  - COA-07
  - DI-03
  - DI-04
  - DI-05
  - JE-02
  - JE-06

must_haves:
  truths:
    - "Prisma schema defines Account, AccountBalance, JournalEntry, JournalEntryLine, JournalEntryAudit, and PeriodClose models"
    - "All money fields use Decimal @db.Decimal(19,4)"
    - "Account model has entityId FK scoping and unique constraint on [entityId, number]"
    - "DB trigger prevents UPDATE/DELETE on posted journal entries"
    - "DB trigger validates SUM(debit) = SUM(credit) per journal entry at commit"
    - "DB trigger prevents posting to closed periods"
    - "Zod schemas validate account and journal entry input with cross-field rules"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "All Phase 2 data models"
      contains: "model Account"
    - path: "src/lib/validators/account.ts"
      provides: "Zod schemas for account create/update"
      exports: ["createAccountSchema", "updateAccountSchema"]
    - path: "src/lib/validators/journal-entry.ts"
      provides: "Zod schema with debit=credit refinement"
      exports: ["journalEntrySchema", "JournalEntryFormValues"]
    - path: "src/lib/journal-entries/post.ts"
      provides: "Posting logic with atomic balance update"
      exports: ["postJournalEntry"]
    - path: "src/lib/journal-entries/reverse.ts"
      provides: "Reversal draft creation"
      exports: ["createReversalDraft"]
    - path: "src/lib/journal-entries/bulk-post.ts"
      provides: "Bulk posting in single transaction"
      exports: ["bulkPostEntries"]
    - path: "src/lib/accounts/template.ts"
      provides: "Family Office Standard template data and merge logic"
      exports: ["FAMILY_OFFICE_TEMPLATE", "applyTemplate"]
    - path: "src/lib/accounts/next-number.ts"
      provides: "Account number auto-suggestion"
      exports: ["suggestNextAccountNumber"]
  key_links:
    - from: "src/lib/journal-entries/post.ts"
      to: "prisma.accountBalance"
      via: "Atomic upsert with increment during posting"
      pattern: "accountBalance\\.upsert"
    - from: "prisma/schema.prisma"
      to: "PostgreSQL triggers"
      via: "Custom migrations with raw SQL"
      pattern: "CREATE.*TRIGGER"
---

<objective>
Build the entire data foundation for the accounting engine: Prisma schema with all Phase 2 models, PostgreSQL triggers for data integrity (immutability, balance validation, closed period enforcement), Zod validators, and all business logic functions (posting, reversals, bulk-post, auto-numbering, template seeding, next-number suggestion).

Purpose: Establish the schema, integrity constraints, and business logic layer that the COA UI (Plan 02) and JE UI (Plans 03-04) build on top of. Every API route and UI component in this phase depends on these models, validators, and functions.

Output: Prisma schema migrated, DB triggers active, Zod validators, and business logic functions with tests.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-accounting-engine/02-CONTEXT.md
@.planning/phases/02-accounting-engine/02-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Phase 1 that executor needs. -->

From prisma/schema.prisma (Phase 1):
```prisma
model Entity {
  id          String     @id @default(cuid())
  name        String
  type        EntityType
  // ... other fields
}
enum EntityType { LP, LLC, CORPORATION, S_CORP, TRUST, FOUNDATION, PARTNERSHIP, INDIVIDUAL, OTHER }
```

From src/lib/db/prisma.ts:
```typescript
export const prisma: PrismaClient;
```

From src/lib/db/scoped-query.ts:
```typescript
export function entityScope(entityId: string | "all"): { entityId?: string };
```

From src/lib/validators/api-response.ts:
```typescript
export function successResponse<T>(data: T, status?: number): Response;
export function errorResponse(message: string, status?: number, errors?: z.ZodError): Response;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 2 Prisma models, DB triggers via custom migrations, and PeriodClose table</name>
  <files>
    prisma/schema.prisma,
    prisma/migrations/*_add_accounting_models/migration.sql,
    prisma/migrations/*_add_posted_immutability_trigger/migration.sql,
    prisma/migrations/*_add_balance_validation_trigger/migration.sql,
    prisma/migrations/*_add_closed_period_trigger/migration.sql
  </files>
  <behavior>
    - Account model has entityId FK, number (String), name, type (AccountType enum), parentId (self-ref), isActive, balance relation
    - AccountBalance model has accountId (unique), debitTotal/creditTotal/balance as Decimal(19,4)
    - JournalEntry model has entityId, entryNumber, date, description, status (JournalEntryStatus enum), audit fields (createdBy, approvedBy, postedBy + timestamps), reversalOfId self-ref
    - JournalEntryLine has journalEntryId, accountId, debit/credit as Decimal(19,4), memo, sortOrder
    - JournalEntryAudit has journalEntryId, action, userId, changes (Json), createdAt
    - PeriodClose has entityId, year (Int), month (Int), closedBy, closedAt with unique on [entityId, year, month]
    - Trigger prevents UPDATE/DELETE on posted journal entries (raises exception)
    - Trigger prevents modification of line items on posted JEs
    - Deferred constraint trigger validates SUM(debit)=SUM(credit) per JE at transaction commit
    - Trigger checks period_closes table before allowing status change to POSTED
  </behavior>
  <action>
    1. Update `prisma/schema.prisma` with all Phase 2 models exactly as specified in 02-RESEARCH.md Architecture Patterns section:
       - Add enums: AccountType (ASSET, LIABILITY, EQUITY, INCOME, EXPENSE), JournalEntryStatus (DRAFT, APPROVED, POSTED)
       - Add models: Account (with self-referencing hierarchy, @@unique([entityId, number])), AccountBalance, JournalEntry (with reversal self-ref, @@unique([entityId, entryNumber])), JournalEntryLine, JournalEntryAudit
       - Add PeriodClose model: id (cuid), entityId (FK to Entity), year (Int), month (Int), closedBy (String), closedAt (DateTime @default(now())), @@unique([entityId, year, month]), @@map("period_closes")
       - Add relations from Entity to Account[], JournalEntry[], PeriodClose[]
       - CRITICAL: All Decimal fields use @db.Decimal(19,4)

    2. Run `npx prisma migrate dev --name add_accounting_models` to create and apply the schema migration.

    3. Create custom migration for immutability trigger (DI-04):
       ```bash
       npx prisma migrate dev --create-only --name add_posted_immutability_trigger
       ```
       Then edit the generated empty migration SQL to add the `prevent_posted_je_modification` and `prevent_posted_line_modification` trigger functions and triggers exactly as specified in 02-RESEARCH.md Pattern 1. Apply with `npx prisma migrate dev`.

    4. Create custom migration for balance validation trigger (DI-05):
       ```bash
       npx prisma migrate dev --create-only --name add_balance_validation_trigger
       ```
       Add the `validate_je_balance` deferred constraint trigger exactly as in 02-RESEARCH.md Pattern 2. CRITICAL: Must be `DEFERRABLE INITIALLY DEFERRED` so it fires at transaction commit, not per-row. Apply with `npx prisma migrate dev`.

    5. Create custom migration for closed period trigger (JE-06):
       ```bash
       npx prisma migrate dev --create-only --name add_closed_period_trigger
       ```
       Add a trigger function `check_closed_period` that fires BEFORE UPDATE on journal_entries when NEW.status = 'POSTED'. It extracts year and month from the JE date, checks period_closes table for a matching row, and raises an exception if found. With no rows in period_closes, this trigger always passes -- Phase 4 adds the UI to close periods.
       Apply with `npx prisma migrate dev`.

    6. Regenerate Prisma client: `npx prisma generate`

    Note: If DATABASE_URL is not configured, the executor should use `npx prisma migrate dev` which will prompt for the connection. If the database is unavailable, use `npx prisma migrate dev --create-only` for all migrations and document that they need to be applied when the DB is available.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx prisma generate 2>&1 | tail -3 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    Prisma schema has all Phase 2 models with correct types, relations, and indexes. Four migrations exist: schema models, immutability trigger, balance validation trigger, and closed period trigger. All Decimal fields use @db.Decimal(19,4). PeriodClose table exists for future period close enforcement. Prisma client regenerated with new types.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Zod validators, business logic functions, and COA template with tests</name>
  <files>
    src/lib/validators/account.ts,
    src/lib/validators/journal-entry.ts,
    src/lib/accounts/next-number.ts,
    src/lib/accounts/template.ts,
    src/lib/journal-entries/post.ts,
    src/lib/journal-entries/reverse.ts,
    src/lib/journal-entries/auto-number.ts,
    src/lib/journal-entries/bulk-post.ts,
    src/lib/validators/account.test.ts,
    src/lib/validators/journal-entry.test.ts,
    src/lib/accounts/next-number.test.ts,
    src/lib/journal-entries/status.test.ts
  </files>
  <behavior>
    - createAccountSchema validates: name (1-200 chars required), number (1-5 digit string required), type (AccountType enum), description (optional, max 500), parentId (optional cuid)
    - createAccountSchema rejects: empty name, missing type, number longer than 5 chars
    - updateAccountSchema is partial of createAccountSchema + optional isActive boolean
    - journalEntrySchema validates: date (required), description (required, 1-500 chars), lineItems (min 2 items)
    - journalEntrySchema.lineItems validates: accountId (required), debit (non-negative string), credit (non-negative string), memo (optional)
    - journalEntrySchema rejects entries where total debits != total credits
    - journalEntrySchema rejects entries where all line items have zero debit and zero credit
    - journalEntrySchema rejects line items where both debit and credit are zero
    - suggestNextAccountNumber returns "10000" for empty entity, next 100-increment for siblings
    - Status transitions: DRAFT->APPROVED valid, DRAFT->POSTED valid, APPROVED->POSTED valid, POSTED->anything invalid
  </behavior>
  <action>
    1. Create `src/lib/validators/account.ts`:
       - createAccountSchema: z.object with name (1-200), number (regex 1-5 digits), type (z.nativeEnum(AccountType)), description (optional max 500), parentId (optional cuid string)
       - updateAccountSchema: createAccountSchema.partial().extend({ isActive: z.boolean().optional() })
       - Export types: CreateAccountInput, UpdateAccountInput

    2. Create `src/lib/validators/journal-entry.ts` exactly as specified in 02-RESEARCH.md Pattern 6:
       - lineItemSchema: accountId (required), debit (string, non-negative via decimal.js), credit (string, non-negative), memo (optional). Refine: at least one of debit/credit must be non-zero.
       - journalEntrySchema: date (required), description (1-500), lineItems (min 2). Refine: total debits must equal total credits using decimal.js math.
       - Export types: JournalEntryFormValues, LineItemFormValues
       - CRITICAL: Use `new Decimal(value || "0")` for all arithmetic -- never native JS math

    3. Create `src/lib/accounts/next-number.ts` as specified in 02-RESEARCH.md:
       - suggestNextAccountNumber(entityId, parentNumber?): queries existing accounts to suggest next available
       - Top-level: next 10000 increment. Sub-account: parent number + next 100 increment
       - Returns string (e.g., "10000", "10100")

    4. Create `src/lib/accounts/template.ts`:
       - FAMILY_OFFICE_TEMPLATE: constant array of ~40 accounts with number, name, type, parentNumber fields as specified in 02-RESEARCH.md Pattern 7 (10000 Assets through 50000+ Expenses with family-office-specific sub-accounts)
       - applyTemplate(entityId: string, tx?: PrismaTransaction): creates all template accounts for entity, uses upsert-like logic to skip duplicates by account number
       - Returns { inserted: number, skipped: number }

    5. Create `src/lib/journal-entries/post.ts` as specified in 02-RESEARCH.md Pattern 3:
       - postJournalEntry(journalEntryId, userId): Prisma interactive transaction that verifies status, updates to POSTED with postedBy/postedAt, atomically updates AccountBalance for each line item using upsert with increment, creates audit trail entry
       - CRITICAL: Use Prisma `increment` for balance updates. If Decimal increment doesn't work, fall back to raw SQL within the transaction

    6. Create `src/lib/journal-entries/reverse.ts` as specified in 02-RESEARCH.md:
       - createReversalDraft(journalEntryId, userId): verifies original is POSTED, creates new DRAFT JE with flipped debits/credits, links via reversalOfId, auto-generates entry number, creates audit trail

    7. Create `src/lib/journal-entries/auto-number.ts`:
       - generateNextEntryNumber(tx, entityId): within a transaction, queries MAX entryNumber for entity, increments, returns formatted string (JE-001, JE-042, etc.)
       - Pads to 3 digits minimum, grows naturally beyond (JE-1000+)

    8. Create `src/lib/journal-entries/bulk-post.ts` as specified in 02-RESEARCH.md:
       - bulkPostEntries(journalEntryIds[], userId): single interactive transaction that posts all entries, updates all balances, creates audit entries. All-or-nothing rollback on failure.

    9. Write tests FIRST (TDD):
       - `src/lib/validators/account.test.ts`: valid create, reject empty name, reject invalid type, reject >5 digit number, update with partial fields, update with isActive
       - `src/lib/validators/journal-entry.test.ts`: valid JE with balanced lines, reject unbalanced, reject <2 lines, reject zero-amount lines, reject negative amounts
       - `src/lib/accounts/next-number.test.ts`: mock Prisma, test empty entity returns "10000", test siblings increment by 100, test top-level increment by 10000
       - `src/lib/journal-entries/status.test.ts`: test valid transitions (DRAFT->APPROVED, DRAFT->POSTED, APPROVED->POSTED), reject invalid (POSTED->DRAFT, POSTED->APPROVED). Create a small validateStatusTransition helper and test it.

    Note: Tests that require database (posting, bulk-post, reverse) will be integration tests that mock Prisma. The Zod validator tests and pure function tests run without DB.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx vitest run src/lib/validators/ src/lib/accounts/next-number.test.ts src/lib/journal-entries/status.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>
    Zod schemas validate account and journal entry input correctly including cross-field debit=credit check. Account number suggestion logic works for top-level and sub-accounts. Status transition validation rejects invalid transitions. Family Office Standard template defined with ~40 accounts. Posting, reversal, bulk-post, and auto-numbering functions implemented. All unit tests pass.
  </done>
</task>

</tasks>

<verification>
1. `npx prisma generate` succeeds with all Phase 2 models available
2. `npx tsc --noEmit` passes with no type errors
3. `npx vitest run src/lib/validators/ --reporter=verbose` -- all validator tests pass
4. `npx vitest run src/lib/accounts/next-number.test.ts --reporter=verbose` -- auto-suggest tests pass
5. `npx vitest run src/lib/journal-entries/status.test.ts --reporter=verbose` -- status transition tests pass
6. Prisma schema has Account, AccountBalance, JournalEntry, JournalEntryLine, JournalEntryAudit, PeriodClose models
7. Four custom migration files exist with trigger SQL
</verification>

<success_criteria>
- All Phase 2 Prisma models defined with correct types, relations, indexes, and Decimal(19,4) fields
- DB triggers created for: posted entry immutability (DI-04), debit=credit validation (DI-05), closed period enforcement (JE-06)
- PeriodClose table exists (empty -- Phase 4 populates it)
- Zod validators for accounts and journal entries with cross-field validation
- Business logic functions: postJournalEntry, createReversalDraft, bulkPostEntries, generateNextEntryNumber, suggestNextAccountNumber, applyTemplate
- Family Office Standard template with ~40 accounts defined
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-accounting-engine/02-01-SUMMARY.md`
</output>
