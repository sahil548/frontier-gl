---
phase: 07-qbo-parity-iii-budget-vs-actual
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/api/budgets.test.ts
  - src/__tests__/api/budget-import.test.ts
  - src/__tests__/api/budget-report.test.ts
  - src/__tests__/validators/budget.test.ts
  - src/__tests__/utils/fiscal-year.test.ts
autonomous: true
requirements: [BUDG-01, BUDG-02, BUDG-03, BUDG-04, BUDG-05]

must_haves:
  truths:
    - "All five test stub files exist and run without errors (all tests skip or fail as expected)"
    - "Test files cover budget upsert, CSV import, budget report variance, entity scoping, and CSV export"
  artifacts:
    - path: "src/__tests__/api/budgets.test.ts"
      provides: "Stub tests for BUDG-01 (upsert) and BUDG-04 (entity scoping)"
      contains: "describe"
    - path: "src/__tests__/api/budget-import.test.ts"
      provides: "Stub tests for BUDG-02 (CSV parse/validate)"
      contains: "describe"
    - path: "src/__tests__/api/budget-report.test.ts"
      provides: "Stub tests for BUDG-03 (variance direction) and BUDG-05 (CSV export)"
      contains: "describe"
    - path: "src/__tests__/validators/budget.test.ts"
      provides: "Zod schema validation tests for budget inputs"
      contains: "describe"
    - path: "src/__tests__/utils/fiscal-year.test.ts"
      provides: "Unit tests for fiscal year month computation"
      contains: "describe"
  key_links: []
---

<objective>
Create test stub files for all Phase 7 requirements before implementation begins.

Purpose: Establishes behavioral test coverage contracts (Nyquist compliance) so that Wave 1+ implementation tasks can verify correctness with vitest, not just typecheck. Tests start as stubs with `.todo` markers; implementation plans fill them in as code is written.
Output: Five test files that run via vitest (all pending/todo initially).
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-qbo-parity-iii-budget-vs-actual/07-CONTEXT.md
@.planning/phases/07-qbo-parity-iii-budget-vs-actual/07-VALIDATION.md

@src/__tests__/validators/entity.test.ts
@src/__tests__/api/entities-create.test.ts
@src/__tests__/setup.ts

<interfaces>
<!-- Test stubs reference these contracts — implementation comes in 07-01 and 07-03 -->

From src/validators/budget.ts (created in 07-01):
```typescript
export const budgetLineSchema: z.ZodObject<{ accountId, year, month, amount }>;
export const budgetUpsertSchema: z.ZodObject<{ fiscalYear, budgets }>;
export const budgetCsvRowSchema: z.ZodObject<{ accountNumber, period, amount }>;
```

From src/lib/utils/fiscal-year.ts (created in 07-01):
```typescript
export function getFiscalYearMonths(fiscalYearEnd: string, fiscalYear: number): { year: number; month: number; label: string }[];
export function getFiscalYearDateRange(fiscalYearEnd: string, fiscalYear: number): { startDate: Date; endDate: Date };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validator and fiscal year utility test stubs</name>
  <files>src/__tests__/validators/budget.test.ts, src/__tests__/utils/fiscal-year.test.ts</files>
  <action>
1. Create src/__tests__/validators/budget.test.ts following the pattern from src/__tests__/validators/entity.test.ts:

```typescript
import { describe, it, expect } from "vitest";
// Import will fail until 07-01 creates the file — that's expected for stubs
// import { budgetLineSchema, budgetUpsertSchema, budgetCsvRowSchema } from "@/validators/budget";

describe("BUDG-01: budget upsert schema validation", () => {
  it.todo("accepts valid budget line with accountId, year, month, amount");
  it.todo("rejects budget line with month < 1 or > 12");
  it.todo("rejects budget line with year outside 2000-2100");
  it.todo("rejects budget line with missing accountId");
  it.todo("accepts valid budgetUpsertSchema with fiscalYear and budgets array");
  it.todo("rejects upsert schema with empty budgets array");
});

describe("BUDG-02: budget CSV row schema validation", () => {
  it.todo("accepts valid CSV row with accountNumber, period MM/YYYY, amount");
  it.todo("rejects CSV row with invalid period format");
  it.todo("rejects CSV row with non-numeric amount");
  it.todo("rejects CSV row with empty accountNumber");
});
```

2. Create src/__tests__/utils/fiscal-year.test.ts:

```typescript
import { describe, it, expect } from "vitest";
// import { getFiscalYearMonths, getFiscalYearDateRange } from "@/lib/utils/fiscal-year";

describe("getFiscalYearMonths", () => {
  it.todo("returns Jan-Dec for FYE 12-31, fiscal year 2025");
  it.todo("returns Apr 2024 - Mar 2025 for FYE 03-31, fiscal year 2025");
  it.todo("returns Jul 2024 - Jun 2025 for FYE 06-30, fiscal year 2025");
  it.todo("returns 12 months for any valid FYE");
  it.todo("labels use short month names (Jan, Feb, Mar, etc.)");
});

describe("getFiscalYearDateRange", () => {
  it.todo("returns Jan 1 - Dec 31 for FYE 12-31, fiscal year 2025");
  it.todo("returns Apr 1 2024 - Mar 31 2025 for FYE 03-31, fiscal year 2025");
});
```

These are .todo stubs — they will be filled in by 07-01 Task 1 when the implementation files are created. The import statements should be commented out initially so vitest can parse the files without errors.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/validators/budget.test.ts src/__tests__/utils/fiscal-year.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist, vitest runs them successfully (all tests show as "todo/skipped"), zero failures.</done>
</task>

<task type="auto">
  <name>Task 2: API endpoint test stubs for budgets, import, and report</name>
  <files>src/__tests__/api/budgets.test.ts, src/__tests__/api/budget-import.test.ts, src/__tests__/api/budget-report.test.ts</files>
  <action>
1. Create src/__tests__/api/budgets.test.ts following the pattern from src/__tests__/api/entities-create.test.ts:

```typescript
import { describe, it, expect } from "vitest";

describe("BUDG-01: PUT /api/entities/:entityId/budgets — budget upsert", () => {
  it.todo("upserts budget amounts for valid fiscalYear and budgets array");
  it.todo("overwrites existing budget for same account+month (upsert behavior)");
  it.todo("skips rows with zero or empty amount");
  it.todo("returns count of upserted rows");
});

describe("BUDG-01: GET /api/entities/:entityId/budgets — load budgets", () => {
  it.todo("returns budget rows filtered to the requested fiscal year");
  it.todo("only returns INCOME and EXPENSE account budgets");
  it.todo("includes account details (id, number, name, type)");
});

describe("BUDG-04: entity-scoped budget access", () => {
  it.todo("returns 403 when user has no access to entity");
  it.todo("returns only budgets belonging to the requested entity");
  it.todo("PUT rejects budget for account not belonging to entity");
});
```

2. Create src/__tests__/api/budget-import.test.ts:

```typescript
import { describe, it, expect } from "vitest";

describe("BUDG-02: POST /api/entities/:entityId/budgets/import — CSV import", () => {
  it.todo("parses valid CSV with account_number, period, amount columns");
  it.todo("upserts parsed rows into budget table");
  it.todo("returns imported count, skipped count, and errors array");
  it.todo("skips rows where account number not found in entity");
  it.todo("skips rows where account is not INCOME or EXPENSE type");
  it.todo("reports validation errors with row numbers");
  it.todo("handles CSV with header variations (accountNumber vs account_number)");
});
```

3. Create src/__tests__/api/budget-report.test.ts:

```typescript
import { describe, it, expect } from "vitest";

describe("BUDG-03: GET /api/entities/:entityId/reports/budget-vs-actual — variance report", () => {
  it.todo("returns income and expense rows with actual, budget, varianceDollar, variancePercent");
  it.todo("computes income variance as actual - budget (positive = favorable)");
  it.todo("computes expense variance as budget - actual (positive = favorable)");
  it.todo("returns null variancePercent when budget is zero");
  it.todo("groups rows into incomeRows and expenseRows");
  it.todo("computes section totals and net income variance");
});

describe("BUDG-05: budget report CSV export data", () => {
  it.todo("report data includes all columns needed for CSV export (Account #, Name, Actual, Budget, Variance $, Variance %)");
  it.todo("section headers and subtotals are included in export-ready format");
});
```

All tests use .todo so they register as pending without failing. Imports are omitted since the implementation files don't exist yet.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/api/budgets.test.ts src/__tests__/api/budget-import.test.ts src/__tests__/api/budget-report.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Three API test files exist, vitest runs them (all tests "todo/skipped"), zero failures. Every BUDG requirement has at least one test stub.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/api/budgets.test.ts src/__tests__/api/budget-import.test.ts src/__tests__/api/budget-report.test.ts src/__tests__/validators/budget.test.ts src/__tests__/utils/fiscal-year.test.ts --reporter=verbose` completes with all tests in "todo" state and zero failures
- All five files exist at expected paths
</verification>

<success_criteria>
Five test stub files covering BUDG-01 through BUDG-05 exist and pass vitest (all pending/todo). Wave 1+ plans can now uncomment imports, fill in test bodies, and use vitest as behavioral verification instead of relying solely on tsc --noEmit.
</success_criteria>

<output>
After completion, create `.planning/phases/07-qbo-parity-iii-budget-vs-actual/07-00-SUMMARY.md`
</output>
