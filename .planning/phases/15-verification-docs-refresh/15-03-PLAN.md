---
phase: 15-verification-docs-refresh
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []
must_haves:
  truths:
    - ".planning/REQUIREMENTS.md has a new `**Total requirements:** 99 unique REQ-IDs across Phases 1–12, all Complete` line inserted immediately after the existing `**Updated:**` line (after line 5)"
    - ".planning/REQUIREMENTS.md has a new `## Phase 11 Requirements — Categorization UX & Opening Balances` section listing all 8 Phase 11 REQ-IDs: CAT-01, CAT-03, OBE-01, OBE-02, OBE-03, REC-01, REC-03, REC-04 (each as `- [x] **REQ-ID**: canonical text` matching RESEARCH.md §REQ-ID Canonical Text)"
    - ".planning/REQUIREMENTS.md has a new `## Phase 12 Requirements — Reporting Fixes & Onboarding Wizard` section listing all 15 Phase 12 REQ-IDs: SCHEMA-01, CF-01, CF-02, CF-03, CONTRA-01, CONTRA-02, RATE-01, RATE-02, WIZ-01, WIZ-02, WIZ-03, CSV-01, CSV-02, CSV-03, CSV-04"
    - "Phase 11 section does NOT include CAT-02, CAT-04, or OBE-04 (those were scoped-out phase-research artifacts per RESEARCH.md §P6; never delivered)"
    - "Traceability table has `POS-01--08 | Phase 10 | Complete` (flipped from Pending)"
    - "Traceability table has 10 new rows for Phases 11–12 REQ-IDs, matching the existing `--` range style: CAT-01/03 | Phase 11 | Complete; OBE-01--03 | Phase 11 | Complete; REC-01, REC-03, REC-04 | Phase 11 | Complete; SCHEMA-01 | Phase 12 | Complete; CF-01--03 | Phase 12 | Complete; CONTRA-01--02 | Phase 12 | Complete; RATE-01--02 | Phase 12 | Complete; WIZ-01--03 | Phase 12 | Complete; CSV-01--04 | Phase 12 | Complete"
    - "Coverage bullets (formerly lines 255-257) replaced with `- Phases 1–12: Complete — 99/99 REQ-IDs satisfied per v1.0 milestone audit` (old `Phase 10 requirements (POS): 8 total -- Pending` line removed)"
    - "Footer italic line (formerly line 261) updated to `*Last updated: 2026-04-16 — v1.0 milestone close-out*`"
    - "Existing formatting conventions preserved: `--` for ranges (not `-` or em-dash), commas for discontinuous IDs within same phase, `**REQ-ID**` bold formatting, em-dash in section headers, single-asterisk italic footer"
    - "File committed to git via a single docs(15-03): ... commit following the existing docs(NN-NN): subject convention"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirements traceability doc with Phase 11/12 sections, refreshed traceability table, and top-of-file total count"
      contains: "## Phase 11 Requirements, ## Phase 12 Requirements, **Total requirements:** 99 unique REQ-IDs, POS-01--08 | Phase 10 | Complete"
  key_links:
    - from: "Phase 15 plan 15-03"
      to: ".planning/REQUIREMENTS.md Traceability table"
      via: "direct Edit/MultiEdit tool edits (no sub-workflow — no such tool exists for REQUIREMENTS.md mutations)"
      pattern: "POS-01--08.*Phase 10.*Complete"
    - from: ".planning/REQUIREMENTS.md new Phase 11/12 sections"
      to: "each phase's PLAN.md `must_haves.truths` and SUMMARY.md `provides`"
      via: "verbatim text from RESEARCH.md §REQ-ID Canonical Text (pre-canonicalized, do NOT re-author)"
      pattern: "\\*\\*(CAT|OBE|REC|SCHEMA|CF|CONTRA|RATE|WIZ|CSV)-\\d+\\*\\*"
estimated_duration: 20m
---

<objective>
Mutate `.planning/REQUIREMENTS.md` to bring its traceability current: add Phase 11 and Phase 12 REQ-ID checklist sections, flip POS-01–08 from Pending to Complete in the traceability table, add 10 new traceability rows for Phase 11/12, replace the bottom Coverage bullets, update the footer, and insert a top-of-file total-requirements count line. Closes Stream C of the three documentation hygiene streams identified by `.planning/v1.0-MILESTONE-AUDIT.md`.

Purpose: v1.0 milestone close-out requires the canonical requirements doc to reflect the as-built state. Currently:
- POS-01–08 incorrectly listed as Pending (all 8 satisfied per 10-01/02/03-SUMMARY.md)
- Phase 11 REQ-IDs (CAT/OBE/REC, 8 total) not documented in the main requirements list at all
- Phase 12 REQ-IDs (SCHEMA/CF/CONTRA/RATE/WIZ/CSV, 15 total) not documented in the main requirements list at all
- No top-of-file total-requirements count
- Bottom Coverage bullets and footer last-updated line stale

All mutations are line-precise per RESEARCH.md §REQUIREMENTS.md Structural Anatomy. No sub-workflow exists for REQUIREMENTS.md edits — this plan uses direct Edit/MultiEdit tool calls.

Output: One file modified (`.planning/REQUIREMENTS.md`). One commit. No production code. No new tests. No other planning docs touched. No ROADMAP.md or STATE.md edits.

**Out of scope for this plan:**
- CAT-02, CAT-04, OBE-04 (scoped-out phase-research artifacts per RESEARCH.md §P6 — NEVER delivered, must NOT appear in REQUIREMENTS.md)
- Re-ordering or re-numbering existing REQUIREMENTS.md sections (per CONTEXT.md §Claude's Discretion: "default to leaving order intact, append new sections after Phase 10")
- Updating ROADMAP.md, STATE.md, or any other planning doc (those are handled by execute-phase roll-up, NOT by this plan)
- Re-verifying the `99` unique REQ-ID count (per RESEARCH.md §P7: "Don't recount. Per CONTEXT.md: use `99` verbatim. Audit is authoritative.")
- Fixing the 7 pre-existing failing tests in src/__tests__/hooks/use-entity.test.ts (Phase 14 scope)
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/15-verification-docs-refresh/15-CONTEXT.md
@.planning/phases/15-verification-docs-refresh/15-RESEARCH.md

# The file being mutated
@.planning/REQUIREMENTS.md

# Source for REQ-ID canonical text (read by executor to cross-check against RESEARCH.md §REQ-ID Canonical Text which already extracts the text verbatim)
@.planning/phases/11-categorization-ux-opening-balances/11-RESEARCH.md
@.planning/phases/11-categorization-ux-opening-balances/11-01-PLAN.md
@.planning/phases/11-categorization-ux-opening-balances/11-02-PLAN.md
@.planning/phases/11-categorization-ux-opening-balances/11-03-PLAN.md
@.planning/phases/11-categorization-ux-opening-balances/11-04-PLAN.md
@.planning/phases/11-categorization-ux-opening-balances/11-05-PLAN.md
@.planning/phases/11-categorization-ux-opening-balances/11-05-SUMMARY.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-RESEARCH.md

<interfaces>
<!-- All REQ-ID canonical text is pre-extracted in RESEARCH.md §REQ-ID Canonical Text (lines 221-279). -->
<!-- DO NOT re-derive from scratch — use the exact text below, lifted verbatim from RESEARCH.md. -->

## Phase 11 REQ-ID Canonical Text (8 entries, from RESEARCH.md §Phase 11 REQ-IDs)

- **CAT-01**: Position picker replaces raw GL account picker as default categorization target for bank transactions
- **CAT-03**: Categorization rules support optional positionId alongside existing accountId (GL resolved at apply-time)
- **OBE-01**: Opening Balance Equity account (3900) auto-created if it does not exist
- **OBE-02**: Opening balance JE auto-generated and posted immediately when holding created with non-zero balance
- **OBE-03**: Adjusting JE auto-generated for the difference when holding balance is edited
- **REC-01**: Posting a categorized bank transaction auto-marks it as RECONCILED (single and bulk paths)
- **REC-03**: Reconciliation status badges on each transaction row (Reconciled green, Pending yellow, Unmatched red)
- **REC-04**: Running reconciled vs unreconciled totals displayed at top of bank feed

## Phase 12 REQ-ID Canonical Text (15 entries, from RESEARCH.md §Phase 12 REQ-IDs)

- **SCHEMA-01**: Account schema accepts cashFlowCategory (enum) and isContra (boolean) fields
- **CF-01**: Existing accounts receive correct cashFlowCategory via name-based inference backfill migration
- **CF-02**: Cash flow statement classifies accounts by cashFlowCategory field, not name matching
- **CF-03**: Account form shows cashFlowCategory dropdown only for ASSET/LIABILITY/EQUITY types
- **CONTRA-01**: isContra flag drives contra-netting display on Balance Sheet (parent shows gross, contra shows as deduction, net total rendered)
- **CONTRA-02**: Contra accounts render with "Less:" prefix and net total on Balance Sheet
- **RATE-01**: Rate-based budget computation = holdingValue × rate / 12, using Decimal.js 4-decimal precision
- **RATE-02**: Budget values snapshot at creation and do not auto-recalculate; manual recalculate via re-POST; holdings eligible if holding FMV OR sum of active position marketValues is non-zero
- **WIZ-01**: Onboarding wizard triggers automatically after creating any new entity
- **WIZ-02**: All four wizard steps (COA, Holdings, Opening Balances, First Transactions) are individually skippable with persistent progress
- **WIZ-03**: Opening balance grid enforces debit=credit balance before JE generation; stored JE date matches form date (no UTC shift)
- **CSV-01**: LLM-powered column mapping infers roles for non-standard CSV headers (bank, COA, budget imports)
- **CSV-02**: System falls back to heuristic COLUMN_PATTERNS when LLM unavailable (missing API key, error, timeout)
- **CSV-03**: User sees mapping confirmation UI before any CSV import proceeds; saved mappings auto-apply on header-fingerprint match; multi-account CSVs route per-row via Account column
- **CSV-04**: Confirmed mappings persist per entity/source/importType and reuse-by-fingerprint

## Traceability Table New/Updated Rows (RESEARCH.md §Traceability Rows to Add)

Edit existing row:
`| POS-01--08 | Phase 10 | Pending |` → `| POS-01--08 | Phase 10 | Complete |`

Insert 10 new rows after the POS-01--08 row (matching the existing `--` range style, comma-separated for discontinuous IDs within same phase):
```
| CAT-01, CAT-03 | Phase 11 | Complete |
| OBE-01--03 | Phase 11 | Complete |
| REC-01, REC-03, REC-04 | Phase 11 | Complete |
| SCHEMA-01 | Phase 12 | Complete |
| CF-01--03 | Phase 12 | Complete |
| CONTRA-01--02 | Phase 12 | Complete |
| RATE-01--02 | Phase 12 | Complete |
| WIZ-01--03 | Phase 12 | Complete |
| CSV-01--04 | Phase 12 | Complete |
```

## Coverage Bullet Replacement (RESEARCH.md §REQUIREMENTS.md Structural Anatomy row 255-257)

Current (lines 255-257):
```
**Coverage:**
- Phases 1--9: Complete
- Phase 10 requirements (POS): 8 total -- Pending
```

New:
```
**Coverage:**
- Phases 1–12: Complete — 99/99 REQ-IDs satisfied per v1.0 milestone audit
```

## Footer Update (line 261)

Current: `*Last updated: 2026-04-12 -- Phase 10 redefined: Positions Model & Holdings Overhaul*`
New: `*Last updated: 2026-04-16 — v1.0 milestone close-out*`

## Top-of-File Total Line Insertion (after line 5)

Current line 4: `**Updated:** 2026-04-12 — Phase 10 redefined: Positions Model & Holdings Overhaul`
Current line 5: `**Core Value:** Accountants can view, manage, and close books across all family office entities in one fast, purpose-built GL — eliminating the per-entity cost and friction of QuickBooks Online.`

Insert AFTER line 5:
`**Total requirements:** 99 unique REQ-IDs across Phases 1–12, all Complete`

## Formatting Conventions to Preserve (per RESEARCH.md §Formatting Conventions)

- Dash ranges: `--` (two hyphens, NOT `-` or em-dash `—`). Example: `BANK-01--05 | Phase 9 | Complete`.
- Discontinuous IDs within same phase: comma-separated (`CAT-01, CAT-03 | Phase 11 | Complete`)
- Section header style: `## Phase N Requirements — Full Name` (em-dash in header)
- Checklist bullet: `- [x] **REQ-ID**: description` (bold REQ-ID, colon, space, description)
- Italics footer: single-asterisk `*` markers, em-dash separator `*Last updated: YYYY-MM-DD — reason*`
- Subsection headers (optional): `### Subsystem Name` (e.g., Phase 5 uses `### Dashboard Charts`, `### Mobile`). For Phase 11/12, either flat bullet list or grouped by CAT/OBE/REC vs SCHEMA/CF/CONTRA/RATE/WIZ/CSV sub-sections is fine — per CONTEXT.md §Claude's Discretion "Formatting details... as long as they match Phase 5–10 structure". Recommended: flat list for compactness, matching Phase 6/7/8/9/10 style (not Phase 5 which has sub-headings for DASH/UI/AUDT/ATTCH/RECR). Phase 11 has mixed prefixes (CAT/OBE/REC) — flat list is simplest.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply line-precise edits to REQUIREMENTS.md (insertions + mutations)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Make all mutations to `.planning/REQUIREMENTS.md` in a single task using the Edit tool (or MultiEdit for the multi-point traceability table updates). All text is pre-canonicalized in the `<interfaces>` block above — do NOT re-derive from phase PLANs or RESEARCH files beyond sanity-checking that the canonical text matches.

**Step 1 — Insert top-of-file total-requirements line (after line 5):**

Use Edit with:
- old_string: `**Core Value:** Accountants can view, manage, and close books across all family office entities in one fast, purpose-built GL — eliminating the per-entity cost and friction of QuickBooks Online.\n\n## v1 Requirements`
- new_string: `**Core Value:** Accountants can view, manage, and close books across all family office entities in one fast, purpose-built GL — eliminating the per-entity cost and friction of QuickBooks Online.\n**Total requirements:** 99 unique REQ-IDs across Phases 1–12, all Complete\n\n## v1 Requirements`

(Match-once on the existing text, append the new Total requirements line with a surrounding newline so it sits between `**Core Value:**` and the `## v1 Requirements` section header.)

**Step 2 — Append Phase 11 Requirements section after the existing Phase 10 Requirements section (after line 209 `POS-08` + divider):**

Use Edit with:
- old_string: `- [x] **POS-08**: Bank transactions post against position-level GL accounts (not holding summary accounts)\n\n---\n\n## Out of Scope`
- new_string:
```
- [x] **POS-08**: Bank transactions post against position-level GL accounts (not holding summary accounts)

---

## Phase 11 Requirements — Categorization UX & Opening Balances

- [x] **CAT-01**: Position picker replaces raw GL account picker as default categorization target for bank transactions
- [x] **CAT-03**: Categorization rules support optional positionId alongside existing accountId (GL resolved at apply-time)
- [x] **OBE-01**: Opening Balance Equity account (3900) auto-created if it does not exist
- [x] **OBE-02**: Opening balance JE auto-generated and posted immediately when holding created with non-zero balance
- [x] **OBE-03**: Adjusting JE auto-generated for the difference when holding balance is edited
- [x] **REC-01**: Posting a categorized bank transaction auto-marks it as RECONCILED (single and bulk paths)
- [x] **REC-03**: Reconciliation status badges on each transaction row (Reconciled green, Pending yellow, Unmatched red)
- [x] **REC-04**: Running reconciled vs unreconciled totals displayed at top of bank feed

---

## Phase 12 Requirements — Reporting Fixes & Onboarding Wizard

- [x] **SCHEMA-01**: Account schema accepts cashFlowCategory (enum) and isContra (boolean) fields
- [x] **CF-01**: Existing accounts receive correct cashFlowCategory via name-based inference backfill migration
- [x] **CF-02**: Cash flow statement classifies accounts by cashFlowCategory field, not name matching
- [x] **CF-03**: Account form shows cashFlowCategory dropdown only for ASSET/LIABILITY/EQUITY types
- [x] **CONTRA-01**: isContra flag drives contra-netting display on Balance Sheet (parent shows gross, contra shows as deduction, net total rendered)
- [x] **CONTRA-02**: Contra accounts render with "Less:" prefix and net total on Balance Sheet
- [x] **RATE-01**: Rate-based budget computation = holdingValue × rate / 12, using Decimal.js 4-decimal precision
- [x] **RATE-02**: Budget values snapshot at creation and do not auto-recalculate; manual recalculate via re-POST; holdings eligible if holding FMV OR sum of active position marketValues is non-zero
- [x] **WIZ-01**: Onboarding wizard triggers automatically after creating any new entity
- [x] **WIZ-02**: All four wizard steps (COA, Holdings, Opening Balances, First Transactions) are individually skippable with persistent progress
- [x] **WIZ-03**: Opening balance grid enforces debit=credit balance before JE generation; stored JE date matches form date (no UTC shift)
- [x] **CSV-01**: LLM-powered column mapping infers roles for non-standard CSV headers (bank, COA, budget imports)
- [x] **CSV-02**: System falls back to heuristic COLUMN_PATTERNS when LLM unavailable (missing API key, error, timeout)
- [x] **CSV-03**: User sees mapping confirmation UI before any CSV import proceeds; saved mappings auto-apply on header-fingerprint match; multi-account CSVs route per-row via Account column
- [x] **CSV-04**: Confirmed mappings persist per entity/source/importType and reuse-by-fingerprint

---

## Out of Scope
```

**Step 3 — Flip POS-01–08 from Pending to Complete in the traceability table (line ~253):**

Use Edit with:
- old_string: `| POS-01--08 | Phase 10 | Pending |`
- new_string: `| POS-01--08 | Phase 10 | Complete |`

**Step 4 — Insert 10 new traceability rows after the POS row:**

Use Edit with:
- old_string: `| POS-01--08 | Phase 10 | Complete |\n\n**Coverage:**`
- new_string:
```
| POS-01--08 | Phase 10 | Complete |
| CAT-01, CAT-03 | Phase 11 | Complete |
| OBE-01--03 | Phase 11 | Complete |
| REC-01, REC-03, REC-04 | Phase 11 | Complete |
| SCHEMA-01 | Phase 12 | Complete |
| CF-01--03 | Phase 12 | Complete |
| CONTRA-01--02 | Phase 12 | Complete |
| RATE-01--02 | Phase 12 | Complete |
| WIZ-01--03 | Phase 12 | Complete |
| CSV-01--04 | Phase 12 | Complete |

**Coverage:**
```

**Step 5 — Replace the Coverage bullet block:**

Use Edit with:
- old_string:
```
**Coverage:**
- Phases 1--9: Complete
- Phase 10 requirements (POS): 8 total -- Pending
```
- new_string:
```
**Coverage:**
- Phases 1–12: Complete — 99/99 REQ-IDs satisfied per v1.0 milestone audit
```

**Step 6 — Update the footer last-updated line:**

Use Edit with:
- old_string: `*Last updated: 2026-04-12 -- Phase 10 redefined: Positions Model & Holdings Overhaul*`
- new_string: `*Last updated: 2026-04-16 — v1.0 milestone close-out*`

**Verification mid-task:** After all 6 edits, run these greps to confirm mutations landed:

```
grep -q "^\*\*Total requirements:\*\* 99 unique REQ-IDs across Phases 1–12, all Complete$" .planning/REQUIREMENTS.md
grep -q "^## Phase 11 Requirements — Categorization UX & Opening Balances$" .planning/REQUIREMENTS.md
grep -q "^## Phase 12 Requirements — Reporting Fixes & Onboarding Wizard$" .planning/REQUIREMENTS.md
grep -q "^- \[x\] \*\*CAT-01\*\*:" .planning/REQUIREMENTS.md
grep -q "^- \[x\] \*\*CSV-04\*\*:" .planning/REQUIREMENTS.md
grep -q "^| POS-01--08 | Phase 10 | Complete |$" .planning/REQUIREMENTS.md
grep -vq "^| POS-01--08 | Phase 10 | Pending |$" .planning/REQUIREMENTS.md   # confirm Pending row gone
grep -q "^| CAT-01, CAT-03 | Phase 11 | Complete |$" .planning/REQUIREMENTS.md
grep -q "^| CSV-01--04 | Phase 12 | Complete |$" .planning/REQUIREMENTS.md
grep -q "^- Phases 1–12: Complete — 99/99 REQ-IDs satisfied per v1.0 milestone audit$" .planning/REQUIREMENTS.md
grep -vq "Phase 10 requirements (POS): 8 total -- Pending" .planning/REQUIREMENTS.md  # old coverage bullet gone
grep -q "^\*Last updated: 2026-04-16 — v1.0 milestone close-out\*$" .planning/REQUIREMENTS.md
```

If any grep returns non-zero (mutation didn't land): Read the file again and re-apply the failed Edit. Do NOT guess at alternative text — the `old_string` must match the existing content exactly. Common causes: byte-level differences between dashes (en-dash `–` vs em-dash `—` vs hyphen `-`), or vi vs nano whitespace variance. If the file already uses a different dash style in a given line, match it exactly in `old_string`.

**Negative-check grep for out-of-scope items (CAT-02/04, OBE-04 per RESEARCH.md §P6):**

```
grep -qv "\*\*CAT-02\*\*" .planning/REQUIREMENTS.md
grep -qv "\*\*CAT-04\*\*" .planning/REQUIREMENTS.md
grep -qv "\*\*OBE-04\*\*" .planning/REQUIREMENTS.md
```

All three should return non-zero (i.e., text not present). If any of those IDs appear in REQUIREMENTS.md, remove them — they are scoped-out phase-research artifacts per RESEARCH.md §P6 and CONTEXT.md §Decisions.
  </action>
  <verify>
    <automated>grep -q "^\*\*Total requirements:\*\* 99 unique REQ-IDs across Phases 1–12, all Complete$" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^## Phase 11 Requirements — Categorization UX &amp; Opening Balances$" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^## Phase 12 Requirements — Reporting Fixes &amp; Onboarding Wizard$" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^- \[x\] \*\*CAT-01\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^- \[x\] \*\*CSV-04\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^| POS-01--08 | Phase 10 | Complete |$" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^| CAT-01, CAT-03 | Phase 11 | Complete |$" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^| CSV-01--04 | Phase 12 | Complete |$" .planning/REQUIREMENTS.md &amp;&amp; grep -q "^\*Last updated: 2026-04-16 — v1.0 milestone close-out\*$" .planning/REQUIREMENTS.md &amp;&amp; ! grep -q "\*\*CAT-02\*\*\|\*\*CAT-04\*\*\|\*\*OBE-04\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md has: (1) top-of-file Total requirements line; (2) Phase 11 + Phase 12 full REQ-ID checklist sections with correct canonical text; (3) POS-01--08 Phase 10 Complete (NOT Pending); (4) 10 new traceability rows for Phase 11/12; (5) Coverage bullet replaced to "Phases 1–12: Complete"; (6) Footer updated to 2026-04-16 v1.0 close-out; (7) CAT-02/CAT-04/OBE-04 NOT present anywhere in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the REQUIREMENTS.md update</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Commit the single-file mutation from Task 1 via gsd-tools, following the existing `docs(NN-NN): subject` convention used throughout the project:

```
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(15-03): refresh REQUIREMENTS.md for v1.0 close-out — add Phase 11/12 sections, update traceability, add total count" --files .planning/REQUIREMENTS.md
```

After commit: Verify working tree clean for the target file:

```
test -z "$(git status --porcelain -- .planning/REQUIREMENTS.md)"
git log --oneline -- .planning/REQUIREMENTS.md | head -1
```

Expected: empty porcelain output (clean working tree for this path), one commit shown in log with the `docs(15-03): ...` subject.

If the commit fails (pre-commit hook rejection, etc.): Follow CLAUDE.md Git Safety Protocol. Do NOT --amend or skip hooks. Create a NEW commit that resolves the hook failure at its root cause. If the failure is unrelated to Phase 15 (e.g., lint error in a different file), investigate whether this is a pre-existing state issue precede this plan — if yes, flag it in SUMMARY.md Open Questions and propose a follow-up plan rather than accumulating the fix into this commit.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain -- .planning/REQUIREMENTS.md)" &amp;&amp; git log --oneline -- .planning/REQUIREMENTS.md | head -1 | grep -q "docs(15-03)"</automated>
  </verify>
  <done>REQUIREMENTS.md is committed to git on the current branch with a `docs(15-03): ...` subject. Working tree clean for this path.</done>
</task>

</tasks>

<verification>
After both tasks complete, confirm the full plan deliverable with the same grep suite used in Task 1 verify plus the git state check:

```
# Phase 11/12 sections and all REQ-IDs present
grep -q "^\*\*Total requirements:\*\* 99 unique REQ-IDs across Phases 1–12, all Complete$" .planning/REQUIREMENTS.md
grep -q "^## Phase 11 Requirements — Categorization UX & Opening Balances$" .planning/REQUIREMENTS.md
grep -q "^## Phase 12 Requirements — Reporting Fixes & Onboarding Wizard$" .planning/REQUIREMENTS.md
for REQ in CAT-01 CAT-03 OBE-01 OBE-02 OBE-03 REC-01 REC-03 REC-04 \
           SCHEMA-01 CF-01 CF-02 CF-03 CONTRA-01 CONTRA-02 RATE-01 RATE-02 WIZ-01 WIZ-02 WIZ-03 CSV-01 CSV-02 CSV-03 CSV-04; do
  grep -q "^- \[x\] \*\*${REQ}\*\*" .planning/REQUIREMENTS.md || echo "MISSING REQ-ID: $REQ"
done

# Traceability table rows
grep -q "^| POS-01--08 | Phase 10 | Complete |$" .planning/REQUIREMENTS.md
grep -q "^| CAT-01, CAT-03 | Phase 11 | Complete |$" .planning/REQUIREMENTS.md
grep -q "^| OBE-01--03 | Phase 11 | Complete |$" .planning/REQUIREMENTS.md
grep -q "^| REC-01, REC-03, REC-04 | Phase 11 | Complete |$" .planning/REQUIREMENTS.md
grep -q "^| SCHEMA-01 | Phase 12 | Complete |$" .planning/REQUIREMENTS.md
grep -q "^| CF-01--03 | Phase 12 | Complete |$" .planning/REQUIREMENTS.md
grep -q "^| CONTRA-01--02 | Phase 12 | Complete |$" .planning/REQUIREMENTS.md
grep -q "^| RATE-01--02 | Phase 12 | Complete |$" .planning/REQUIREMENTS.md
grep -q "^| WIZ-01--03 | Phase 12 | Complete |$" .planning/REQUIREMENTS.md
grep -q "^| CSV-01--04 | Phase 12 | Complete |$" .planning/REQUIREMENTS.md

# Coverage bullet replaced
grep -q "^- Phases 1–12: Complete — 99/99 REQ-IDs satisfied per v1.0 milestone audit$" .planning/REQUIREMENTS.md
! grep -q "Phase 10 requirements (POS): 8 total -- Pending" .planning/REQUIREMENTS.md

# Footer updated
grep -q "^\*Last updated: 2026-04-16 — v1.0 milestone close-out\*$" .planning/REQUIREMENTS.md

# Scoped-out REQ-IDs NOT present
! grep -q "\*\*CAT-02\*\*\|\*\*CAT-04\*\*\|\*\*OBE-04\*\*" .planning/REQUIREMENTS.md

# Committed to git
test -z "$(git status --porcelain -- .planning/REQUIREMENTS.md)"
git log --oneline -- .planning/REQUIREMENTS.md | head -1 | grep -q "docs(15-03)"
```

Expected output: no `MISSING REQ-ID` lines, all greps return zero exit, no negative-check greps fail.
</verification>

<success_criteria>
- `.planning/REQUIREMENTS.md` has all 23 new REQ-IDs documented (8 Phase 11 + 15 Phase 12) with canonical text matching RESEARCH.md §REQ-ID Canonical Text
- Traceability table has POS-01--08 Complete (not Pending) + 10 new Phase 11/12 rows in the existing `--` range + comma-discontinuous style
- Top-of-file `**Total requirements:** 99 unique REQ-IDs across Phases 1–12, all Complete` line inserted
- Bottom Coverage bullet replaced with single "Phases 1–12: Complete — 99/99 REQ-IDs satisfied per v1.0 milestone audit" line
- Footer updated to 2026-04-16 v1.0 milestone close-out
- CAT-02, CAT-04, OBE-04 NOT present in the file (scoped-out per RESEARCH.md §P6)
- File committed to git via single `docs(15-03): ...` commit
- Existing formatting conventions preserved (`--` for ranges, em-dash in headers, single-asterisk italic footer)
- No other files touched (no src/, no tests/, no prisma/, no other planning docs)
</success_criteria>

<output>
After completion, create `.planning/phases/15-verification-docs-refresh/15-03-SUMMARY.md` per the standard summary template. Record:
- Which edits used single Edit vs MultiEdit
- Any edits that required retry (dash-style mismatches between old_string and file content)
- Confirmation that CAT-02/CAT-04/OBE-04 were not accidentally included
- Commit SHA and subject
- Whether the recommended flat bullet list style (vs Phase 5-style sub-headings) was chosen for Phase 11/12 sections, and why

Do NOT update ROADMAP.md or STATE.md in this plan — the execute-phase orchestrator and the plan-level roll-up handle those.
</output>
