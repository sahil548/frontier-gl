---
phase: 15-verification-docs-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md
  - .planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - ".planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md exists with valid frontmatter and terminal verifier-approved status (one of passed | human_needed | gaps_found)"
    - ".planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md exists with valid frontmatter and terminal verifier-approved status (one of passed | human_needed | gaps_found)"
    - "12-VERIFICATION.md cites 12-UAT.md as primary evidence for the 12 UAT test cases plus the 7 gap closures delivered by plans 12-06/07/08/09 (verifier confirms citations match live code, does NOT re-execute UAT cases)"
    - "Both new VERIFICATION.md files are committed to git with a docs(15-01): ... subject following the existing docs(NN-NN): convention"
    - "Phase 15 plan task flow preserves sub-workflow bookkeeping — /gsd:verify-phase is invoked literally via the Skill tool, not substituted by Task() or Bash() wrappers"
  artifacts:
    - path: ".planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md"
      provides: "gsd-verifier VERIFICATION.md for Phase 10 (Positions Model & Holdings Overhaul) — 8 REQ-IDs POS-01..POS-08 verified against live code"
    - path: ".planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md"
      provides: "gsd-verifier VERIFICATION.md for Phase 12 (Reporting Fixes & Onboarding Wizard) — 15 REQ-IDs (SCHEMA-01, CF-01..03, CONTRA-01..02, RATE-01..02, WIZ-01..03, CSV-01..04) verified against live code + 12-UAT.md evidence"
  key_links:
    - from: "Phase 15 plan 15-01 executor"
      to: "/gsd:verify-phase sub-workflow"
      via: "Skill tool invocation (preserves workflow metadata)"
      pattern: "Skill\\(skill=\"gsd:verify-phase\", args=\"(10|12)\"\\)"
    - from: "12-VERIFICATION.md body"
      to: ".planning/phases/12-reporting-fixes-onboarding-wizard/12-UAT.md"
      via: "explicit citation of UAT evidence in the verifier output"
      pattern: "12-UAT\\.md"
estimated_duration: 15m
---

<objective>
Generate the two missing VERIFICATION.md files (Phase 10 and Phase 12) via `/gsd:verify-phase`, closing the verification-coverage gap catalogued in `.planning/v1.0-MILESTONE-AUDIT.md` (phases 10 and 12 have `VERIFICATION.md | missing`). Both phases were rolled up to ROADMAP [x] via SUMMARY + UAT evidence without a formal gsd-verifier pass; this plan fills that discipline gap.

Purpose: Close Stream A of the three documentation hygiene streams identified by the v1.0 milestone audit. After this plan completes, `verification_coverage` advances from 9/11 to 11/11. `human_needed` is an accepted terminal status per CONTEXT.md — matches the Phase 11 VERIFICATION.md pattern — so post-roll-up browser checks stay documented in `human_verification` frontmatter for post-phase follow-up rather than blocking Phase 15 completion.

Output: Two new files under `.planning/phases/`, one commit. No production code touched. No ROADMAP.md or STATE.md direct edits (sub-workflow runs in standalone mode — /gsd:verify-phase's `passed → update_roadmap` routing only fires from execute-phase orchestration, and Phase 10/12 ROADMAP entries are already `[x]`).

**Out of scope for this plan:**
- Test creation (Phase 13 owned CLASS-03/04/05 + Phase 11 it.todo stubs)
- Production code refactors (Phase 14 owned applyRules orphan, bank-tx POST delegation, Wizard OB DRAFT→POSTED, 7 deferred TS/test items)
- VALIDATION.md frontmatter refresh (Plan 15-02 owns this)
- REQUIREMENTS.md edits (Plan 15-03 owns this)
- Inline Chrome browser verification of already-manually-confirmed visuals — those become `human_verification` frontmatter entries in the VERIFICATION.md files, surfaced post-phase
- Fixing the 7 pre-existing failing tests in src/__tests__/hooks/use-entity.test.ts (Phase 14 scope — localStorage.clear Node 25 issue, unrelated to Phase 10/12 verification)
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/workflows/verify-phase.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/verification-report.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/15-verification-docs-refresh/15-CONTEXT.md
@.planning/phases/15-verification-docs-refresh/15-RESEARCH.md

# Phase 10 inputs — gsd-verifier will read these directly via verify-phase Step 2 (establish_must_haves reads PLAN must_haves)
@.planning/phases/10-positions-model-holdings-overhaul/10-01-PLAN.md
@.planning/phases/10-positions-model-holdings-overhaul/10-01-SUMMARY.md
@.planning/phases/10-positions-model-holdings-overhaul/10-02-PLAN.md
@.planning/phases/10-positions-model-holdings-overhaul/10-02-SUMMARY.md
@.planning/phases/10-positions-model-holdings-overhaul/10-03-PLAN.md
@.planning/phases/10-positions-model-holdings-overhaul/10-03-SUMMARY.md

# Phase 12 inputs — verifier reads these + 12-UAT.md as primary evidence
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-01-PLAN.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-01-SUMMARY.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-02-PLAN.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-02-SUMMARY.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-03-PLAN.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-03-SUMMARY.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-04-PLAN.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-04-SUMMARY.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-05-PLAN.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-05-SUMMARY.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-06-PLAN.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-06-SUMMARY.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-07-PLAN.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-07-SUMMARY.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-08-PLAN.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-08-SUMMARY.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-09-PLAN.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-09-SUMMARY.md
@.planning/phases/12-reporting-fixes-onboarding-wizard/12-UAT.md

# Template reference — Phase 11 VERIFICATION.md is the canonical `human_needed` example
@.planning/phases/11-categorization-ux-opening-balances/11-VERIFICATION.md

<interfaces>
<!-- Sub-workflow invocation pattern — locked by CONTEXT.md §Plan breakdown and RESEARCH.md §P8 -->
<!-- Use Skill tool, NOT Task tool or Bash wrapper. Skill tool preserves workflow metadata (model resolution, git bookkeeping). -->

Invocation syntax:
```
Skill(skill="gsd:verify-phase", args="10")
Skill(skill="gsd:verify-phase", args="12")
```

Produces (path computed by verify-phase.md line 211):
- .planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md
- .planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md

Frontmatter schema (verify-phase.md lines 10-15, template verification-report.md):
```yaml
---
phase: NN-slug
verified: YYYY-MM-DDTHH:MM:SSZ
status: passed | gaps_found | human_needed
score: N/M must-haves verified
# Optional:
human_verification:
  - test: "..."
    expected: "..."
    why_human: "..."
---
```

Must-haves source (verify-phase.md Step 2): Option A — PLAN frontmatter `must_haves` JSON.
Both Phase 10 and Phase 12 have must_haves blocks in all their PLAN.md files (confirmed in RESEARCH.md §Sub-Workflow Output Schemas).

Auto-commit behavior (RESEARCH.md §P5): verify-phase.md does NOT auto-commit when invoked standalone (i.e., not via /gsd:execute-phase orchestration). This task MUST include an explicit git commit step after both verify-phase runs complete.

Terminal statuses (verify-phase.md lines 190-198):
- `passed` — all truths verified, no human items
- `gaps_found` — blocking issue needs a fix plan (would trigger orchestrator fix-plan loop; for Phase 15 standalone use, the existing file on disk is still a valid Phase 15 deliverable — Phase 13/14 already own the underlying code/test debt)
- `human_needed` — automated checks pass, browser/visual items remain (accepted by CONTEXT.md as terminal)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run /gsd:verify-phase 10 to generate 10-VERIFICATION.md</name>
  <files>.planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md</files>
  <action>
Invoke the /gsd:verify-phase sub-workflow for Phase 10 via the Skill tool — this is the literal locked pattern from 15-CONTEXT.md and 15-RESEARCH.md §P8. Do NOT substitute with Task(subagent_type="gsd-verifier", ...) or Bash("claude skill run ..."). The Skill tool preserves workflow metadata (model resolution, STATE session entries).

Exact invocation:
```
Skill(skill="gsd:verify-phase", args="10")
```

What the sub-workflow does (from verify-phase.md):
1. Reads .planning/phases/10-positions-model-holdings-overhaul/ — 10-01/02/03-PLAN.md must_haves + 10-01/02/03-SUMMARY.md
2. Spawns gsd-verifier subagent to verify live code against must_haves (POS-01 through POS-08)
3. Writes .planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md with terminal status (expected: passed OR human_needed — Phase 10 has position-level GL display, 13-type holdings page grouping, AddPositionsPrompt which are Chrome-verifiable visuals that map naturally to human_verification frontmatter entries)
4. Does NOT auto-commit when invoked standalone — RESEARCH.md §P5 explicitly flags this

Acceptance pre-commit:
- File exists at .planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md
- Frontmatter includes `phase: 10-positions-model-holdings-overhaul`, `verified: <ISO timestamp>`, `status: <passed | human_needed | gaps_found>`, `score: N/M must-haves verified`
- Body sections follow verification-report.md template: Goal Achievement, Requirements Coverage, Anti-Patterns, Human Verification Required, Gaps Summary, Verification Metadata

If verifier returns `gaps_found`: The file on disk is still a valid Phase 15 deliverable — Phase 13 already owns the missing test coverage layer and Phase 14 already owns the orphan/refactor hygiene. Record the file as-is and move on. Do NOT attempt fix-plan loops — they are the execute-phase orchestrator's job, not Phase 15's.

If the Skill tool is unavailable or the sub-workflow errors out: Fall back to Task(subagent_type="gsd-verifier", ...) with the same phase argument, and note the fallback in the task log for SUMMARY commentary.

After the sub-workflow returns: Commit the new file explicitly (this is the MANDATORY post-verify commit step per RESEARCH.md §P5, since verify-phase does NOT auto-commit standalone):

```
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(15-01): generate 10-VERIFICATION.md via gsd-verifier" --files .planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md
```

Reserve the cross-plan umbrella commit for Task 3 below (after Task 2 also lands). If the executor prefers to defer both commits to a single end-of-plan commit in Task 3, that is also acceptable — either granularity matches the "one commit per plan (per-stream commits)" locked decision in 15-CONTEXT.md §Plan breakdown.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md &amp;&amp; grep -E "^status: (passed|human_needed|gaps_found)$" .planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md</automated>
  </verify>
  <done>10-VERIFICATION.md exists with a terminal status in {passed, human_needed, gaps_found}. Frontmatter includes `phase:`, `verified:`, `status:`, `score:`. Body follows verification-report.md template sections.</done>
</task>

<task type="auto">
  <name>Task 2: Run /gsd:verify-phase 12 to generate 12-VERIFICATION.md</name>
  <files>.planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md</files>
  <action>
Invoke the /gsd:verify-phase sub-workflow for Phase 12 via the Skill tool. Same invocation pattern as Task 1 — literal, not substituted.

Exact invocation:
```
Skill(skill="gsd:verify-phase", args="12")
```

What the sub-workflow does:
1. Reads 12-01 through 12-09 PLANs + SUMMARYs (9 plans — 6 initial + 4 gap-closure from UAT; note 12-00 Wave 0 stubs plus 12-01..09 is 10 plan rows per ROADMAP.md line 209)
2. Spawns gsd-verifier subagent to verify live code against must_haves (SCHEMA-01, CF-01..03, CONTRA-01..02, RATE-01..02, WIZ-01..03, CSV-01..04 — 15 REQ-IDs total)
3. Writes 12-VERIFICATION.md

CRITICAL CITATION REQUIREMENT (from 15-CONTEXT.md §Implementation Decisions):
`12-VERIFICATION.md` MUST cite `.planning/phases/12-reporting-fixes-onboarding-wizard/12-UAT.md` as primary evidence for the 12 UAT test cases + 7 gap closures (plans 12-06/07/08/09). The verifier confirms the citations match live code — it does NOT re-execute all UAT cases (that would be redundant; UAT already closed all 7 gaps per the audit). If the gsd-verifier subagent produces output that omits 12-UAT.md references, the executor must either:
  (a) re-invoke the sub-workflow with an additional hint passed to the Skill args ("args=\"12 --uat-evidence=12-UAT.md\"" or via an env-hint mechanism if the workflow exposes one), OR
  (b) edit the produced 12-VERIFICATION.md to add an explicit "See 12-UAT.md for UAT-level evidence of WIZ-01..03 + CSV-01..04 + RATE-02 closures" note in the Requirements Coverage and/or Gaps Summary sections.

Option (b) is the safe fallback — a one-line Edit-tool injection preserves the verifier's automated analysis while meeting the citation requirement.

If a UAT gap closure is mis-reported as an open gap (e.g., verifier doesn't know that 12-07 closed WIZ-01/02/03 ReturnToWizardBanner + date-fidelity + backfill): This is a terminal `gaps_found` outcome for the file. Do NOT attempt to re-run Phase 12 fix plans — all 7 UAT gaps are already closed in code and documented in 12-UAT.md + the respective 12-06..09 SUMMARYs. Accept the file as-is. The verifier's gap table may be stale relative to UAT; the citation requirement above surfaces this.

Terminal status expectations:
- `passed` — ideal but unlikely given Phase 12's UI-heavy surface area (CF dropdown, contra display, wizard UX, column mapping confirmation dialog)
- `human_needed` — most likely outcome; Phase 12 has multiple browser-verifiable visuals (CF-03 dropdown visibility, CONTRA-02 Less: prefix rendering, WIZ-01 wizard flow, CSV-03 confirmation UI)
- `gaps_found` — acceptable terminal outcome for Phase 15; Phase 13/14 already own the underlying debt

After the sub-workflow returns: Commit explicitly (same pattern as Task 1):

```
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(15-01): generate 12-VERIFICATION.md via gsd-verifier" --files .planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md
```

OR defer to a single plan-level commit in Task 3 (executor's choice, both satisfy 15-CONTEXT.md's per-stream commit lock).
  </action>
  <verify>
    <automated>test -f .planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md &amp;&amp; grep -E "^status: (passed|human_needed|gaps_found)$" .planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md &amp;&amp; grep -l "12-UAT" .planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md</automated>
  </verify>
  <done>12-VERIFICATION.md exists with terminal status in {passed, human_needed, gaps_found}, frontmatter includes all required fields, and body cites `12-UAT.md` as primary evidence for the 12 UAT cases + 7 gap closures.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate plan-level commit (if Task 1/2 did not commit individually)</name>
  <files>.planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md, .planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md</files>
  <action>
This task is an umbrella safety commit. If Task 1 and Task 2 both committed individually (two separate docs(15-01): commits), this task is a no-op — just verify that `git status --porcelain` is empty for the two target files.

If Task 1 and/or Task 2 deferred their commit to this umbrella task (executor's choice), run a single consolidated commit:

```
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(15-01): generate 10-VERIFICATION.md + 12-VERIFICATION.md via gsd-verifier" --files .planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md .planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md
```

Either path satisfies 15-CONTEXT.md's "one commit per plan" locked decision — "per plan" admits either 1 umbrella or N-granular commits under the same plan scope, as long as each commit subject follows `docs(15-01): ...` and both files land on main before the 15-01-SUMMARY.md write.

After commit (or no-op): Confirm both files are at HEAD:

```
git log --oneline --all -- .planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md .planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md | head -5
```

If any commit step fails (e.g., pre-commit hook rejection): Fix the root cause per CLAUDE.md Git Safety Protocol. Do NOT --amend or skip hooks (--no-verify). Create a NEW commit that resolves the hook failure. If the failure is irrelevant to Phase 15 (e.g., an unrelated lint error in an untouched file), investigate whether a pre-existing state issue precedes this plan — if yes, flag in SUMMARY.md Open Questions and propose a follow-up plan rather than accumulating the fix into this commit.
  </action>
  <verify>
    <automated>git log --oneline -- .planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md | head -1 &amp;&amp; git log --oneline -- .planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md | head -1 &amp;&amp; test -z "$(git status --porcelain -- .planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md .planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md)"</automated>
  </verify>
  <done>Both VERIFICATION.md files are committed to git on the current branch. `git status --porcelain` is empty for the two target paths. Commit subject(s) follow `docs(15-01): ...` convention.</done>
</task>

</tasks>

<verification>
After all three tasks complete, confirm the full plan deliverable:

```
test -f .planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md && \
  test -f .planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md && \
  grep -E "^status: (passed|human_needed|gaps_found)$" .planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md && \
  grep -E "^status: (passed|human_needed|gaps_found)$" .planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md && \
  grep -l "12-UAT" .planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md && \
  test -z "$(git status --porcelain -- .planning/phases/10-positions-model-holdings-overhaul/10-VERIFICATION.md .planning/phases/12-reporting-fixes-onboarding-wizard/12-VERIFICATION.md)"
```

Expected output: both files present, both with terminal status, 12-VERIFICATION.md references 12-UAT.md, working tree clean for both paths.
</verification>

<success_criteria>
- Both VERIFICATION.md files exist on disk and are committed to git
- Both have valid frontmatter (phase, verified, status, score)
- Both have terminal status in {passed, human_needed, gaps_found}
- 12-VERIFICATION.md cites 12-UAT.md as primary evidence
- No production code touched (no src/, no tests/, no prisma/)
- No new test files created (Phase 13 scope, not Phase 15)
- No ROADMAP.md or STATE.md direct edits in plan body
- verification_coverage advances from 9/11 to 11/11 per v1.0-MILESTONE-AUDIT.md accounting
</success_criteria>

<output>
After completion, create `.planning/phases/15-verification-docs-refresh/15-01-SUMMARY.md` per the standard summary template. Record:
- Which sub-workflow runs produced which file
- Terminal status for each phase (passed / human_needed / gaps_found)
- Any human_verification entries flagged by the verifier (surface for post-phase follow-up)
- Whether commit strategy was per-file or umbrella
- Any fallback paths used (Task-tool invocation instead of Skill, Edit-tool post-hoc injection for UAT citation)

Do NOT update ROADMAP.md or STATE.md in this plan — the execute-phase orchestrator and the plan-level roll-up handle those.
</output>
