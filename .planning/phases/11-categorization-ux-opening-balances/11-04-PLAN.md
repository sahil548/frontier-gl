---
phase: 11-categorization-ux-opening-balances
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - src/app/api/entities/[entityId]/bank-transactions/[transactionId]/route.ts
  - src/app/api/entities/[entityId]/bank-transactions/bulk-categorize/route.ts
  - src/components/bank-feed/transaction-table.tsx
  - src/components/bank-feed/reconciliation-summary.tsx
  - src/app/(auth)/bank-feed/page.tsx
  - src/app/(auth)/holdings/page.tsx
autonomous: true
requirements: [REC-01, REC-03, REC-04]

must_haves:
  truths:
    - "Posting a categorized bank transaction automatically sets reconciliationStatus to RECONCILED"
    - "Bulk-post sets reconciliationStatus to RECONCILED on all posted transactions"
    - "Each transaction row shows a reconciliation status badge (Reconciled green, Pending yellow, Unmatched red)"
    - "Running reconciled vs unreconciled totals displayed at top of bank feed"
    - "Clicking a bank account holding navigates to bank feed filtered to that holding"
  artifacts:
    - path: "src/components/bank-feed/reconciliation-summary.tsx"
      provides: "Running totals bar showing reconciled vs unreconciled counts and amounts"
      min_lines: 40
    - path: "src/components/bank-feed/transaction-table.tsx"
      provides: "Reconciliation status badges on each transaction row"
    - path: "src/app/api/entities/[entityId]/bank-transactions/[transactionId]/route.ts"
      provides: "Auto-reconcile on POST (set reconciliationStatus=RECONCILED)"
    - path: "src/app/api/entities/[entityId]/bank-transactions/bulk-categorize/route.ts"
      provides: "Bulk auto-reconcile on all posted transactions"
  key_links:
    - from: "src/app/api/entities/[entityId]/bank-transactions/[transactionId]/route.ts"
      to: "prisma.bankTransaction.update"
      via: "reconciliationStatus set to RECONCILED after JE creation"
      pattern: "reconciliationStatus.*RECONCILED"
    - from: "src/components/bank-feed/reconciliation-summary.tsx"
      to: "bank-feed/page.tsx"
      via: "aggregated transaction status data passed as props"
      pattern: "ReconciliationSummary"
    - from: "src/app/(auth)/holdings/page.tsx"
      to: "/bank-feed?subledgerItemId="
      via: "bank account holding click navigates to filtered bank feed"
      pattern: "router\\.push.*bank-feed.*subledgerItemId"
---

<objective>
Auto-reconcile on post, reconciliation status badges, running totals, and holdings-to-bank-feed navigation

Purpose: Merge reconciliation into the bank feed. Posting a transaction automatically marks it reconciled. Each transaction row shows its status via a colored badge. A summary bar at the top shows running totals of reconciled vs unreconciled amounts. Bank account holdings link directly to the bank feed page.
Output: Auto-reconcile in transaction POST/bulk-post, status badges in TransactionTable, ReconciliationSummary component, holdings navigation to bank feed
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-categorization-ux-opening-balances/11-CONTEXT.md
@.planning/phases/11-categorization-ux-opening-balances/11-RESEARCH.md
@.planning/phases/11-categorization-ux-opening-balances/11-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plans 01 and 02. -->

From prisma/schema.prisma (updated in Plan 01):
```prisma
model BankTransaction {
  // ... existing fields
  reconciliationStatus  String  @default("PENDING")  // PENDING, RECONCILED, UNMATCHED
}
```

From src/app/api/entities/[entityId]/bank-transactions/[transactionId]/route.ts (existing POST handler):
```typescript
// POST handler: creates JE from categorized transaction
// Currently: creates JE, updates transaction with journalEntryId and status=POSTED
// Needs: also set reconciliationStatus = "RECONCILED" after JE creation
```

From src/app/api/entities/[entityId]/bank-transactions/bulk-categorize/route.ts (existing):
```typescript
// POST handler: atomically categorizes and posts multiple transactions
// Needs: set reconciliationStatus = "RECONCILED" on each posted transaction
```

From src/components/bank-feed/transaction-table.tsx (existing):
```typescript
// TransactionTable with checkbox selection, status badges, account combobox
// Columns: checkbox, date, description, amount, account, status, actions
// compact prop for Holdings inline display
// Needs: reconciliationStatus badge column
```

From src/app/(auth)/bank-feed/page.tsx (existing):
```typescript
// Bank Feed page with CSV import, tab filtering, bank account selector
// Fetches transactions from GET /api/entities/:entityId/bank-transactions
// Needs: ReconciliationSummary at top, pass reconciliationStatus in data
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-reconcile on POST + bulk-post + reconciliation summary component</name>
  <files>src/app/api/entities/[entityId]/bank-transactions/[transactionId]/route.ts, src/app/api/entities/[entityId]/bank-transactions/bulk-categorize/route.ts, src/components/bank-feed/reconciliation-summary.tsx</files>
  <action>
1. **Transaction POST handler auto-reconcile (src/app/api/entities/[entityId]/bank-transactions/[transactionId]/route.ts):**
   - In the POST handler, after the JE is created and the transaction is updated with `journalEntryId` and `status: "POSTED"`:
   - Add `reconciliationStatus: "RECONCILED"` to the same `prisma.bankTransaction.update` call
   - This is a one-line addition to the existing update object
   - Do NOT set reconciliationStatus when the transaction is EXCLUDED (only on actual JE posting)

2. **Bulk-categorize auto-reconcile (src/app/api/entities/[entityId]/bank-transactions/bulk-categorize/route.ts):**
   - In the bulk processing loop, where each transaction is posted and gets `status: "POSTED"`:
   - Add `reconciliationStatus: "RECONCILED"` to the same update call
   - Same one-line addition per transaction in the batch

3. **ReconciliationSummary component (src/components/bank-feed/reconciliation-summary.tsx):**
   - New 'use client' component
   - Props: `{ transactions: Array<{ amount: string | number, reconciliationStatus: string }> }`
   - Compute from transactions array:
     - `reconciledCount`: count where status === "RECONCILED"
     - `pendingCount`: count where status === "PENDING"
     - `unmatchedCount`: count where status === "UNMATCHED"
     - `reconciledTotal`: sum of absolute amounts where RECONCILED
     - `unreconciledTotal`: sum of absolute amounts where PENDING or UNMATCHED
   - Layout: horizontal bar with 3 sections (use flex-row gap-4):
     - "Reconciled: {count} ({formatCurrency(total)})" with green dot indicator
     - "Pending: {count} ({formatCurrency(total)})" with yellow dot indicator
     - "Unmatched: {count}" with red dot indicator (if any)
   - Use the existing `formatCurrency` utility from `@/lib/utils/accounting`
   - Render as a Card or muted background bar above the transaction table
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Transaction POST and bulk-post set reconciliationStatus to RECONCILED after JE creation. ReconciliationSummary component computes and displays running totals with colored indicators.</done>
</task>

<task type="auto">
  <name>Task 2: Transaction table badges + bank feed integration + holdings navigation</name>
  <files>src/components/bank-feed/transaction-table.tsx, src/app/(auth)/bank-feed/page.tsx, src/app/(auth)/holdings/page.tsx</files>
  <action>
1. **TransactionTable reconciliation badges (src/components/bank-feed/transaction-table.tsx):**
   - Add a "Recon" column (narrow, after the existing Status column):
     - RECONCILED: green Badge with "Reconciled" text (or green circle icon for compact mode)
     - PENDING: yellow/amber Badge with "Pending" text
     - UNMATCHED: red Badge with "Unmatched" text
   - Use shadcn Badge with variant styling: `className` with `bg-green-100 text-green-700` / `bg-amber-100 text-amber-700` / `bg-red-100 text-red-700` (Claude's discretion on exact colors)
   - In compact mode: show just a colored dot (w-2 h-2 rounded-full) instead of full badge text to save space
   - Ensure the column does not appear if the data has no reconciliationStatus field (backward compat)

2. **Bank feed page integration (src/app/(auth)/bank-feed/page.tsx):**
   - Import ReconciliationSummary component
   - Place it above the transaction table, below the tab filters
   - Pass the current filtered transactions array to ReconciliationSummary
   - Ensure the GET API response includes `reconciliationStatus` in the serialized transaction data (check the API route's serialize function -- add it if missing)
   - The GET API route at `/api/entities/[entityId]/bank-transactions/route.ts` needs to include `reconciliationStatus` in its serialization if it doesn't already (it should come through since it's a direct field, but verify)

3. **Holdings page navigation (src/app/(auth)/holdings/page.tsx):**
   - For bank account holdings (itemType === "BANK_ACCOUNT"): clicking the holding name navigates to `/bank-feed?subledgerItemId=${item.id}` instead of expanding inline
   - Use `router.push()` (already available via `useRouter`)
   - The bank feed page already supports `subledgerItemId` as a filter parameter for the bank account selector
   - Keep the existing InlineBankTransactions for non-bank holdings (INVESTMENT, REAL_ESTATE, etc.) -- those don't have bank feed transactions
   - Add a small "View Bank Feed" link/button next to bank account holdings as an additional entry point (lucide-react ExternalLink icon)
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx tsc --noEmit 2>&1 | tail -10 && npx vitest run tests/bank-transactions/reconciliation-summary.test.ts tests/bank-transactions/auto-reconcile.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>TransactionTable shows reconciliation status badges (green/yellow/red). Bank feed page displays ReconciliationSummary with running totals above the table. Bank account holdings navigate directly to bank feed page. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run tests/bank-transactions/reconciliation-summary.test.ts tests/bank-transactions/auto-reconcile.test.ts --reporter=verbose` all tests pass
- Posting a transaction sets reconciliationStatus to RECONCILED (visible in badge)
- Bank feed page shows ReconciliationSummary bar with counts and totals
- Transaction rows show colored reconciliation badges
- Clicking a bank account holding navigates to bank feed filtered to that account
</verification>

<success_criteria>
- Auto-reconcile: posting a transaction (single or bulk) sets reconciliationStatus=RECONCILED
- Badges: each transaction row shows colored reconciliation status badge (green/yellow/red)
- Summary: running totals bar shows reconciled count/amount vs pending count/amount
- Navigation: bank account holdings click through to bank feed page
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-categorization-ux-opening-balances/11-04-SUMMARY.md`
</output>
