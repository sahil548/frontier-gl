---
phase: 03-ledger-and-trial-balance
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/app/api/entities/[entityId]/ledger/[accountId]/route.ts
  - src/app/(dashboard)/gl-ledger/page.tsx
  - src/app/(dashboard)/gl-ledger/[accountId]/page.tsx
  - src/components/gl-ledger/ledger-columns.tsx
  - src/components/gl-ledger/ledger-filters.tsx
  - src/components/gl-ledger/account-summary-card.tsx
  - src/components/gl-ledger/ledger-export.tsx
autonomous: true
requirements: [LED-01, LED-02, LED-03]

must_haves:
  truths:
    - "User can view all posted transactions for any account with date, JE number, description, debit, credit, and running balance"
    - "User can filter the GL ledger by date range, amount range, and memo text"
    - "Ledger is paginated at 50 transactions per page"
    - "User can navigate to GL Ledger via sidebar or by clicking an account in Chart of Accounts"
  artifacts:
    - path: "src/app/api/entities/[entityId]/ledger/[accountId]/route.ts"
      provides: "GET endpoint returning ledger transactions with running balance"
      exports: ["GET"]
    - path: "src/app/(dashboard)/gl-ledger/page.tsx"
      provides: "Standalone GL Ledger page with account combobox selector"
    - path: "src/app/(dashboard)/gl-ledger/[accountId]/page.tsx"
      provides: "Account-specific ledger view with table, filters, and export"
    - path: "src/components/gl-ledger/ledger-columns.tsx"
      provides: "TanStack Table column definitions for ledger"
      exports: ["ledgerColumns"]
    - path: "src/components/gl-ledger/ledger-filters.tsx"
      provides: "Inline filter bar with date range, amount, memo search"
      exports: ["LedgerFilters"]
    - path: "src/components/gl-ledger/account-summary-card.tsx"
      provides: "Account header card showing number, name, type, balance, YTD activity"
      exports: ["AccountSummaryCard"]
    - path: "src/components/gl-ledger/ledger-export.tsx"
      provides: "Export dropdown button with CSV and PDF options"
      exports: ["LedgerExport"]
  key_links:
    - from: "src/app/(dashboard)/gl-ledger/[accountId]/page.tsx"
      to: "/api/entities/:entityId/ledger/:accountId"
      via: "fetch with filter params"
      pattern: "fetch.*ledger"
    - from: "src/components/gl-ledger/ledger-export.tsx"
      to: "src/lib/export/csv-export.ts"
      via: "exportToCsv function call"
      pattern: "exportToCsv"
    - from: "src/components/gl-ledger/ledger-export.tsx"
      to: "src/lib/export/pdf-export.ts"
      via: "exportLedgerToPdf function call"
      pattern: "exportLedgerToPdf"
---

<objective>
Build the complete GL Ledger feature: API endpoint, standalone page with account selector, account-specific ledger view with data table, filters, summary card, and export.

Purpose: The GL Ledger is the primary read-side view for accountants to see all posted transactions for any account with running balance -- the most frequently used accounting report.
Output: Fully functional GL Ledger accessible via sidebar nav and COA drill-down.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ledger-and-trial-balance/03-CONTEXT.md
@.planning/phases/03-ledger-and-trial-balance/03-RESEARCH.md
@.planning/phases/03-ledger-and-trial-balance/03-01-SUMMARY.md
@.planning/phases/03-ledger-and-trial-balance/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From src/components/data-table/data-table.tsx:
```typescript
export function DataTable<TData, TValue>({ columns, data, pageSize, footerRow }: DataTableProps<TData, TValue>): JSX.Element
```

From src/components/ui/account-combobox.tsx:
```typescript
export function AccountCombobox({ accounts, value, onSelect, placeholder }: AccountComboboxProps): JSX.Element
```

From src/components/ui/date-range-picker.tsx:
```typescript
export function DateRangePicker({ dateRange, onDateRangeChange, className }: DateRangePickerProps): JSX.Element
```

<!-- From Plan 02 -->
From src/lib/queries/ledger-queries.ts:
```typescript
export type LedgerFilters = { startDate: Date; endDate: Date; memoSearch?: string; minAmount?: number; maxAmount?: number }
export type LedgerRow = { date: Date; jeNumber: string; jeId: string; description: string; lineMemo: string | null; debit: number; credit: number; runningBalance: number }
export function getLedgerTransactions(entityId: string, accountId: string, filters: LedgerFilters): Promise<{ beginningBalance: number; transactions: LedgerRow[] }>
export function getAccountSummary(entityId: string, accountId: string): Promise<{ currentBalance: number; ytdDebits: number; ytdCredits: number }>
```

From src/lib/export/csv-export.ts:
```typescript
export function exportToCsv(data: Record<string, unknown>[], filename: string): void
```

From src/lib/export/pdf-export.ts:
```typescript
export function exportLedgerToPdf(data: LedgerRow[], meta: { entityName: string; accountName: string; accountNumber: string; dateRange: string; beginningBalance: number; totalDebits: number; totalCredits: number; netChange: number }): void
```

From src/lib/utils/accounting.ts:
```typescript
export function formatCurrency(amount: number | string): string
export type AccountType = 'ASSET' | 'LIABILITY' | 'EQUITY' | 'INCOME' | 'EXPENSE'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GL Ledger API endpoint and column definitions</name>
  <files>
    src/app/api/entities/[entityId]/ledger/[accountId]/route.ts,
    src/components/gl-ledger/ledger-columns.tsx
  </files>
  <action>
**API route** (`src/app/api/entities/[entityId]/ledger/[accountId]/route.ts`):
- GET handler accepting query params: `startDate`, `endDate`, `memoSearch`, `minAmount`, `maxAmount`
- Validate params with Zod: startDate and endDate required (ISO strings), memoSearch optional string, minAmount/maxAmount optional numbers
- Default date range: first of current month through today (using date-fns startOfMonth)
- Call `getLedgerTransactions` from `@/lib/queries/ledger-queries`
- Call `getAccountSummary` from `@/lib/queries/ledger-queries`
- Fetch account details (name, number, type) from Prisma -- `prisma.account.findUnique({ where: { id: accountId, entityId } })`
- Return JSON: `{ account: { id, name, accountNumber, type }, summary: { currentBalance, ytdDebits, ytdCredits }, beginningBalance, transactions: LedgerRow[] }`
- Return 404 if account not found or doesn't belong to entity
- Return 400 for invalid params with error message
- Follow existing API response pattern from Phase 1 (check `src/app/api/` for conventions)

**Column definitions** (`src/components/gl-ledger/ledger-columns.tsx`):
- "use client" file exporting `ledgerColumns: ColumnDef<LedgerRow>[]`
- Columns per user decision: Date | JE# | Description | Debit | Credit | Balance
- Date column: format with date-fns `format(date, "MMM d, yyyy")`, sortable via DataTableColumnHeader
- JE# column: render as clickable Link to `/journal-entries/{jeId}` (per user decision: JE# is a clickable link). Use Next.js Link component.
- Description column: show `description` with `lineMemo` on a second line in muted text if present
- Debit column: right-aligned, show `formatCurrency(debit)` only when debit > 0, otherwise empty string
- Credit column: right-aligned, show `formatCurrency(credit)` only when credit > 0, otherwise empty string
- Balance column: right-aligned, show `formatCurrency(runningBalance)`, not sortable (running balance is positional)
- Default sort: newest first (date descending) -- per user decision
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>GET /api/entities/:entityId/ledger/:accountId returns ledger data with running balance; column definitions match the locked layout: Date | JE# | Description | Debit | Credit | Balance</done>
</task>

<task type="auto">
  <name>Task 2: Create GL Ledger filter bar, summary card, and export dropdown</name>
  <files>
    src/components/gl-ledger/ledger-filters.tsx,
    src/components/gl-ledger/account-summary-card.tsx,
    src/components/gl-ledger/ledger-export.tsx
  </files>
  <action>
**Ledger Filters** (`src/components/gl-ledger/ledger-filters.tsx`):
- "use client" component, always visible inline above table (per user decision: not collapsible)
- Props: `onFiltersChange: (filters: LedgerFilterValues) => void`, `initialDateRange: DateRange`
- Layout: horizontal flex row with items: DateRangePicker, memo text Input, min amount Input, max amount Input, clear button
- Date range: use DateRangePicker component from Plan 01. Default: current month (startOfMonth through today)
- Memo search: shadcn Input with placeholder "Search description/memo...", 300ms debounce (use custom useDebouncedCallback hook or useEffect with setTimeout/clearTimeout). Per user decision: debounced instant apply.
- Amount range: two shadcn Input fields (type="number", step="0.01") for min and max. Label "Amount range". Per user decision: filters on debit OR credit falling within range.
- Active filters shown as removable Badge chips below the filter row. Each chip shows the filter name/value and has an X button to remove that filter.
- When a filter is cleared via chip, immediately call onFiltersChange with that filter removed
- Clear All button resets to defaults (current month date range, no text/amount filters)

**Account Summary Card** (`src/components/gl-ledger/account-summary-card.tsx`):
- "use client" component displayed above the filter bar and table (per user decision)
- Props: `account: { accountNumber: string; name: string; type: AccountType }`, `summary: { currentBalance: number; ytdDebits: number; ytdCredits: number }`
- Card layout using shadcn Card: left side shows account number (large, bold), account name, and account type badge. Right side shows current balance (large number), YTD total debits and YTD total credits (smaller, labeled).
- Use formatCurrency for all money values

**Ledger Export** (`src/components/gl-ledger/ledger-export.tsx`):
- "use client" component
- Props: `transactions: LedgerRow[]`, `meta: { entityName: string; accountName: string; accountNumber: string; dateRange: string; beginningBalance: number; totalDebits: number; totalCredits: number; netChange: number }`
- Single "Export" dropdown button using shadcn DropdownMenu (per user decision)
- Two items: "Export as CSV" with FileSpreadsheet icon, "Export as PDF" with FileText icon from lucide-react
- CSV option: assembles data array with beginning balance row at top, all transaction rows, and totals/net change footer row (per user decision), then calls `exportToCsv`
- PDF option: calls `exportLedgerToPdf` with transactions and meta
- Per user decision: exports include only currently filtered/visible data (WYSIWYG)
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Filter bar renders inline with date range, memo search (debounced 300ms), amount range, and removable chips. Summary card shows account details and YTD activity. Export dropdown triggers CSV and PDF generation.</done>
</task>

<task type="auto">
  <name>Task 3: Create GL Ledger pages and wire everything together</name>
  <files>
    src/app/(dashboard)/gl-ledger/page.tsx,
    src/app/(dashboard)/gl-ledger/[accountId]/page.tsx
  </files>
  <action>
**Standalone GL Ledger page** (`src/app/(dashboard)/gl-ledger/page.tsx`):
- Server component that fetches the list of accounts for the current entity
- Get entityId from the entity context (however Phase 1 implemented entity switching -- check existing code for the pattern, likely a context provider or cookie/localStorage)
- Fetch accounts from existing Phase 2 API or directly from Prisma: `prisma.account.findMany({ where: { entityId, isActive: true }, orderBy: { accountNumber: 'asc' } })`
- Renders page title "General Ledger" and the AccountCombobox component
- When an account is selected, navigate to `/gl-ledger/{accountId}` using Next.js router
- If no accounts exist, show empty state: "No accounts found. Create accounts in Chart of Accounts first." with a link to /chart-of-accounts
- Per user decision: standalone ledger page uses searchable combobox (type-ahead by account name/number)

**Account Ledger page** (`src/app/(dashboard)/gl-ledger/[accountId]/page.tsx`):
- Client component ("use client") that orchestrates the full ledger view
- Extract accountId from route params
- Get entityId from entity context
- State: `filters` (LedgerFilterValues), `ledgerData` (API response), `loading` (boolean)
- On mount and filter change: fetch from `/api/entities/${entityId}/ledger/${accountId}?${filterParams}`
- Default filters: current month date range (startOfMonth through today per user decision)
- Compute totals from transactions: totalDebits = sum of all debit values, totalCredits = sum of all credit values, netChange = totalDebits - totalCredits
- Render in order (top to bottom per user decisions):
  1. AccountSummaryCard -- account number, name, type, current balance, YTD activity
  2. LedgerFilters -- inline, always visible
  3. LedgerExport -- positioned right-aligned above table
  4. DataTable with ledgerColumns, pageSize=50 (per user decision), with:
     - Beginning balance row at top: a synthetic row showing "Beginning Balance" in description, balance in Balance column (per user decision)
     - Totals/summary footer row: period total debits, total credits, net change (per user decision)
  5. Loading state: show skeleton (Skeleton components from shadcn/ui) while fetching
  6. Empty state: "No posted transactions for this account" if transactions array is empty after load. Add suggestion "Check that journal entries are posted (not in Draft status)."

- The beginning balance row should be visually distinct (bold or slightly different background) and NOT sortable/paginated -- it always appears at the top. Implementation: prepend it to the data array with a special flag, or render it as a separate row above the DataTable.
- The footer row should always appear at the bottom of the table regardless of pagination. Use the DataTable's `footerRow` prop.

- Update sidebar navigation to include "GL Ledger" item. Per user decision, sidebar nav order: Dashboard, Chart of Accounts, GL Ledger, Trial Balance, Journal Entries. Find the sidebar/nav config file from Phase 1 and add the GL Ledger entry with a BookOpen or Scroll icon from lucide-react.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>GL Ledger is accessible via sidebar nav and account combobox. Account ledger shows summary card, inline filters, data table with beginning balance and footer totals, and export dropdown. Paginated at 50 per page. Default date range is current month.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- GL Ledger page renders at /gl-ledger with account selector
- Account ledger renders at /gl-ledger/:accountId with full table
- API returns transactions with running balance
- Filters apply with 300ms debounce on memo text
- Export dropdown has CSV and PDF options
- Sidebar nav includes GL Ledger in correct position
</verification>

<success_criteria>
- LED-01: User can view all posted transactions with date, JE#, description, debit, credit, running balance
- LED-02: User can filter by date range, amount range, and memo text with debounced instant apply
- LED-03: Paginated at 50 per page with TanStack Table
- LED-04/LED-05: Export dropdown with CSV and PDF options (uses utilities from Plan 02)
- JE# links to journal entry detail page
- Beginning balance row and totals footer per user decisions
- Account summary card above table per user decision
</success_criteria>

<output>
After completion, create `.planning/phases/03-ledger-and-trial-balance/03-03-SUMMARY.md`
</output>
