---
phase: 03-ledger-and-trial-balance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/data-table/data-table.tsx
  - src/components/data-table/data-table-pagination.tsx
  - src/components/data-table/data-table-column-header.tsx
  - src/components/ui/account-combobox.tsx
  - src/components/ui/date-range-picker.tsx
autonomous: true
requirements: [UI-03]

must_haves:
  truths:
    - "DataTable component renders tabular data with sorting, filtering, and pagination"
    - "Account combobox allows searching accounts by name or number"
    - "Date range picker allows selecting start and end dates"
  artifacts:
    - path: "src/components/data-table/data-table.tsx"
      provides: "Reusable DataTable wrapping TanStack Table v8 + shadcn/ui Table"
      exports: ["DataTable"]
    - path: "src/components/data-table/data-table-pagination.tsx"
      provides: "Pagination controls for DataTable"
      exports: ["DataTablePagination"]
    - path: "src/components/data-table/data-table-column-header.tsx"
      provides: "Sortable column header component"
      exports: ["DataTableColumnHeader"]
    - path: "src/components/ui/account-combobox.tsx"
      provides: "Searchable account selector using shadcn Popover + Command"
      exports: ["AccountCombobox"]
    - path: "src/components/ui/date-range-picker.tsx"
      provides: "Date range picker using shadcn Popover + Calendar in range mode"
      exports: ["DateRangePicker"]
  key_links:
    - from: "src/components/data-table/data-table.tsx"
      to: "@tanstack/react-table"
      via: "useReactTable hook"
      pattern: "useReactTable"
    - from: "src/components/ui/account-combobox.tsx"
      to: "shadcn Command + Popover"
      via: "composition"
      pattern: "CommandInput.*CommandItem"
---

<objective>
Install Phase 3 dependencies and build the shared reusable components that both GL Ledger and Trial Balance pages depend on: the DataTable component (TanStack Table v8 + shadcn/ui), the searchable account combobox, and the date range picker.

Purpose: These shared components are the foundation for both read-side views. Building them first avoids duplication and establishes the data table pattern for the entire app (UI-03).
Output: Reusable DataTable, AccountCombobox, and DateRangePicker components ready for consumption by Plans 02-04.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ledger-and-trial-balance/03-CONTEXT.md
@.planning/phases/03-ledger-and-trial-balance/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and add shadcn/ui components</name>
  <files>package.json</files>
  <action>
Install Phase 3 npm dependencies:
```bash
npm install @tanstack/react-table jspdf jspdf-autotable papaparse
npm install -D @types/papaparse
```

Add required shadcn/ui components (skip any already installed from prior phases):
```bash
npx shadcn@latest add table calendar popover command badge dropdown-menu
```

Verify all packages are in package.json after install.
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); const deps = {...p.dependencies, ...p.devDependencies}; const needed = ['@tanstack/react-table','jspdf','jspdf-autotable','papaparse','@types/papaparse']; const missing = needed.filter(n => !deps[n]); if(missing.length) { console.error('Missing:', missing); process.exit(1); } console.log('All deps installed');"</automated>
  </verify>
  <done>All Phase 3 dependencies installed and lockfile updated</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable DataTable, AccountCombobox, and DateRangePicker components</name>
  <files>
    src/components/data-table/data-table.tsx,
    src/components/data-table/data-table-pagination.tsx,
    src/components/data-table/data-table-column-header.tsx,
    src/components/ui/account-combobox.tsx,
    src/components/ui/date-range-picker.tsx
  </files>
  <action>
**DataTable component** (`src/components/data-table/data-table.tsx`):
- "use client" component wrapping TanStack Table v8 + shadcn/ui Table
- Generic props: `columns: ColumnDef<TData, TValue>[]`, `data: TData[]`, `pageSize?: number` (default 50), `footerRow?: React.ReactNode`
- Internal state: `SortingState`, `ColumnFiltersState`
- Use `getCoreRowModel`, `getPaginationRowModel`, `getSortedRowModel`, `getFilteredRowModel`
- Render using shadcn Table, TableHeader, TableBody, TableRow, TableCell, TableFooter
- Use `flexRender` for cell content
- Include empty state: "No results." message when table has no rows
- Pass `table` instance to DataTablePagination child component

**DataTable Pagination** (`src/components/data-table/data-table-pagination.tsx`):
- Accepts TanStack `Table` instance as prop
- Shows "Page X of Y" text
- Previous/Next buttons using `table.previousPage()` / `table.nextPage()` with disabled states
- First/Last page buttons
- Use shadcn Button component with ChevronLeft/Right and ChevronsLeft/Right icons from lucide-react

**DataTable Column Header** (`src/components/data-table/data-table-column-header.tsx`):
- Accepts `column: Column<TData, TValue>` and `title: string`
- Renders sortable header: click toggles ascending/descending/none
- Shows ArrowUp/ArrowDown/ArrowUpDown icon from lucide-react based on sort state
- Use `cn()` for conditional classes
- Use shadcn DropdownMenu with sort options (Asc, Desc, Hide) -- per shadcn/ui data table docs

**AccountCombobox** (`src/components/ui/account-combobox.tsx`):
- "use client" component using shadcn Popover + Command pattern (per shadcn/ui Combobox docs)
- Props: `accounts: { id: string; accountNumber: string; name: string }[]`, `value: string | null`, `onSelect: (accountId: string) => void`, `placeholder?: string`
- Searchable by both account number and name (CommandItem value = `${accountNumber} ${name}`)
- Shows selected account as `{accountNumber} - {name}` in trigger button
- Shows "Select account..." when no selection
- ChevronsUpDown icon on trigger, Check icon on selected item
- CommandEmpty: "No account found."

**DateRangePicker** (`src/components/ui/date-range-picker.tsx`):
- "use client" component using shadcn Popover + Calendar in range mode
- Props: `dateRange: DateRange | undefined`, `onDateRangeChange: (range: DateRange | undefined) => void`, `className?: string`
- Displays formatted range "MMM d, yyyy - MMM d, yyyy" in trigger button using date-fns format
- CalendarIcon from lucide-react in trigger
- Calendar with `mode="range"`, `numberOfMonths={2}`, `defaultMonth={startOfMonth(new Date())}`
- Import DateRange from react-day-picker
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>DataTable renders with sorting/filtering/pagination support (UI-03), AccountCombobox provides searchable account selection, DateRangePicker provides date range selection -- all type-check cleanly</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- All 5 component files exist with correct exports
- DataTable accepts generic column definitions and data arrays
</verification>

<success_criteria>
- UI-03 requirement satisfied: DataTable component uses TanStack Table with sorting, filtering, and pagination
- AccountCombobox ready for GL Ledger standalone page account selection
- DateRangePicker ready for GL Ledger filter bar
- All components follow shadcn/ui patterns and are "use client" components
</success_criteria>

<output>
After completion, create `.planning/phases/03-ledger-and-trial-balance/03-01-SUMMARY.md`
</output>
