---
phase: 09-bank-transactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/validators/bank-transaction.ts
  - src/lib/bank-transactions/csv-parser.ts
  - src/lib/bank-transactions/duplicate-check.ts
  - src/lib/bank-transactions/categorize.ts
  - src/lib/bank-transactions/create-je.ts
  - tests/bank-transactions/csv-parser.test.ts
  - tests/bank-transactions/duplicate-check.test.ts
  - tests/bank-transactions/categorize.test.ts
  - tests/bank-transactions/create-je.test.ts
  - tests/bank-transactions/plaid-sync.test.ts
autonomous: true
requirements:
  - BANK-01
  - BANK-04
  - BANK-05

must_haves:
  truths:
    - "BankTransaction, PlaidConnection, and CategorizationRule models exist in Prisma schema with correct fields and relations"
    - "CSV text can be parsed into structured transaction objects with auto-detected columns"
    - "Duplicate transactions are detected by externalId (Plaid) or SHA-256 hash (CSV)"
    - "Categorization rules match transactions by case-insensitive description substring and optional amount range"
    - "A categorized bank transaction can be converted into a balanced double-entry journal entry"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "BankTransaction, PlaidConnection, CategorizationRule models + enums"
      contains: "model BankTransaction"
    - path: "src/validators/bank-transaction.ts"
      provides: "Zod schemas for transactions, rules, CSV import"
      exports: ["bankTransactionSchema", "categorizationRuleSchema", "csvImportSchema"]
    - path: "src/lib/bank-transactions/csv-parser.ts"
      provides: "PapaParse-based CSV parser with column auto-detection"
      exports: ["parseBankStatementCsv"]
    - path: "src/lib/bank-transactions/duplicate-check.ts"
      provides: "SHA-256 hash generation and duplicate lookup"
      exports: ["generateTransactionHash", "findDuplicates"]
    - path: "src/lib/bank-transactions/categorize.ts"
      provides: "Rule matching engine"
      exports: ["matchRule", "applyRules"]
    - path: "src/lib/bank-transactions/create-je.ts"
      provides: "Transaction to JournalEntry creation"
      exports: ["createJournalEntryFromTransaction"]
  key_links:
    - from: "src/lib/bank-transactions/csv-parser.ts"
      to: "src/validators/bank-transaction.ts"
      via: "Zod validation of parsed rows"
      pattern: "csvRowSchema\\.safeParse"
    - from: "src/lib/bank-transactions/create-je.ts"
      to: "prisma.journalEntry.create"
      via: "JE creation with debit/credit lines"
      pattern: "journalEntry\\.create"
---

<objective>
Create the Prisma schema, Zod validators, core library modules, and Wave 0 test stubs for bank transaction import and categorization.

Purpose: Establish all data models, type contracts, and business logic foundations that Plans 02-04 build upon. Tests-first ensures each lib module has verifiable behavior before API/UI integration.
Output: 3 new Prisma models, 4 lib modules with tests, Zod validators
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-bank-transactions/09-CONTEXT.md
@.planning/phases/09-bank-transactions/09-RESEARCH.md

<interfaces>
<!-- Existing models the new schema extends -->

From prisma/schema.prisma:
```prisma
model SubledgerItem {
  id              String              @id @default(cuid())
  entityId        String
  accountId       String
  name            String
  itemType        SubledgerItemType
  ...
  reconciliations BankReconciliation[]
  positions       Position[]
  @@map("subledger_items")
}

model Entity {
  id             String         @id @default(cuid())
  ...
  subledgerItems SubledgerItem[]
  dimensions     Dimension[]
  budgets        Budget[]
  eliminationRulesAsA EliminationRule[] @relation("EliminationRuleEntityA")
  eliminationRulesAsB EliminationRule[] @relation("EliminationRuleEntityB")
}

model Account {
  id          String             @id @default(cuid())
  entityId    String
  ...
  subledgerItems SubledgerItem[]
  budgets        Budget[]
  eliminationRulesAsA EliminationRule[] @relation("EliminationRuleAccountA")
  eliminationRulesAsB EliminationRule[] @relation("EliminationRuleAccountB")
}

model JournalEntry {
  id           String              @id @default(cuid())
  entityId     String
  entryNumber  String
  date         DateTime            @db.Date
  description  String
  status       JournalEntryStatus  @default(DRAFT)
  ...
  templateId   String?
  @@map("journal_entries")
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma schema + Zod validators</name>
  <files>prisma/schema.prisma, src/validators/bank-transaction.ts</files>
  <action>
  1. Add three new models to prisma/schema.prisma following the research schema exactly:

  **BankTransaction model** with fields: id (cuid), subledgerItemId, externalId (nullable), date (DateTime @db.Date), description, amount (Decimal 19,4), originalDescription (nullable), merchantName (nullable), category (nullable), source (TransactionSource enum), status (TransactionStatus enum, default PENDING), accountId (nullable), ruleId (nullable), isSplit (Boolean default false), parentTransactionId (nullable), journalEntryId (nullable), createdAt, updatedAt. Self-relation "TransactionSplit" for parent/children. Relations to SubledgerItem, Account, CategorizationRule, JournalEntry, parent/children. Unique constraint on [subledgerItemId, externalId]. Indexes on [subledgerItemId, status] and [subledgerItemId, date]. Map to "bank_transactions".

  **PlaidConnection model** with fields: id (cuid), subledgerItemId (unique -- one connection per bank account), accessToken, itemId, institutionId (nullable), institutionName (nullable), plaidAccountId (nullable), syncCursor (nullable), lastSyncAt (nullable DateTime), status (PlaidConnectionStatus enum, default ACTIVE), error (nullable), createdAt, updatedAt. Relation to SubledgerItem. Index on [status]. Map to "plaid_connections".

  **CategorizationRule model** with fields: id (cuid), entityId, pattern (String), amountMin (nullable Decimal 19,4), amountMax (nullable Decimal 19,4), accountId, dimensionTags (Json nullable), isActive (Boolean default true), priority (Int default 0), createdAt, updatedAt. Relations to Entity, Account, BankTransaction[]. Indexes on [entityId, isActive]. Map to "categorization_rules".

  **Three new enums:**
  - TransactionSource: CSV, PLAID
  - TransactionStatus: PENDING, CATEGORIZED, POSTED, EXCLUDED
  - PlaidConnectionStatus: ACTIVE, ERROR, DISCONNECTED

  **Add relations to existing models:**
  - SubledgerItem: add `bankTransactions BankTransaction[]` and `plaidConnection PlaidConnection?`
  - Entity: add `categorizationRules CategorizationRule[]`
  - Account: add `bankTransactions BankTransaction[]` and `categorizationRules CategorizationRule[]`
  - JournalEntry: add `bankTransaction BankTransaction?`

  2. Run `npx prisma db push` to apply schema changes.

  3. Create `src/validators/bank-transaction.ts` with Zod schemas:
  - `csvRowSchema`: validates a single parsed CSV row (date as string, description as string, amount as string or number)
  - `csvImportSchema`: validates the import request body { csv: string, subledgerItemId: string }
  - `bankTransactionSchema`: validates transaction create/update fields
  - `categorizationRuleSchema`: validates rule create { pattern: string (min 1 char), amountMin?: number, amountMax?: number, accountId: string, dimensionTags?: Record<string, string> }
  - `bulkCategorizeSchema`: validates bulk action { transactionIds: string[], accountId: string, postImmediately?: boolean }
  - `splitLineSchema`: validates a split line { accountId: string, amount: number (positive), memo?: string }
  - `splitTransactionSchema`: validates split request { lines: splitLineSchema[] } with refinement that lines array has >= 2 entries

  Use z from "zod" (already installed as v4.3.6). Follow existing pattern in src/validators/ files.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx prisma validate 2>&1 && npx tsc --noEmit src/validators/bank-transaction.ts 2>&1 | head -20</automated>
  </verify>
  <done>Prisma schema validates with 3 new models and 3 new enums. All existing model relations updated. Zod validators export 7 schemas with correct types. `prisma db push` succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Core lib modules + test stubs</name>
  <files>src/lib/bank-transactions/csv-parser.ts, src/lib/bank-transactions/duplicate-check.ts, src/lib/bank-transactions/categorize.ts, src/lib/bank-transactions/create-je.ts, tests/bank-transactions/csv-parser.test.ts, tests/bank-transactions/duplicate-check.test.ts, tests/bank-transactions/categorize.test.ts, tests/bank-transactions/create-je.test.ts, tests/bank-transactions/plaid-sync.test.ts</files>
  <behavior>
    csv-parser.test.ts:
    - parseBankStatementCsv with standard columns (Date, Description, Amount) returns correct objects
    - Auto-detects split debit/credit columns (Debit, Credit) and converts to single signed amount
    - Handles alternate column names (Posted Date, Memo, Transaction Amount)
    - Throws on empty CSV or missing required columns
    - Skips empty rows
    - Handles quoted fields with commas inside

    duplicate-check.test.ts:
    - generateTransactionHash produces consistent SHA-256 for same inputs
    - Different inputs produce different hashes
    - Hash normalizes amount to 4 decimal places
    - Hash lowercases and trims description

    categorize.test.ts:
    - matchRule returns matching rule for case-insensitive substring in description
    - matchRule respects amountMin/amountMax range (uses absolute amount)
    - matchRule returns null when no rules match
    - matchRule returns first matching rule (priority order)
    - applyRules assigns accountId and dimensionTags from matched rule

    create-je.test.ts:
    - createJournalEntryFromTransaction produces balanced debit/credit lines
    - Handles split transactions (multiple debit lines, one credit line to bank account)
    - Split amounts must sum to original transaction amount (validation)
    - Supports postImmediately flag (POSTED vs DRAFT status)

    plaid-sync.test.ts:
    - Test stubs (it.todo) for sync loop, cursor update, error handling (Plaid integration tested in Plan 03)
  </behavior>
  <action>
  Create test files FIRST with concrete test cases, then implement each lib module to make tests pass.

  **csv-parser.ts:** Use PapaParse (already installed) with `header: true, skipEmptyLines: true, dynamicTyping: false`. Implement `detectColumns(headers: string[])` using COLUMN_PATTERNS map from research. Export `parseBankStatementCsv(csvText: string): ParsedBankRow[]` that returns `{ date: string, description: string, amount: number, reference?: string }[]`. Handle both single-amount column and separate debit/credit columns (debit as negative, credit as positive for GL convention). Throw descriptive error if required columns (date, description, amount or debit+credit) cannot be detected.

  **duplicate-check.ts:** Export `generateTransactionHash(date: string, amount: string, description: string, rowIndex?: number): string` using `crypto.createHash('sha256')`. Normalize: trim description, lowercase, format amount to 4 decimal places. Export `findDuplicates(subledgerItemId: string, hashes: string[]): Promise<Set<string>>` that queries BankTransaction by externalId. Include rowIndex in hash only when provided (for same-day same-amount same-description disambiguation).

  **categorize.ts:** Export `matchRule(transaction: { description: string, amount: number | Decimal }, rules: CategorizationRule[]): CategorizationRule | null`. Rules should be pre-sorted by priority (lower = higher priority). Case-insensitive `.includes()` for description match. Use `Math.abs(Number(amount))` for amount range check. Export `applyRules(transactions: BankTransaction[], rules: CategorizationRule[]): { matched: BankTransaction[], unmatched: BankTransaction[] }` that assigns accountId and dimensionTags from matched rule.

  **create-je.ts:** Export `createJournalEntryFromTransaction(params: { transaction: BankTransaction, bankAccountId: string, entityId: string, userId: string, splits?: SplitLine[], postImmediately: boolean }): JournalEntryCreateInput`. Creates balanced JE: for non-split, single debit line to categorized account + single credit line to bank account (or vice versa for deposits). For splits, multiple debit lines + one credit line summing to transaction amount. Validate split amounts sum to original. Use existing `generateNextEntryNumber` pattern from `src/lib/journal-entries/`.

  **plaid-sync.test.ts:** Create with `it.todo()` stubs only -- covers BANK-02 behavior that will be implemented in Plan 03.

  For all test files, use vitest (`describe`, `it`, `expect`) matching existing test conventions. Mock Prisma calls where needed using vi.mock.
  </action>
  <verify>
    <automated>cd "/Users/sahilnandwani/Desktop/Frontier GL Claude App" && npx vitest run tests/bank-transactions/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All csv-parser, duplicate-check, categorize, and create-je tests pass. plaid-sync.test.ts has todo stubs. 4 lib modules export correct functions with TypeScript types.</done>
</task>

</tasks>

<verification>
- `npx prisma validate` passes (schema is valid)
- `npx vitest run tests/bank-transactions/ --reporter=verbose` -- all non-todo tests pass
- `npx tsc --noEmit` -- no type errors in new files
</verification>

<success_criteria>
- 3 new Prisma models (BankTransaction, PlaidConnection, CategorizationRule) with correct fields, relations, and indexes
- 3 new enums (TransactionSource, TransactionStatus, PlaidConnectionStatus)
- Existing models (SubledgerItem, Entity, Account, JournalEntry) have new relation fields
- 7 Zod validator schemas exported
- 4 lib modules with passing unit tests
- 1 test stub file for plaid-sync
</success_criteria>

<output>
After completion, create `.planning/phases/09-bank-transactions/09-01-SUMMARY.md`
</output>
