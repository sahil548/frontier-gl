---
phase: 08-family-office-i-multi-entity-consolidation
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/lib/validators/elimination-rule.ts
  - src/app/api/consolidated/elimination-rules/route.ts
  - src/app/api/consolidated/elimination-rules/[ruleId]/route.ts
  - src/app/(auth)/settings/eliminations/page.tsx
  - src/components/settings/elimination-rule-form.tsx
  - src/components/settings/elimination-rules-table.tsx
  - src/components/layout/sidebar.tsx
autonomous: true
requirements: [CONS-03, CONS-04]

must_haves:
  truths:
    - "User can create an elimination rule with label, entity pair, and account pair"
    - "User can view all elimination rules with exposure summary"
    - "User can toggle rules active/inactive"
    - "Only users with Owner role on both entities can create/edit rules"
    - "Settings page is accessible from sidebar navigation"
  artifacts:
    - path: "src/app/api/consolidated/elimination-rules/route.ts"
      provides: "GET (list) and POST (create) for elimination rules"
      exports: ["GET", "POST"]
    - path: "src/app/api/consolidated/elimination-rules/[ruleId]/route.ts"
      provides: "PATCH (update) and DELETE (soft-delete) for elimination rules"
      exports: ["PATCH", "DELETE"]
    - path: "src/app/(auth)/settings/eliminations/page.tsx"
      provides: "Settings page for intercompany elimination rules management"
      min_lines: 50
    - path: "src/components/settings/elimination-rule-form.tsx"
      provides: "Form for creating/editing elimination rules"
    - path: "src/components/settings/elimination-rules-table.tsx"
      provides: "Table listing rules with exposure summary"
  key_links:
    - from: "elimination-rule-form.tsx"
      to: "/api/consolidated/elimination-rules"
      via: "POST fetch on submit"
      pattern: "fetch.*elimination-rules"
    - from: "elimination-rules-table.tsx"
      to: "/api/consolidated/elimination-rules/[ruleId]"
      via: "PATCH for toggle, DELETE for remove"
      pattern: "fetch.*elimination-rules"
    - from: "sidebar.tsx"
      to: "/settings/eliminations"
      via: "navigation link"
      pattern: "eliminations"
---

<objective>
Build the elimination rules CRUD system with settings page UI.

Purpose: Accountants need to define intercompany elimination rules (entity/account pairs) before consolidated reports can show eliminations. This plan creates the full CRUD flow: API routes, validation, settings page with exposure summary, and rule management form.
Output: Elimination rules API (list, create, update, toggle), Settings > Intercompany Eliminations page with table and form.
</objective>

<execution_context>
@/Users/sahilnandwani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sahilnandwani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-family-office-i-multi-entity-consolidation/08-CONTEXT.md
@.planning/phases/08-family-office-i-multi-entity-consolidation/08-RESEARCH.md
@.planning/phases/08-family-office-i-multi-entity-consolidation/08-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 -->
From src/types/consolidated.ts:
```typescript
export interface EliminationRuleInput { label: string; entityAId: string; accountAId: string; entityBId: string; accountBId: string; }
export interface SerializedEliminationRule { id: string; label: string; entityAId: string; entityAName: string; accountAId: string; accountANumber: string; accountAName: string; entityBId: string; entityBName: string; accountBId: string; accountBNumber: string; accountBName: string; isActive: boolean; createdAt: string; }
```

From src/lib/db/entity-access.ts:
```typescript
export async function getInternalUser(clerkUserId: string);
export async function getAccessibleEntityIds(clerkUserId: string): Promise<string[]>;
```

From src/lib/api/response.ts:
```typescript
export function successResponse(data: unknown, status?: number): NextResponse;
export function errorResponse(message: string, status: number, details?: unknown): NextResponse;
```

Existing settings page pattern (src/app/(auth)/settings/page.tsx):
- "use client" page under (auth) route group
- Uses Card, CardHeader, CardTitle, CardContent from shadcn
- Fetches data via useEffect + fetch

Sidebar pattern (src/components/layout/sidebar.tsx):
- NavLink items with icon + label
- Settings section already exists with sub-items
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create elimination rules API routes with authorization</name>
  <files>src/lib/validators/elimination-rule.ts, src/app/api/consolidated/elimination-rules/route.ts, src/app/api/consolidated/elimination-rules/[ruleId]/route.ts</files>
  <action>
1. Create src/lib/validators/elimination-rule.ts:
   - createEliminationRuleSchema: z.object({ label: z.string().min(1).max(200), entityAId: z.string().min(1), accountAId: z.string().min(1), entityBId: z.string().min(1), accountBId: z.string().min(1) }). Add refinement: entityAId !== entityBId (rule must span two different entities).
   - updateEliminationRuleSchema: z.object({ label: z.string().min(1).max(200).optional(), entityAId, accountAId, entityBId, accountBId all optional, isActive: z.boolean().optional() })

2. Create helper function canManageEliminationRule(clerkUserId, entityAId, entityBId) in src/lib/db/entity-access.ts (or colocate in the route file if simpler):
   - Get internal user via getInternalUser
   - Check EntityAccess for OWNER role on both entityAId and entityBId
   - Return boolean

3. Create GET /api/consolidated/elimination-rules:
   - Auth: get userId from auth()
   - Get accessible entity IDs via getAccessibleEntityIds
   - Fetch all elimination rules where (entityAId in accessible AND entityBId in accessible), include entityA (select name), accountA (select number, name), entityB (select name), accountB (select number, name)
   - Serialize to SerializedEliminationRule[] format
   - Return successResponse({ data: rules })

4. Create POST /api/consolidated/elimination-rules:
   - Validate body with createEliminationRuleSchema
   - Check canManageEliminationRule -- return 403 "Must be Owner on both entities"
   - Verify both accounts exist and belong to their respective entities
   - Create EliminationRule with createdBy = internal user ID
   - Return successResponse({ data: serialized rule }, 201)

5. Create PATCH /api/consolidated/elimination-rules/[ruleId]:
   - Validate body with updateEliminationRuleSchema
   - Fetch existing rule, verify user has Owner on both entities (current or new if entity IDs changing)
   - Update rule
   - Return successResponse({ data: serialized rule })

6. Create DELETE /api/consolidated/elimination-rules/[ruleId]:
   - This is a soft-delete: set isActive = false (per soft-delete pattern)
   - Verify Owner on both entities
   - Return successResponse({ data: { id: ruleId, isActive: false } })
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Frontier\ GL\ Claude\ App && npx tsc --noEmit src/app/api/consolidated/elimination-rules/route.ts src/app/api/consolidated/elimination-rules/\[ruleId\]/route.ts src/lib/validators/elimination-rule.ts 2>&1 | head -20</automated>
  </verify>
  <done>Elimination rules CRUD API compiles, GET lists rules user can see, POST/PATCH require Owner on both entities, DELETE soft-deactivates</done>
</task>

<task type="auto">
  <name>Task 2: Create Settings > Intercompany Eliminations page with rule management</name>
  <files>src/app/(auth)/settings/eliminations/page.tsx, src/components/settings/elimination-rule-form.tsx, src/components/settings/elimination-rules-table.tsx, src/components/layout/sidebar.tsx</files>
  <action>
1. Create src/components/settings/elimination-rules-table.tsx:
   - Props: rules: SerializedEliminationRule[], onToggle: (ruleId, isActive) => void, onEdit: (rule) => void
   - Display a table with columns: Label | Entity A > Account A | Entity B > Account B | Status (active/inactive Badge) | Actions (toggle Switch, edit Button)
   - Above the table, show an exposure summary card: "X active rules | Y total intercompany pairs" (simple count, not balance amounts -- balance amounts require fetching report data which is expensive; keep exposure summary lightweight)
   - Empty state: "No intercompany elimination rules defined. Create one to eliminate intercompany balances from consolidated reports."

2. Create src/components/settings/elimination-rule-form.tsx:
   - Props: rule?: SerializedEliminationRule (for edit mode), entities: { id, name }[], onSuccess: () => void, onCancel: () => void
   - Form fields: Label (text input), Entity A (select from entities list), Account A (account combobox filtered by selected Entity A), Entity B (select), Account B (account combobox filtered by Entity B)
   - Account comboboxes: fetch accounts from /api/entities/{entityId}/accounts when entity is selected
   - On submit: POST to /api/consolidated/elimination-rules (create) or PATCH to /api/consolidated/elimination-rules/{ruleId} (edit)
   - Show toast via sonner on success/error
   - Uses react-hook-form with zodResolver and createEliminationRuleSchema
   - Dialog/Sheet pattern: open form in a Sheet (slide-over) consistent with COA create pattern

3. Create src/app/(auth)/settings/eliminations/page.tsx:
   - "use client" page
   - Fetch rules from GET /api/consolidated/elimination-rules on mount
   - Fetch entities from GET /api/entities for the entity selectors
   - State: rules[], showForm (boolean), editingRule (SerializedEliminationRule | null)
   - Render: page title "Intercompany Eliminations", description text, Create Rule button
   - Render EliminationRulesTable with toggle handler (PATCH to toggle isActive) and edit handler (opens form in edit mode)
   - Render EliminationRuleForm in Sheet when showForm is true
   - After create/edit/toggle, refetch rules list

4. Add sidebar navigation link:
   - In src/components/layout/sidebar.tsx, add "Eliminations" link under Settings section
   - Use ArrowLeftRight icon from lucide-react (or similar icon suggesting intercompany)
   - href: /settings/eliminations
  </action>
  <verify>
    <automated>cd /Users/sahilnandwani/Desktop/Frontier\ GL\ Claude\ App && npx tsc --noEmit src/app/\(auth\)/settings/eliminations/page.tsx src/components/settings/elimination-rule-form.tsx src/components/settings/elimination-rules-table.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Settings > Intercompany Eliminations page renders with rule table, exposure summary, create/edit form in Sheet, toggle active/inactive, sidebar link added</done>
</task>

</tasks>

<verification>
- All TypeScript files compile without errors
- API routes enforce Owner-on-both-entities authorization
- Settings page fetches and displays rules
- Sidebar shows Eliminations link under Settings
</verification>

<success_criteria>
- CRUD API: list, create, update, soft-delete for elimination rules
- Owner role required on both entities for create/edit (403 otherwise)
- Settings page shows rule table with exposure summary, create/edit Sheet form
- Entity-scoped account comboboxes in form (fetch accounts when entity selected)
- Sidebar navigation includes Eliminations link
</success_criteria>

<output>
After completion, create `.planning/phases/08-family-office-i-multi-entity-consolidation/08-03-SUMMARY.md`
</output>
