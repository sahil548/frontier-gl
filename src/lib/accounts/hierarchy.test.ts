import { describe, it } from "vitest";

describe("Account Hierarchy", () => {
  it.todo("rejects 3rd-level nesting");
  it.todo("requires parent to exist");
  it.todo("sub-account inherits parent type");
  it.todo("parent cannot be deactivated with active children");
});
