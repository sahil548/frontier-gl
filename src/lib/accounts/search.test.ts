import { describe, it } from "vitest";

describe("Account Search", () => {
  it.todo("filters by name substring");
  it.todo("filters by number prefix");
  it.todo("combines name and type filter");
  it.todo("returns all when no filter");
});
