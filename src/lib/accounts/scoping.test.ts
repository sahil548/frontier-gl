import { describe, it } from "vitest";

describe("Account Entity Scoping", () => {
  it.todo("returns only accounts for given entityId");
  it.todo("rejects cross-entity account access");
  it.todo("template applies to specific entity only");
});
