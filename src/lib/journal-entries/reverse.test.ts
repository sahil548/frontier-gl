import { describe, it } from "vitest";

describe("Reversal Entries", () => {
  it.todo("creates draft with flipped debits/credits");
  it.todo("links reversal to original via reversalOfId");
  it.todo("rejects reversal of non-posted entry");
  it.todo("auto-generates entry number");
});
