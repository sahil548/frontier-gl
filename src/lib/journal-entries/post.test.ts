import { describe, it } from "vitest";

describe("Journal Entry Posting", () => {
  it.todo("updates AccountBalance atomically");
  it.todo("rejects posting to closed period");
  it.todo("rejects posting already-posted entry");
  it.todo("creates audit trail on post");
});
