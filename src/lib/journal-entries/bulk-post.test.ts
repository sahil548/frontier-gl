import { describe, it } from "vitest";

describe("Bulk Post", () => {
  it.todo("posts multiple entries in single transaction");
  it.todo("rolls back all on single failure");
  it.todo("updates all account balances");
  it.todo("creates audit entries for each");
});
